# 管理后台 - 业务逻辑详解

> 编写：Claude Code | 日期：2026-02-18 | 目标读者：产品经理（无需技术背景）

---

## 一、管理后台概述

### 1.1 管理后台是什么？

管理后台是系统管理员（你）使用的「控制面板」，用来：
- **配置**演练场景和角色
- **管理**用户和权限
- **查看**演练数据和报告
- **维护**系统正常运行

简单说，就是「管理这个 AI 演练系统的地方」。

### 1.2 管理后台能做什么？

```mermaid
mindmap
  root((管理后台功能))
    场景管理
      创建 Agent
      配置 Persona
    PPT 管理
      上传 PPT
      配置要点
    知识库管理
      上传文档
      问答对管理
    语音配置
      选择语音
      调整语速
    用户管理
      添加用户
      分配权限
    数据分析
      查看报表
      导出数据
```

---

## 二、核心功能模块

### 2.1 功能模块一览

```mermaid
flowchart TB
    subgraph "管理后台功能"
        A[Agent 管理] --> E[销售场景配置]
        A --> F[PPT 演练配置]

        B[Persona 管理] --> G[客户角色配置]
        B --> H[评委角色配置]

        C[知识库管理] --> I[产品资料库]
        C --> J[常见问答库]

        D[演练记录] --> K[查看历史]
        D --> L[分析报告]
    end

    style A fill:#e8f5e9
    style B fill:#fff3e0
    style C fill:#e1f5fe
    style D fill:#fce4ec
```

---

## 三、Agent 管理

### 3.1 Agent 是什么？

**Agent = 演练场景的配置包**

想象 Agent 是一份「培训课程大纲」，定义了：
- 这次演练的目标是什么
- 评估标准是什么
- AI 需要具备哪些能力

### 3.2 创建 Agent 的流程

```mermaid
flowchart TB
    subgraph "创建 Agent 流程"
        A[点击"新建 Agent"] --> B[填写基本信息]
        B --> C[选择场景类型]
        C --> D[配置能力模块]
        D --> E[设置评分标准]
        E --> F[保存并发布]
    end

    style A fill:#e1f5fe
    style F fill:#e8f5e9
```

### 3.3 Agent 配置项

```mermaid
mindmap
  root((Agent 配置项))
    基本信息
      Agent 名称
      场景描述
      适用人群
    场景类型
      销售对练
      PPT 演练
      面试练习
    能力模块
      模糊话术检测
      知识库检索
      实时评分
      销售阶段识别
    评分权重
      逻辑性
      准确度
      完整度
```

### 3.4 Agent 状态管理

```mermaid
stateDiagram-v2
    [*] --> 草稿: 创建 Agent

    草稿 --> 已发布: 点击"发布"
    已发布 --> 已归档: 场景过时
    已归档 --> [*]

    草稿 --> 已发布: 测试通过
    已发布 --> 草稿: 修改配置
```

---

## 四、Persona 管理

### 4.1 Persona 是什么？

**Persona = AI 扮演的角色设定**

想象 Persona 是一份「角色剧本」，告诉 AI：
- 你是谁？（角色背景）
- 你什么性格？（态度、语气）
- 你会遇到什么问题？（常见挑战）

### 4.2 创建 Persona 的流程

```mermaid
flowchart TB
    subgraph "创建 Persona 流程"
        A[点击"新建角色"] --> B[填写角色信息]
        B --> C[设定性格特征]
        C --> D[编写角色话术]
        D --> E[设置难度等级]
        E --> F[保存]
    end

    style A fill:#fff3e0
    style F fill:#e8f5e9
```

### 4.3 Persona 配置详解

```mermaid
mindmap
  root((Persona 配置))
    角色基本信息
      角色名称
      角色背景故事
      职位/身份
    性格特征
      态度
        友好型
        中立型
        质疑型
      决策风格
        快速决定
        谨慎考虑
      预算敏感度
        高
        中
        低
    难度等级
      简单
      中等
      困难
    话术设定
      开场白
      常见问题
      异议表达
```

### 4.4 典型 Persona 示例

```mermaid
graph LR
    subgraph "销售对练 Persona 示例"
        P1[友好型客户] -->|特点| F1[容易沟通]
        P2[质疑型客户] -->|特点| F2[爱提问题]
        P3[强硬型客户] -->|特点| F3[要求高]

        F1 --> C1[适合新人练习]
        F2 --> C2[适合进阶练习]
        F3 --> C3[适合高级练习]
    end
```

---

## 五、知识库管理

### 5.1 知识库是什么？

**知识库 = AI 的「产品手册」**

当 AI 和用户对话时，它需要知道产品的真实信息，这些信息就存在知识库里。

### 5.2 知识库的作用

```mermaid
sequenceDiagram
    participant 销售 as 销售人员
    participant AI as AI 客户
    participant 知识库 as 知识库

    销售: 我们的产品可以提升效率
    AI->>知识库: 查询"效率提升"
    知识库-->>AI: 返回产品资料

    AI: 具体能提升多少？
    销售: 30% 左右
    AI->>知识库: 验证"30%"
    知识库-->>AI: 文档说是 25-30%

    AI: 好的，那具体怎么实现？
```

### 5.3 知识库管理功能

```mermaid
flowchart TB
    subgraph "知识库管理"
        A[文档管理] --> B[上传文档]
        B --> C[PDF/Word/Text]
        C --> D[自动向量化]

        E[问答对管理] --> F[添加问答]
        F --> G[问题]
        F --> H[答案]

        I[知识检索测试] --> J[输入测试问题]
        J --> K[查看检索结果]
    end

    style A fill:#e1f5fe
    style E fill:#fff3e0
```

### 5.4 知识库配置流程

```mermaid
flowchart TB
    A[创建知识库] --> B{选择类型}
    B -->|文档库| C[上传文档]
    B -->|问答库| D[添加问答]
    C --> E[系统处理]
    D --> E
    E --> F[验证检索]
    F --> G[完成]
```

---

## 六、PPT 管理

### 6.1 PPT 管理功能

```mermaid
mindmap
  root((PPT 管理))
    PPT 上传
      支持格式
      文件大小限制
    内容解析
      文字提取
      页面结构
    要点配置
      每页必讲点
      重点标注
    禁忌词配置
      敏感词库
      违规提醒
```

### 6.2 要点配置界面

```mermaid
flowchart TB
    subgraph "要点配置流程"
        A[上传 PPT] --> B[系统解析]
        B --> C[显示页面列表]
        C --> D[选择页面]
        D --> E[添加要点]
        E --> F[设置禁忌词]
        F --> G[保存]
    end
```

---

## 七、语音配置

### 7.1 语音配置的意义

用户和 AI 对话时，需要：
- **TTS**：AI 说话的声音（文字转语音）
- **ASR**：用户说话被识别（语音转文字）

### 7.2 可配置的语音参数

```mermaid
mindmap
  root((语音配置))
    TTS 配置
      语音选择
        中文男声
        中文女声
      语速调节
        慢
        正常
        快
      音量调节
    ASR 配置
      识别模型
        通用模型
        行业模型
      方言支持
    语音模式
      标准模式
      实时模式
```

---

## 八、用户管理

### 8.1 用户管理功能

```mermaid
mindmap
  root((用户管理))
    用户列表
      查看用户
      编辑信息
      禁用/启用
    角色分配
      管理员
      普通用户
    权限管理
      可访问模块
      可演练场景
```

### 8.2 用户权限模型

```mermaid
flowchart TB
    subgraph "用户权限"
        A[用户] --> B[管理员]
        A --> C[普通用户]

        B -->|拥有| D[全部权限]
        C -->|拥有| E[部分权限]

        D --> E1[用户管理]
        D --> E2[Agent 配置]
        D --> E3[数据查看]

        E --> F1[开始演练]
        E --> F2[查看自己的报告]
    end
```

---

## 九、数据分析

### 9.1 数据分析功能

```mermaid
mindmap
  root((数据分析))
    整体概览
      总用户数
      总演练次数
      活跃度趋势
    个人报告
      个人得分趋势
      能力雷达图
      历史记录
    团队报告
      团队平均分
      薄弱环节
      排名统计
```

### 9.2 数据可视化

```mermaid
graph LR
    subgraph "可视化图表"
        A[折线图] --> B[趋势分析]
        C[柱状图] --> D[对比分析]
        E[雷达图] --> F[能力分析]
        G[饼图] --> H[占比分析]
    end
```

---

## 十、演练记录

### 10.1 演练记录功能

```mermaid
mindmap
  root((演练记录))
    记录列表
      按时间筛选
      按场景筛选
      按用户筛选
    详情查看
      会话内容
      评估报告
      音频回放
    数据导出
      Excel 导出
      PDF 报告
```

### 10.2 记录详情页面

```mermaid
flowchart TB
    A[选择一条记录] --> B[查看详情]
    B --> C[基本信息]
    C --> D[用户、场景、时间]
    B --> E[对话内容]
    E --> F[逐轮对话]
    B --> G[评估结果]
    G --> H[得分明细]
    B --> I[操作]
    I --> J[导出/删除]
```

---

## 十一、工作流程总结

### 11.1 管理员日常操作

```mermaid
flowchart LR
    subgraph "日常操作"
        A[日常] --> B[查看数据]
        B --> C[处理异常]
        C --> D[优化配置]
        D --> E[周期性任务]
    end

    B -->|查看| F[仪表盘]
    C -->|用户反馈| G[调整 Agent]
    C -->|数据异常| H[排查问题]
    D -->|更新| I[优化话术]
    D -->|补充| J[完善知识库]
```

### 11.2 新场景创建流程

```mermaid
flowchart TB
    A[需要新场景] --> B{场景类型}
    B -->|销售| C[创建 Agent]
    B -->|演讲| D[上传 PPT]

    C --> E[配置 Persona]
    D --> F[配置要点]

    E --> G[关联知识库]
    F --> G

    G --> H[测试演练]
    H -->|有问题| I[调整配置]
    H -->|通过| J[发布上线]
```

---

## 十二、总结

管理后台的核心价值：

1. **配置能力**：定义各种演练场景和角色
2. **内容管理**：维护产品知识库和话术
3. **数据洞察**：了解用户演练情况和效果
4. **运营支持**：日常运维和异常处理

通过管理后台，你可以：
- 快速创建新的销售场景
- 调整 AI 扮演的客户性格
- 更新产品资料库
- 查看培训效果数据
