# PPT 演讲演练场景 - 业务逻辑详解

> 编写：Claude Code | 日期：2026-02-18 | 目标读者：产品经理（无需技术背景）

---

## 一、场景概述

### 1.1 PPT 演讲演练是什么？

想象一下，你要做一场重要的演讲（产品发布会、工作汇报、竞标演示），但在正式上台前，你希望能有人帮你：

1. **提醒你漏掉了重点内容** → 系统检测你是否有遗漏关键要点
2. **指出你说错了什么** → 系统检测你是否说了禁忌词
3. **实时给你反馈** → 讲得不好时 AI 教练即时提醒

**PPT 演讲演练场景的价值**：让 AI 充当你的「演讲教练」，在模拟演讲过程中实时指导你。

### 1.2 PPT 演练 vs 销售对练

| 对比维度 | PPT 演讲演练 | 销售对练 |
|---------|-------------|---------|
| **AI 角色** | 教练（指导者） | 客户（对话者） |
| **交互方式** | 单向演讲 + 即时点评 | 双向对话 |
| **核心检测** | 要点覆盖 + 禁忌词 | 话术技巧 |
| **反馈时机** | 演讲过程中实时 | 对话过程中实时 |

---

## 二、核心流程总览

### 2.1 完整业务流程

```mermaid
flowchart TB
    subgraph "PPT 演练完整流程"
        A[管理员上传 PPT] --> B[系统解析 PPT]
        B --> C[管理员配置要点和禁忌词]
        C --> D[用户选择 PPT 开始演练]

        D --> E[用户翻到第 N 页]
        E --> F[加载该页要点]
        F --> G[用户开始演讲]

        G --> H{用户说的内容}
        H -->|覆盖要点| I[标记为已讲]
        H -->|提到禁忌词| J[记录禁忌词]
        H -->|模糊话术| K[标记模糊]

        G --> L[AI 教练实时反馈]
        I --> L
        J --> L
        K --> L

        L --> M{继续演讲？}
        M -->|是| G
        M -->|否| N[翻页或结束]

        N --> O[翻到下一页]
        O --> E
        N --> P[结束演练]

        P --> Q[生成评估报告]
    end

    style A fill:#e1f5fe
    style D fill:#fff3e0
    style L fill:#e8f5e9
    style Q fill:#fce4ec
```

---

## 三、PPT 管理与配置

### 3.1 管理员的工作：准备 PPT

当管理员要创建一个演讲演练时，需要做以下事情：

```mermaid
flowchart TB
    subgraph "管理员准备流程"
        A[上传 PPT 文件] --> B[系统自动解析]
        B --> C[提取每页文字]
        C --> D[配置演讲要点]
        D --> E[配置禁忌词]
        E --> F[保存配置]
    end
```

### 3.2 演讲要点配置

每个 PPT 页面可以配置「必讲要点」，即用户演讲时必须覆盖的内容：

```mermaid
mindmap
  root((页面 1: 产品介绍))
    必讲要点（用户必须提到）
      公司成立时间
      核心产品功能
      目标客户群体
      竞争优势
    禁忌词（用户不能说）
      最便宜
      100% 有效
      绝对没问题
```

**举例说明**：

假设有一个产品介绍 PPT，第 3 页是「产品功能」：

| 配置类型 | 配置内容 |
|---------|---------|
| **必讲要点** | 1. 功能 A：自动化流程<br/>2. 功能 B：数据分析<br/>3. 功能 C：团队协作 |
| **禁忌词** | 1. 「最便宜」<br/>2. 「保证效果」<br/>3. 「独一无二」 |

用户在演讲时：
- ✅ 提到「自动化流程」→ 系统标记要点已覆盖
- ❌ 提到「最便宜」→ 系统标记禁忌词触犯

---

## 四、用户演讲流程

### 4.1 用户视角的操作流程

```mermaid
sequenceDiagram
    participant 用户 as 用户（你）
    participant 前端 as 页面
    participant 后端 as 服务器

    Note over 用户,前端: 步骤 1：选择 PPT
    用户->>前端: 选择要演练的 PPT
    前端-->>用户: 显示 PPT 预览

    Note over 用户,前端: 步骤 2：开始演练
    用户->>前端: 点击"开始演练"
    前端->>后端: WebSocket 连接
    后端-->>前端: 加载第 1 页要点

    Note over 用户,前端: 步骤 3：翻页
    用户->>前端: 翻到第 2 页
    前端->>后端: 请求加载第 2 页配置
    后端-->>前端: 返回第 2 页要点和禁忌词

    Note over 用户,前端: 步骤 4：开始演讲
    用户->>前端: 对着麦克风演讲
    前端->>后端: 发送语音流

    rect rgb(240, 248, 255)
    Note over 后端: AI 实时处理
    后端->>后端: 语音识别
    后端->>后端: 检测要点覆盖
    后端->>后端: 检测禁忌词
    end

    后端-->>前端: 实时反馈
    前端->>用户: 显示：✅ 已覆盖"核心功能"<br/>⚠️ 提到了禁忌词"最便宜"
    用户->>前端: 继续演讲...

    Note over 用户,前端: 步骤 5：结束
    用户->>前端: 点击"结束演练"
    前端->>后端: 结束会话
    后端-->>前端: 返回评估报告
    前端->>用户: 显示详细报告
```

---

## 五、实时反馈机制

### 5.1 AI 教练做什么？

当用户演讲时，AI 教练同时在做以下几件事情：

```mermaid
flowchart TB
    subgraph "AI 教练的工作"
        A[用户的语音] --> B[语音识别成文字]
    end

    subgraph "要点检测"
        B --> C1{是否提到要点 1？}
        C1 -->|是| D1[✅ 标记已覆盖]
        C1 -->|否| E1[❌ 未覆盖]
    end

    subgraph "禁忌词检测"
        B --> C2{是否说到禁忌词？}
        C2 -->|是| D2[⚠️ 记录禁忌词]
        C2 -->|否| E2[✅ 正常]
    end

    subgraph "模糊话术检测"
        B --> C3[检测模糊表达]
        C3 --> D3[💡 建议优化表达]
    end

    subgraph "综合反馈"
        D1 --> F[生成实时反馈]
        E1 --> F
        D2 --> F
        E2 --> F
        D3 --> F
    end

    F --> G[推送给用户]
```

### 5.2 反馈内容示例

用户在演讲过程中，可能收到的实时反馈：

```mermaid
mindmap
  root((实时反馈))
    要点覆盖
      ✅ 已覆盖
        "提到了核心功能"
      ❌ 未覆盖
        "还没提到竞争优势"
    禁忌词
      ⚠️ 警告
        "刚才说了'最便宜'"
    表达优化
      💡 建议
        "'我觉得'有点模糊<br/>建议改成具体数据"
    整体进度
      📊 进度
        "已覆盖 3/5 个要点"
```

---

## 六、演讲过程详解

### 6.1 单页演讲流程

以某一页 PPT 为例，用户演讲时的完整流程：

```mermaid
stateDiagram-v2
    [*] --> 加载页面: 用户翻到这一页

    加载页面 --> 显示要点: 系统显示该页要点
    显示要点 --> 开始演讲: 用户开始说话

    状态: 演讲中
    开始演讲 --> 实时检测: ASR 识别语音
    实时检测 --> 检测要点覆盖: AI 分析内容
    检测要点覆盖 --> 更新状态: 标记已讲要点
    实时检测 --> 检测禁忌词: AI 分析内容
    检测禁忌词 --> 更新警告: 记录禁忌词
    实时检测 --> 发送反馈: 推送给用户

    更新状态 --> 继续演讲: 用户继续
    更新警告 --> 继续演讲: 用户继续
    发送反馈 --> 继续演讲: 用户继续

    继续演讲 --> 翻页: 用户翻到下一页
    翻页 --> [*]

    继续演讲 --> 结束: 用户结束演练
    结束 --> [*]
```

### 6.2 多页演讲流程

用户通常需要演讲多页 PPT：

```mermaid
flowchart LR
    subgraph "多页演讲"
        P1[第 1 页] -->|演讲| C1[检测要点+禁忌词]
        C1 --> R1[实时反馈]

        P2[第 2 页] -->|演讲| C2[检测要点+禁忌词]
        C2 --> R2[实时反馈]

        P3[第 3 页] -->|演讲| C3[检测要点+禁忌词]
        C3 --> R3[实时反馈]

        P4[第 4 页] -->|演讲| C4[检测要点+禁忌词]
        C4 --> R4[实时反馈]

        R4 --> END[生成最终报告]
    end
```

---

## 七、评估报告

### 7.1 报告包含的内容

当用户结束演练后，会生成一份详细的评估报告：

```mermaid
mindmap
  root((评估报告))
    整体得分
      综合得分
      要点覆盖率
      禁忌词触犯次数
    逐页分析
      第 1 页
        要点覆盖情况
        禁忌词情况
      第 2 页
        ...
    时间分析
      每页用时
      节奏评估
    改进建议
      遗漏要点
      禁忌词提醒
      表达优化建议
```

### 7.2 报告可视化示例

```mermaid
graph LR
    subgraph "报告可视化"
        A[雷达图] --> B[展示各维度得分]
        C[进度条] --> D[要点覆盖进度]
        E[时间轴] --> F[各页用时分布]
    end
```

---

## 八、技术实现要点（可选了解）

### 8.1 PPT 解析流程

```mermaid
flowchart TB
    subgraph "PPT 解析"
        A[PPT 文件] --> B[PPTX 解析库]
        B --> C[提取页面]
        C --> D[提取文字]
        D --> E[提取图片]
        E --> F[结构化存储]
    end
```

### 8.2 反馈展示方式

```mermaid
flowchart TB
    subgraph "前端展示"
        A[要点列表] -->|覆盖打勾| B[✅ 已讲]
        A -->|未覆盖| C[❌ 未讲]
        D[禁忌词列表] -->|触发| E[⚠️ 警告]
    end
```

---

## 九、典型使用场景

### 场景一：产品发布会演练

```
管理员配置：
- PPT：产品发布会
- 第 1 页要点：产品名称、发布时间
- 第 2 页要点：核心功能 1/2/3
- 第 3 页要点：价格策略
- 禁忌词：最便宜、保证效果

用户演练：
- 翻到第 1 页，开始演讲
- 系统实时检测是否提到"产品名称"
- 提到"最便宜"时，系统警告
- 翻到第 2 页继续...
```

### 场景二：工作汇报演练

```
管理员配置：
- PPT：季度工作汇报
- 第 1 页要点：业绩完成情况
- 第 2 页要点：存在问题
- 第 3 页要点：下季度计划

用户演练：
- 系统检测要点覆盖
- 检测到模糊表达时给出建议
- 结束后生成评估报告
```

---

## 十、与销售对练的对比

### 10.1 核心差异

| 维度 | PPT 演讲演练 | 销售对练 |
|------|-------------|---------|
| **交互模式** | 单向演讲 + 即时点评 | 双向对话 |
| **AI 角色** | 教练 | 客户 |
| **检测重点** | 内容覆盖度 | 对话技巧 |
| **反馈内容** | 要点+禁忌词 | 话术+应变 |
| **评估方式** | 要点覆盖率 | 多维度评分 |

### 10.2 适用场景

| 场景 | 推荐使用 |
|------|---------|
| 练习演讲技能 | PPT 演练 |
| 练习销售话术 | 销售对练 |
| 练习产品介绍 | 两者都可 |
| 练习客户应对 | 销售对练 |

---

## 十一、总结

PPT 演讲演练场景的核心逻辑：

1. **准备阶段**：管理员上传 PPT 并配置要点和禁忌词
2. **演练阶段**：用户演讲每页 PPT，AI 实时检测
3. **反馈阶段**：实时显示要点覆盖和禁忌词触犯
4. **评估阶段**：生成完整的演讲评估报告

这套机制让演讲培训变得：
- **标准化**：每个人考核标准一致
- **客观**：避免人为评分的主观性
- **即时**：边练边改，效率更高
- **可量化**：用数据说话，了解实际水平
