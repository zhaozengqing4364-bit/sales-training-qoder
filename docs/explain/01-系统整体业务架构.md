# AI 智能演练系统 - 整体业务架构说明

> 编写：Claude Code | 日期：2026-02-18 | 目标读者：产品经理（无需技术背景）

---

## 一、系统定位与核心价值

### 1.1 系统是什么？

想象一下，你的公司需要培训销售人员或让员工练习演讲，传统方式是：
- 找真人扮演客户或评委 → 费时费力
- 观看教学视频 → 被动学习，缺乏互动
- 实际面对客户练习 → 犯错成本高

**这套系统的核心价值**是：**用 AI 模拟真实场景，让员工可以随时随地进行「无风险练习」**。

### 1.2 系统提供两种核心演练场景

```mermaid
graph TB
    subgraph "AI 智能演练系统"
        A[用户] --> B{选择演练场景}
        B --> C[PPT 演讲演练]
        B --> D[销售对练]

        C --> C1[AI 教练实时指导]
        D --> D1[AI 客户模拟对话]

        C1 --> E[演讲评估报告]
        D1 --> F[销售能力报告]
    end

    style A fill:#e1f5fe
    style C fill:#fff3e0
    style D fill:#e8f5e9
```

---

## 二、系统整体架构图

### 2.1 用户视角的业务架构

下面这张图展示了用户使用系统时，背后有哪些「服务」在支持：

```mermaid
flowchart LR
    subgraph "用户端（你打开的网页）"
        WE[Web 浏览器<br/>或企业微信]
    end

    subgraph "云端服务（看不见的后台）"
        API[API 服务器<br/>处理业务请求]
        WS[WebSocket 服务器<br/>实时语音通信]
        AI[AI 大脑<br/>理解你的话并生成回复]
    end

    subgraph "外部能力（云服务）"
        ASR[语音识别<br/>把你的话转成文字]
        TTS[语音合成<br/>把 AI 的话转成语音]
        KB[知识库<br/>存储产品资料]
    end

    WE -->|1. 点击按钮| API
    WE -->|2. 语音对话| WS
    WS -->|3. 识别语音| ASR
    ASR -->|4. 文字内容| WS
    WS -->|5. 理解意图| AI
    AI -->|6. 查资料| KB
    AI -->|7. 生成回复| WS
    WS -->|8. 转语音| TTS
    TTS -->|9. 语音流| WE

    style WE fill:#e3f2fd
    style API fill:#f3e5f5
    style WS fill:#fff3e0
    style AI fill:#e8f5e9
    style ASR fill:#ffebee
    style TTS fill:#ffebee
    style KB fill:#fce4ec
```

### 2.2 核心模块分工

系统后端由多个「模块」组成，每个模块负责不同的事情：

```mermaid
graph TB
    subgraph "AI 智能演练系统后端"
        A[入口层<br/>main.py] --> B[Admin 管理后台]
        A --> C[Agent 智能体]
        A --> D[Presentation Coach<br/>PPT演练]
        A --> E[Sales Bot<br/>销售对练]
        A --> F[Evaluation 评估]
        A --> G[Common 共享基础]

        B -->|配置| C
        C -->|提供能力| D
        C -->|提供能力| E
        D -->|生成报告| F
        E -->|生成报告| F
    end

    style A fill:#e1f5fe
    style B fill:#fff3e0
    style C fill:#e8f5e9
    style D fill:#e0f7fa
    style E fill:#f3e5f5
    style F fill:#ffebee
    style G fill:#f5f5f5
```

---

## 三、核心业务流程总览

### 3.1 演练会话的完整生命周期

当你（用户）开始一次演练时，系统会经历以下阶段：

```mermaid
stateDiagram-v2
    [*] --> 选择场景: 用户选择演练类型

    选择场景 --> 准备会话: 点击开始

    状态: 准备会话
    准备会话 --> 建立连接: WebSocket 连接
    建立连接 --> AI 初始化: 加载配置

    状态: 进行中
    AI 初始化 --> 对话交互: 用户说话
    对话交互 --> AI 回复: AI 生成回复
    AI 回复 --> 对话交互: 用户继续说话
    对话交互 --> 用户中断: 用户打断 AI

    对话交互 --> 结束会话: 用户点击结束
    用户中断 --> 对话交互: 继续对话

    状态: 已结束
    结束会话 --> 生成报告: AI 分析表现
    生成报告 --> 查看报告: 用户查看
    查看报告 --> [*]
```

### 3.2 典型用户操作流程

下面是一个完整的「销售对练」场景，用户做了什么、系统做了什么：

```mermaid
sequenceDiagram
    participant 用户 as 用户（你）
    participant 网页 as 前端页面
    participant 后端 as AI 系统
    participant AI大脑 as AI 大模型
    participant 知识库 as 知识库

    Note over 用户,网页: 步骤 1：选择场景
    用户->>网页: 选择"销售对练"<br/>选择"说服型客户"角色
    网页->>后端: 创建演练会话
    后端-->>网页: 返回会话ID

    Note over 用户,网页: 步骤 2：开始对话
    网页->>后端: 建立 WebSocket 连接
    后端->>后端: 加载"说服型客户"配置

    用户->>网页: 对着麦克风说话
    网页->>后端: 发送语音

    rect rgb(240, 248, 255)
    Note over 后端,AI大脑: AI 处理过程（用户看不见）
    后端->>后端: 语音识别成文字
    后端->>AI大脑: 发送用户说的话
    AI大脑->>知识库: 查询相关产品信息
    知识库-->>AI大脑: 返回产品资料
    AI大脑->>AI大脑: 生成"客户"的回复
    end

    AI大脑-->>后端: 返回回复内容
    后端-->>网页: 推送回复（语音+文字）

    网页->>用户: 播放 AI 语音<br/>显示对话内容

    Note over 用户,网页: 步骤 3：多轮对话（重复上述过程）
    loop 多轮对话
        用户->>网页: 继续说话...
        网页->>后端: 发送语音
        后端-->>网页: AI 回复
        网页->>用户: 播放语音
    end

    Note over 用户,网页: 步骤 4：结束会话
    用户->>网页: 点击"结束演练"
    网页->>后端: 请求结束会话
    后端-->>网页: 返回评估报告
    网页->>用户: 显示评估结果

    Note over 用户,网页: 步骤 5：查看详情
    用户->>网页: 查看详细报告
    网页-->>用户: 展示各项得分和建议
```

---

## 四、两种演练场景对比

| 对比维度 | PPT 演讲演练 | 销售对练 |
|---------|-------------|---------|
| **核心目标** | 练习演讲技能 | 练习销售话术 |
| **AI 角色** | 教练（指导者） | 客户（对话者） |
| **输入内容** | PPT 文件 | 销售场景配置 |
| **AI 反馈** | 实时点评 | 对话式互动 |
| **评估维度** | 要点覆盖、禁忌词 | 话术技巧、应变能力 |
| **典型用户** | 需要演讲培训的员工 | 需要销售培训的员工 |

---

## 五、数据流转示意图

### 5.1 一次完整的语音对话，数据是怎么流动的？

```mermaid
flowchart TB
    subgraph "你的设备"
        MIC[麦克风] --> S1[采集音频]
        S1 --> S2[编码压缩]
    end

    subgraph "网络传输"
        S2 -->|互联网| NET[数据传输]
    end

    subgraph "服务器"
        NET --> R1[接收音频流]
        R1 --> R2[语音识别 ASR]
        R2 --> R3[文字内容]
        R3 --> R4[AI 大脑理解]
        R4 --> R5[生成回复]
        R5 --> R6[文字内容]
        R6 --> R7[语音合成 TTS]
        R7 --> R8[音频数据]
    end

    subgraph "网络传输"
        R8 -->|互联网| NET2[数据传输]
    end

    subgraph "你的设备"
        NET2 --> S3[接收音频流]
        S3 --> S4[解码]
        S4 --> S5[播放声音]
    end

    style MIC fill:#e3f2fd
    style S5 fill:#e3f2fd
    style R2 fill:#fff3e0
    style R4 fill:#e8f5e9
    style R7 fill:#fff3e0
```

---

## 六、关键概念解释

### 6.1 什么是 Agent（智能体）？

**简单理解**：Agent = 「演练场景的配置包」

| 概念 | 生活中类似的例子 | 在系统中的作用 |
|------|----------------|---------------|
| **Agent** | 一份「培训教材」 | 定义演练的目标、规则、评分标准 |
| **Persona** | 扮演的「角色」 | AI 扮演什么类型的客户/评委 |
| **知识库** | 「产品手册」 | AI 回答问题时参考的资料 |

### 6.2 什么是 WebSocket？

**简单理解**：WebSocket = 「保持连接的打电话」

- 传统网页请求像「发短信」：你发一条请求，服务器回复一条，然后就断了
- WebSocket 像「打电话」：一旦连接建立，双方可以随时说话、随时回应

这对语音对话场景至关重要，因为：
1. 用户可以随时打断 AI 说话
2. AI 可以边说边推送给你，不用等全部说完

### 6.3 什么是 ASR 和 TTS？

| 缩写 | 全称 | 功能 | 生活中类似的例子 |
|------|------|------|----------------|
| **ASR** | Automatic Speech Recognition<br/>自动语音识别 | 把你说的话转成文字 | 录音转文字 APP |
| **TTS** | Text-to-Speech<br/>语音合成 | 把文字转成声音 | 导航软件的语音播报 |

---

## 七、系统能力地图

下面这张图展示了系统能够做什么：

```mermaid
mindmap
  root((AI 演练系统))
    PPT 演讲演练
      PPT 上传解析
      要点跟踪
      禁忌词检测
      实时反馈
      演讲评分
    销售对练
      多轮对话
      角色扮演
      知识库检索
      实时评分
      阶段识别
    管理后台
      场景配置
      角色管理
      知识库管理
      数据分析
    评估系统
      分阶段评估
      综合报告
      能力雷达图
```

---

## 八、总结

这套系统的本质是：**用 AI 技术模拟真实场景，让员工可以低成本、高效率地进行技能训练**。

核心逻辑很简洁：
1. **配置** → 管理员配置演练场景（Agent）和角色（Persona）
2. **执行** → 用户选择场景开始演练，AI 实时互动
3. **评估** → 演练结束后生成评估报告

希望这份文档能帮助你理解系统的整体架构。如果还想深入了解某个具体场景的细节，请阅读后续文档。
