# 语音交互全流程解析

> 编写：Claude Code | 日期：2026-02-18 | 目标读者：产品经理（无需技术背景）

---

## 一、语音交互概述

### 1.1 什么是语音交互？

语音交互 = 用户对着麦克风说话，AI 实时回应

这套系统最核心的特点是：**全双工语音交互**，意思是你一句我一句，像打电话一样流畅。

### 1.2 对比其他交互方式

| 交互方式 | 特点 | 适用场景 |
|---------|------|---------|
| **文字聊天** | 打字输入，速度慢 | 客服、简单咨询 |
| **语音消息** | 说一段，发送一段 | 微信语音、录音 |
| **语音通话** | 实时对话，即时回应 | 电话、视频会议 |
| **语音对练** | AI 实时互动，可打断 | 销售演练、演讲练习 |

---

## 二、语音交互全流程

### 2.1 完整流程概览

```mermaid
flowchart TB
    subgraph "语音交互全流程"
        A[用户说话] --> B[麦克风采集]
        B --> C[音频编码]
        C --> D[发送到服务器]
        D --> E[服务器接收]
        E --> F[语音识别 ASR]
        F --> G[得到文字]
        G --> H[AI 理解意图]
        H --> I[AI 生成回复]
        I --> J[文字转语音 TTS]
        J --> K[推送语音]
        K --> L[用户听到]
    end

    style A fill:#e1f5fe
    style L fill:#e8f5e9
```

### 2.2 分步详解

#### 第一步：用户说话

```
用户对着麦克风说话...
       ↓
       ↓
       ↓
声音是空气的振动
       ↓
       ↓
       ↓
麦克风把振动转成电信号
```

#### 第二步：音频采集

```mermaid
flowchart TB
    A[声音波形] --> B[采样]
    B --> C[量化]
    C --> D[编码压缩]
    D --> E[传输格式]
```

#### 第三步：发送到服务器

```
前端 (你的浏览器)
     │
     │ WebSocket 连接
     ↓
服务器 (云端)
```

#### 第四步：语音识别 (ASR)

```mermaid
sequenceDiagram
    participant 音频 as 音频流
    participant ASR as 语音识别服务

    音频->>ASR: 发送音频数据
    ASR->>ASR: 识别声音内容
    ASR-->>音频: 返回文字
```

#### 第五步：AI 处理

```mermaid
flowchart TB
    subgraph "AI 处理流程"
        A[用户说的文字] --> B[检测模糊词]
        A --> C[识别销售阶段]
        A --> D[检索知识库]
        A --> E[实时评分]
    end

    B --> F[生成回复]
    C --> F
    D --> F
    E --> F
```

#### 第六步：语音合成 (TTS)

```mermaid
sequenceDiagram
    participant AI as AI 服务
    participant TTS as 语音合成服务

    AI->>TTS: 发送回复文字
    TTS->>TTS: 合成语音
    TTS-->>AI: 返回音频
```

---

## 三、关键技术点

### 3.1 WebSocket 连接

WebSocket = 保持连接的电话

```mermaid
flowchart LR
    subgraph "HTTP 请求"
        A[浏览器] -->|请求| B[服务器]
        B -->|响应| A
    end

    subgraph "WebSocket"
        C[浏览器] -->|建立连接| D[服务器]
        D <-->|双向通信| C
    end
```

**WebSocket 的优势**：
- 连接建立后保持打开
- 服务器可以主动推送
- 适合实时交互

---

### 3.2 语音识别 (ASR)

ASR = Automatic Speech Recognition，把声音变成文字

```mermaid
mindmap
  root((ASR 能力))
    支持语言
      中文
      英文
    功能
      实时识别
      连续识别
      标点智能添加
    效果
      高准确率
      方言适配
      噪音过滤
```

---

### 3.3 语音合成 (TTS)

TTS = Text-to-Speech，把文字变成声音

```mermaid
mindmap
  root((TTS 能力))
    语音选择
      男声
      女声
    参数调节
      语速
      音量
      音调
    效果
      自然流畅
      多音字正确
      情感表达
```

---

## 四、双工模式

### 4.1 什么是双工？

**双工 = 双方可以同时说话**

想象两个人打电话：
- **单工**：只能一个人说，另一个人听
- **半双工**：可以说，但要先等对方说完
- **全双工**：双方可以同时说

### 4.2 系统支持的能力

```mermaid
flowchart TB
    subgraph "交互能力"
        A[用户说话] --> B[AI 听着]
        B --> C[AI 说话]

        C --> D{用户能否打断？}
        D -->|能| E[打断 AI]
        D -->|等| F[听 AI 说完]

        E --> G[用户开始说]
        F --> H[继续对话]
    end
```

---

## 五、语音降级机制

### 5.1 为什么要降级？

当云端语音服务不可用时，系统需要「降级」到备用方案：

```mermaid
flowchart TB
    subgraph "语音服务降级链"
        A[阿里云 TTS] -->|失败| B[Edge TTS]
        B -->|失败| C[浏览器 TTS]
    end
```

| 优先级 | 服务 | 说明 |
|--------|------|------|
| 1 | 阿里云 TTS | 质量最好 |
| 2 | Edge TTS | 免费，Windows 内置 |
| 3 | 浏览器 TTS | 所有浏览器都有 |

---

## 六、实时反馈机制

### 6.1 边说边反馈

用户说话的同时，系统已经在分析：

```mermaid
sequenceDiagram
    participant 用户 as 用户说话
    participant 系统 as 实时分析

    用户->>系统: 说"我觉得这个产品..."
    Note over 系统: 立即检测
    Note over 系统: 1. 检测模糊词
    Note over 系统: 2. 识别阶段
    Note over 系统: 3. 计算得分

    系统-->>用户: 实时反馈：
    Note over 系统,用户: "我觉得"有点模糊<br/>建议改成具体数据
```

---

## 七、音频传输

### 7.1 音频格式

```mermaid
flowchart TB
    subgraph "音频处理"
        A[原始音频] --> B[压缩]
        B --> C[编码]
        C --> D[传输]
        D --> E[解码]
        E --> F[播放]
    end
```

常用音频编码：
| 编码 | 特点 |
|------|------|
| PCM | 质量最高，文件大 |
| Opus | 压缩率高，适合网络传输 |
| AAC | 平衡质量和大小 |

---

## 八、网络传输优化

### 8.1 延迟优化

语音交互对延迟敏感：

```mermaid
flowchart LR
    subgraph "理想延迟"
        A[说] -->|100ms| B[识别]
        B -->|200ms| C[AI处理]
        C -->|200ms| D[播放]
    end

    subgraph "目标：< 1 秒"
        A --> E[用户感知]
    end
```

### 8.2 抗丢包

网络不稳定时：

```mermaid
flowchart TB
    subgraph "网络不稳定"
        A[发送音频] --> B{网络状况}
        B -->|良好| C[正常传输]
        B -->|抖动| D[缓冲重传]
        B -->|丢包| E[丢包补偿]
    end
```

---

## 九、用户设备适配

### 9.1 支持的设备

```mermaid
mindmap
  root((支持的设备))
    电脑
      Windows
      macOS
      Linux
    手机
      iOS
      Android
    浏览器
      Chrome
      Safari
      Edge
      Firefox
```

---

## 十、典型场景

### 场景一：网络良好的标准流程

```
用户：你好，我想了解一下你们的产品
    ↓
系统：（100ms）识别完成
    ↓
系统：（200ms）AI 处理中
    ↓
系统：（400ms）开始播放回复
    ↓
系统：当然可以，请问您想了解哪方面的产品？
```

### 场景二：网络不好时

```
用户：你好...
    ↓
系统：网络有点慢，正在处理...
    ↓
系统：（稍等）识别到"你好"
    ↓
系统：抱歉让您久等了，请问有什么可以帮您？
```

---

## 十一、技术架构

### 11.1 整体架构

```mermaid
flowchart TB
    subgraph "语音架构"
        前端 -->|WebSocket| 网关
        网关 -->|转发| ASR服务
        网关 -->|转发| AI服务
        AI服务 -->|调用| TTS服务
        TTS服务 -->|推送| 网关
        网关 -->|WebSocket| 前端
    end
```

---

## 十二、总结

语音交互的核心要点：

1. **实时性**：从说话到听到回复 < 1 秒
2. **双工性**：可以随时打断 AI 说话
3. **降级机制**：主服务不可用时自动切换备用
4. **抗丢包**：网络不稳定时仍能保持通话

这套语音交互系统让用户感觉像在和真人打电话，极大地提升了演练的真实感。
