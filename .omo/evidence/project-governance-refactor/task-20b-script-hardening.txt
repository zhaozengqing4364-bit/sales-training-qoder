Task 20b Script Safety Hardening
Date: 2026-06-20

Scope
- Target script: backend/scripts/seed_newcomer_training_path.py
- Target tests: backend/tests/unit/test_seed_newcomer_training_path.py

Change summary
- The newcomer training path seed CLI now defaults to the non-writing verification path.
- Mutating seed behavior requires the explicit --apply flag.
- --verify-only remains supported as an explicit non-writing mode and is mutually exclusive with --apply.
- The underlying seed(db) and verify(db) behavior was not changed.

Red test evidence

Command:
cd backend && venv/bin/python -m pytest tests/unit/test_seed_newcomer_training_path.py -k "cli_" --no-cov -q
Exit before implementation: 1
Relevant failures:
- default CLI called run(verify_only=False)
- --apply was not a recognized argument

Green test evidence

Command:
cd backend && venv/bin/python -m pytest tests/unit/test_seed_newcomer_training_path.py -k "cli_" --no-cov -q
Exit after implementation: 0
Result:
2 passed, 8 deselected, 1 warning

Command:
cd backend && venv/bin/python -m pytest tests/unit/test_seed_newcomer_training_path.py --no-cov -q
Exit: 0
Result:
10 passed, 1 warning

Command:
cd backend && venv/bin/python -m ruff check scripts/seed_newcomer_training_path.py tests/unit/test_seed_newcomer_training_path.py
Exit: 0
Result:
All checks passed!

Non-writing CLI evidence

Command:
cd backend && venv/bin/python scripts/seed_newcomer_training_path.py --help
Exit: 0
Output:
usage: seed_newcomer_training_path.py [-h] [--verify-only | --apply]

Seed or verify newcomer_training_path_v1 baseline modules.

options:
  -h, --help     show this help message and exit
  --verify-only  Only verify baseline records without mutating data. This is
                 the default.
  --apply        Apply seed changes. Omit to run the non-writing verification
                 path.

Command:
cd backend && venv/bin/python scripts/seed_newcomer_training_path.py
Exit: 1
Output:
ppt_explanation completion_rule must be passed

Interpretation
- The default CLI invocation no longer calls the mutating seed path.
- The default command fails visibly on the current data/config precondition, which is expected for verify-only mode on this database state.
- No --apply command was executed in this task.
