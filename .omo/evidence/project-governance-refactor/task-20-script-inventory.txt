Task 20 Script Safety Inventory
Date: 2026-06-20

Scope
- backend/scripts/seed_newcomer_training_path.py
- backend/scripts/seed_presales_cio_first_visit.py
- backend/scripts/import_coo_learning_content.py
- backend/scripts/repair_runtime_snapshots.py

Inventory

1. backend/scripts/seed_newcomer_training_path.py
- Risk: high. Broad baseline seed script with default write mode.
- Non-writing mode: yes, --verify-only.
- Explicit apply flag: no. Running without --verify-only mutates data.
- Scope/limit controls: no per-run scope or limit flag found.
- Affected counts: summary.to_lines() is printed; creation/update granularity depends on summary content.
- Current result: verify-only executes without writes, but fails on current data/config.
- Next todo: make mutating mode explicit with --apply or add default dry-run behavior before using outside controlled local repair; keep verify-only as the release gate.
- Retained reason for now: existing tests cover the verify path, and changing execution semantics would be broader than an inventory task.

2. backend/scripts/seed_presales_cio_first_visit.py
- Risk: high. Legacy direct DB seed path for presales scenario data.
- Non-writing mode: yes, --verify-only behind the explicit --legacy-seed-unsafe gate.
- Explicit apply flag: partial. Default invocation is blocked; legacy write path still mutates when --legacy-seed-unsafe is passed without --verify-only.
- Scope/limit controls: no per-run scope or limit flag found.
- Affected counts: summary object tracks created/updated counts in the seed path.
- Current result: default command refuses deprecated legacy seed and points to Config Asset Center Import API; legacy verify-only runs without writes but fails because the ruleset is not published and active.
- Next todo: retire the legacy direct DB seed path after Config Asset Import fully owns this seed; if kept, require --apply in addition to --legacy-seed-unsafe for mutation.
- Retained reason for now: default execution already blocks unsafe writes, so this can be corrected after release bridge without blocking the current refactor.

3. backend/scripts/import_coo_learning_content.py
- Risk: medium-high. Imports/publishes COO learning content and writes by default.
- Non-writing mode: yes, --verify-only.
- Explicit apply flag: no. Running without --verify-only mutates data.
- Scope/limit controls: content source is fixed to coo.md; no limit flag found.
- Affected counts: prints created, updated, verified, content_id, and chapter_count.
- Current result: verify-only passes with created=0, updated=0, verified=True, chapter_count=15.
- Next todo: add explicit --apply or default dry-run before using as an operational runbook script; keep output counts as mandatory evidence.
- Retained reason for now: current verify-only is stable and reports enough affected-count evidence for release bridge checks.

4. backend/scripts/repair_runtime_snapshots.py
- Risk: high. Repairs historical PracticeSession runtime snapshot drift.
- Non-writing mode: yes. Default mode is dry-run.
- Explicit apply flag: yes, --apply is required for writes.
- Scope/limit controls: yes, --session-id can narrow to specific sessions; --limit bounds scans when no session id is provided; --include-completed is explicit.
- Affected counts: JSON/log summary includes dry_run, scanned_sessions, finding_count, repaired_sessions, and individual findings.
- Current result: dry-run works. Full default dry-run scanned 236 sessions, found 96 findings, repaired 0 sessions. Small dry-run with --limit 3 scanned 3 sessions, found 0 findings, repaired 0 sessions.
- Next todo: no immediate script-safety change required; retain explicit --apply plus scope/limit controls.
- Retained reason for now: this script already satisfies the safety gate expected for broad repair/backfill scripts.

Non-writing command evidence

Command:
cd backend && venv/bin/python scripts/seed_newcomer_training_path.py --verify-only
Exit: 1
Output:
ppt_explanation completion_rule must be passed

Command:
cd backend && venv/bin/python scripts/seed_presales_cio_first_visit.py --verify-only
Exit: 2
Output:
{"deprecated": true, "message": "seed_presales_cio_first_visit.py is deprecated. Use the Config Asset Center Import API with backend/config-assets/presales-cio-first-visit.export.json. Pass --legacy-seed-unsafe only for local emergency repair.", "ok": false}

Command:
cd backend && venv/bin/python scripts/seed_presales_cio_first_visit.py --legacy-seed-unsafe --verify-only
Exit: 1
Output:
{"errors": ["ruleset must be published and active"], "ok": false, "verify_only": true}

Command:
cd backend && venv/bin/python scripts/import_coo_learning_content.py --verify-only
Exit: 0
Output:
created=0
updated=0
verified=True
content_id=b5befe06-e645-46d0-862d-38610fc76502
chapter_count=15

Command:
cd backend && venv/bin/python scripts/repair_runtime_snapshots.py --json
Exit: 0
Output summary:
dry_run=True
scanned_sessions=236
finding_count=96
repaired_sessions=0
notes=Full JSON included 96 findings and was intentionally summarized here to avoid committing session-level identifiers in bulk evidence.

Command:
cd backend && venv/bin/python scripts/repair_runtime_snapshots.py --json --limit 3
Exit: 0
Output:
{
  "dry_run": true,
  "scanned_sessions": 3,
  "finding_count": 0,
  "repaired_sessions": 0,
  "findings": []
}

Acceptance status
- Covered all required scripts.
- High-risk scripts have either next-step remediation todos or explicit retained reasons.
- Non-writing paths were executed; failures are configuration/data preconditions, not write-safety failures.
