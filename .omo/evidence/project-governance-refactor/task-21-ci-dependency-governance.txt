Task 21 CI Branch and Dependency Governance
Date: 2026-06-20

Scope
- .github/workflows/release-truth-gate.yml
- scripts/dependency-governance.sh status output

Change summary
- Release Truth Gate now triggers on push and pull_request for main, dev, and the legacy 001-ai-practice-system branch.
- This keeps existing branch coverage while adding the assumed integration branch dev.
- No dependency scanner was installed in this task; missing scanners are recorded as blockers rather than reported as green.

Workflow trigger evidence

Command:
sed -n '1,12p' .github/workflows/release-truth-gate.yml
Exit: 0
Output:
name: Release Truth Gate

on:
  push:
    branches: [main, dev, 001-ai-practice-system]
  pull_request:
    branches: [main, dev, 001-ai-practice-system]
  workflow_dispatch:

Command:
python3 - <<'PY'
import pathlib
import yaml
workflow = yaml.safe_load(pathlib.Path('.github/workflows/release-truth-gate.yml').read_text())
print(workflow.get('on') or workflow.get(True))
PY
Exit: 0
Output:
{'push': {'branches': ['main', 'dev', '001-ai-practice-system']}, 'pull_request': {'branches': ['main', 'dev', '001-ai-practice-system']}, 'workflow_dispatch': None}

Dependency governance evidence

Command:
bash scripts/dependency-governance.sh status
Exit: 0
Relevant output:
- npm: ready
- web/package.json: present
- web/package-lock.json: present
- backend python: /Users/zhaozengqing/github/销售训练qoder/backend/venv/bin/python
- pip_audit: blocked (install into backend venv before claiming backend vulnerability proof)
- pip-licenses: blocked (install into backend venv before claiming backend license proof)
- npx: ready (web license scan can use license-checker when network/cache is available)
- backend pyproject extras: drift detected (no [project.optional-dependencies] block; CI install -e .[test] is not authoritative)

Command:
bash scripts/dependency-governance.sh license-plan
Exit: 0
Relevant output:
- Frontend license command: npx --yes license-checker --start ./web --production --summary
- Backend license command: backend/venv/bin/python -m piplicenses --from=mixed --format=json
- Policy: do not claim license proof if these commands were not run successfully; record missing prerequisites explicitly instead.

Blocker classification
- pip_audit: explained environment/tooling blocker. Resolution is to install pip-audit into backend/venv before claiming backend vulnerability proof.
- pip-licenses: explained environment/tooling blocker. Resolution is to install pip-licenses into backend/venv before claiming backend license proof.
- backend pyproject extras drift: explained packaging metadata blocker. Resolution is to add a project optional-dependencies block before making pyproject the backend dependency authority.

Acceptance status
- CI trigger strategy now includes the assumed dev integration branch.
- Dependency status still contains blockers, but none are unexplained or reported as a passing baseline.
