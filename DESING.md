# Product Design & Engineering Rules｜Workspace Edition

> **状态：规范性文件（Normative）**  
> **用途：** 放在项目根目录，供产品、设计、前端、后端和 AI Coding Agent 在开发用户可见功能时共同遵守。  
> **目标：** 建立克制、清晰、高密度、可靠、可维护的企业级产品体验。参考 Linear、Vercel、Stripe、Raycast、GitHub、Notion 等成熟 ToB／开发者产品的判断方式，但不复制任何品牌皮肤。

---

## 0. 使用方式与权威关系

### 0.1 这份文件管什么

它约束的是：

- 产品问题定义与任务主线；
- 领域对象、信息架构、导航和页面模型；
- 布局、信息密度、视觉层级和克制风格；
- 组件选择、交互行为、状态、反馈与恢复；
- 表格、表单、筛选、审批、权限、设置等企业级模式；
- 文案、响应式、可访问性、性能与工程韧性；
- AI 功能、Agent、生成式 UI、证据链与人工控制；
- 设计系统治理、AI Coding Agent 工作流与上线验收。

它**不固定**具体品牌色、字体、圆角、图标库、阴影和主题。项目已有视觉系统时必须优先复用；项目没有视觉系统时，应先建立少量、语义化、可访问、可维护的 Foundation，而不是临时在页面中发明样式。

### 0.2 冲突优先级

发生冲突时，按以下顺序处理：

1. 法律、合同、安全、隐私和数据治理要求；
2. 已批准的业务规则、权限、风险策略和领域模型；
3. 当前项目正式 PRD、架构决策、设计决策和 API 契约；
4. 项目现有 Token、组件库、Storybook、正式页面模式；
5. 本文件；
6. 历史截图、外部产品参考和个人偏好。

外部产品只能作为证据和启发，不能覆盖当前项目的真实业务。

### 0.3 Agent 的最低执行合同

任何改变用户看到、理解、选择或执行内容的任务，Agent 必须：

1. 先检查项目现有页面、组件、Token、术语、路由和数据模型；
2. 明确目标用户、核心任务、业务对象、页面模型、主操作和风险；
3. 复用正式组件和模式，不从默认 Dashboard、卡片墙或聊天框开始；
4. 同时实现正常、加载、空、无结果、失败、权限、冲突和恢复状态；
5. 不发明业务规则、指标、权限、数据和 AI 结论；
6. 不因“更现代”而无边界重构或引入新的视觉体系；
7. 完成后检查渲染结果、键盘、焦点、窄屏、长文本和异常数据；
8. 无法遵守规范时明确报告偏差、原因和影响，不得静默绕过。

### 0.4 目标风格

目标不是“极简截图”，而是**克制的企业级工具**：

- 工作对象优先于产品外壳；
- 重要信息优先于装饰；
- 正常状态保持安静，风险和阻塞才提升注意力；
- 信息密度可以高，但关系必须清楚；
- 结构主要依靠层级、对齐、间距和稳定模式表达；
- 高频操作快速、熟悉、可预测；
- 新颖集中在少数真正值得记忆的场景；
- 精致来自细节、反馈、性能与可靠性，而不是渐变、光效和动画表演；
- AI 是业务能力的一部分，不是默认界面形态或视觉主题。

---

## 目录

1. 产品设计原则
2. 信息架构与页面模型
3. 布局、密度与视觉层级
4. 组件与交互规范
5. 企业级产品模式
6. 状态、反馈、错误与韧性
7. 内容设计与微文案
8. 响应式、可访问性与性能
9. AI 产品与 Agent 规范
10. 设计系统工程与治理
11. AI Coding Agent 工作流
12. 项目实施 Playbook
13. 设计评审与 QA 清单
14. 反模式
15. ToB 克制风格指南

---


# 01｜Product Design Principles

本文件定义项目中的高层判断原则。它们不决定某个按钮的颜色，而决定页面是否围绕正确的问题构建。

---

## 1. 先定义问题，再选择界面

不要从 Dashboard、聊天框、卡片列表、左侧导航或某个组件库模板开始。先回答：

1. 谁在什么场景下使用？
2. 用户进入时掌握哪些信息？
3. 用户离开前必须获得什么结果？
4. 当前最重要的业务对象是什么？
5. 用户需要做决定、执行动作，还是理解信息？
6. 错误决策的成本是什么？
7. 哪些约束来自权限、数据、流程和组织？
8. 能否用更少步骤、更少界面完成？

### 交付要求

每个核心页面都必须有一段不超过 100 字的任务定义：

> 当【角色】处于【场景】时，帮助其基于【关键对象/信息】完成【主要任务】，并得到【可验证结果】。

如果无法写清这句话，页面结构通常也不会清楚。

---

## 2. 以业务对象组织产品，而不是以功能菜单组织产品

企业产品应围绕稳定对象构建，例如：

- 客户、联系人、商机、项目、合同；
- 课程、任务、练习、考试、能力；
- 文档、知识条目、版本、权限；
- Workflow、Run、Step、Approval；
- 异常、规则、证据、处理记录。

对象需要具备：

- 唯一身份；
- 状态与生命周期；
- 负责人和权限；
- 关系对象；
- 关键动作；
- 变更历史；
- 可分享或可定位 URL。

功能菜单会不断增加；对象模型更稳定。导航和页面应该让用户理解“我正在处理哪个对象、它处于什么状态、接下来能做什么”。

---

## 3. 不争夺尚未赢得的注意力

视觉权重必须由任务重要程度决定，而不是由组件默认样式决定。

建议的注意力顺序：

```text
当前业务对象或待决策问题
> 风险、异常与阻塞
> 页面主要动作
> 当前工作内容
> 辅助工具与筛选
> 导航、元数据和说明
```

评审时直接问：

> 页面中是否存在一个元素，仅仅因为它有颜色、图标、卡片、动效或“AI”标签，就抢到了它没有资格获得的注意力？

典型问题：

- 侧边栏比工作区更亮；
- 所有卡片都同样突出；
- 普通状态被做成高饱和 Badge；
- AI 入口比核心业务动作更醒目；
- 元数据比决策信息更大；
- 装饰图表占据首屏，但无法支持行动。

---

## 4. 结构应该被感受到，而不是被画满

结构主要来自：

- 语义接近的元素靠近；
- 不同层级有稳定间距；
- 对齐关系明确；
- 页面有一致的阅读和操作顺序；
- 表面层级与信息层级对应；
- 相同对象使用相同表示方式。

边框、阴影、背景和卡片只是辅助。若页面需要大量分割线才能让人看懂，通常是信息架构本身不够清楚。

### 反模式

- 卡片套卡片；
- 每个段落一个容器；
- 每个状态一个彩色框；
- 用阴影代替层级；
- 用竖线和横线弥补无序布局；
- 不同页面对同一对象使用不同结构。

---

## 5. 保持丰富密度，但消除噪声

ToB 产品不是营销海报。用户通常需要同时比较、扫描、判断和执行，因此不能用过度留白换取所谓高级感。

高密度不等于拥挤。密度健康的界面具有：

- 清晰的行列关系；
- 稳定的标题、字段和数值对齐；
- 次要信息明显退后；
- 高频操作容易触达；
- 用户无需频繁打开弹窗才能理解对象；
- 可折叠的细节，而不是永久隐藏关键上下文；
- 紧凑但足够的点击目标和键盘支持。

### 密度判断

- 同屏信息是否支持比较或决策？
- 删除某块内容是否影响任务？
- 用户是否因为页面过空而频繁滚动？
- 用户是否因为页面过密而无法找到下一步？
- 所有信息是否拥有相同视觉重量？

---

## 6. 90% 熟悉，10% 集中创新

成熟产品的大部分交互应该熟悉、可预测。新颖性应集中在真正形成产品辨识度或解释复杂业务的少数位置。

适合创新的地方：

- 关键业务对象的独特表达；
- 复杂流程的可视化；
- 高价值的对比和推演；
- 低频里程碑；
- 具有真实语义的动态反馈；
- 能显著减少步骤的新输入模式。

不适合创新的地方：

- 基础表单；
- 普通表格；
- 保存、取消、筛选、分页；
- 高频导航；
- 权限和错误处理；
- 破坏性操作确认。

新颖程度应与使用频率大体反向相关。

---

## 7. 一页一个主要任务，一域一个主操作

“一页一个主任务”不等于页面只能有一个按钮，而是所有局部操作都应服务于同一任务。

规则：

- 页面顶部应能看出当前任务；
- 每个操作作用域通常只有一个高强调动作；
- 次级动作降低视觉权重；
- 破坏性动作不与主操作并列争抢；
- 按钮文案说明结果，而不是写“确定”“提交”“处理”；
- 若存在多个同等重要目标，应拆分流程、Tab 或角色视图。

---

## 8. 先结论，再证据，再细节

企业用户通常先需要判断，再需要验证，最后才需要完整细节。

推荐结构：

```text
结论 / 当前状态
→ 影响 / 推荐动作
→ 关键证据
→ 完整记录与技术细节
```

但不得隐藏影响决策的反例、限制和不确定性。所谓“先结论”不是只展示漂亮摘要。

---

## 9. 渐进披露，不做永久隐藏

渐进披露适用于：

- 低频高级选项；
- 需要时才查看的技术细节；
- 大量审计和历史记录；
- 可安全默认的配置；
- 专家用户功能。

不应被隐藏的内容：

- 当前状态；
- 高风险影响；
- 权限范围；
- 关键约束；
- 已应用的筛选；
- 自动保存状态；
- 会影响结果的 AI 上下文；
- 破坏性动作的对象和后果。

---

## 10. 可逆优先于确认泛滥

对于低风险、易恢复的操作，优先：

- 立即执行；
- 明确反馈；
- 提供 Undo；
- 保留历史。

对于高风险、不可逆或对外动作，使用：

- 明确对象；
- 前后差异；
- 影响范围；
- 必要审批；
- 二次确认；
- 审计与补偿方案。

不要让每一个普通动作都弹确认框。确认泛滥会训练用户机械点击。

---

## 11. 设计必须覆盖真实状态

完整设计至少覆盖：

- 初始；
- 正常；
- 加载；
- 空；
- 无结果；
- 失败；
- 部分成功；
- 无权限；
- 数据过期；
- 并发冲突；
- 长任务；
- 取消；
- 恢复；
- 离线或网络不稳定；
- 极端数据量；
- 长文本和异常字符。

只设计“有 6 条漂亮数据的正常态”，不算生产级设计。

---

## 12. 让决策可以被复用和质疑

重要设计结论应记录：

- 决策是什么；
- 为什么这样做；
- 适用范围；
- 证据；
- 例外；
- 好例和坏例；
- 未解决问题；
- Owner 和复审日期。

不要把一次截图、一个外部案例或一位评审者的意见直接升级成普遍规则。规则要有证据，也允许被新证据推翻。


---


# 02｜Information Architecture & Page Models

信息架构的目标不是把功能放进菜单，而是建立用户可以预测的对象、路径和操作关系。

---

## 1. 从领域模型开始

在设计导航前，先列出：

| 维度 | 需要回答 |
|---|---|
| 核心对象 | 用户真正管理什么？ |
| 对象关系 | 一对一、一对多、层级、依赖、引用？ |
| 生命周期 | 创建、草稿、审核、发布、归档、失效？ |
| 角色 | 谁查看、编辑、批准、管理？ |
| 动作 | 高频动作、关键动作、高风险动作？ |
| 状态 | 状态由谁改变，能否回退？ |
| 证据 | 结果依据、来源和历史在哪里？ |
| URL | 对象和视图能否被定位、分享和恢复？ |

没有领域模型时，产品会出现“功能很多但不知道数据属于谁”的问题。

---

## 2. 导航原则

### 2.1 导航表达稳定区域

主导航适合放：

- 稳定业务域；
- 高频访问的核心对象；
- 长期存在的工作空间。

不适合放：

- 临时活动；
- 某个对象的局部动作；
- 所有后台配置；
- 每一个新功能；
- 重复的角色入口。

### 2.2 控制层级

- 尽量不超过 3 层；
- 同一层级使用同一分类标准；
- 不要一半按对象、一半按部门、一半按流程；
- 子导航项过多时先重新分组，不要无限折叠；
- 设置、帮助、组织和个人信息与业务域分开；
- 当前对象位置必须可识别。

### 2.3 URL 是界面状态的一部分

建议将以下状态写入 URL：

- Tab；
- 筛选；
- 搜索词；
- 分页；
- 排序；
- 展开的详情对象；
- 时间范围；
- 对比对象；
- 可分享的视图配置。

这样刷新、返回、前进、分享和多标签页才能符合用户预期。

---

## 3. 页面模型选择

不要默认生成 Dashboard。先从以下模式中选择。

### 3.1 List–Detail｜列表—详情

**适用：** CRM、知识条目、课程、异常、用户、Agent、Workflow。

**结构：**

- 列表承担扫描、比较、筛选和批量操作；
- 详情承担理解、编辑、关系、历史和局部动作；
- 当前选择需要在 URL 或可恢复状态中保留。

**避免：**

- 列表每行展示全部信息；
- 详情只重复列表字段；
- 每次查看详情都跳到完全不同的页面且丢失列表位置；
- 操作列堆满按钮。

### 3.2 Dashboard–Drilldown｜概览—下钻

**适用：** 主管看板、经营分析、训练运营、异常总览。

**Dashboard 必须回答：**

1. 当前是否正常？
2. 哪些变化值得关注？
3. 原因是什么？
4. 应该采取什么行动？
5. 如何下钻到可操作对象？

**避免：** 指标卡、趋势图、排行榜只是为了显得“完整”，却没有行动路径。

### 3.3 Canvas–Inspector｜画布—检查器

**适用：** Workflow、知识图谱、可视化编排、页面构建器。

**结构：**

- 画布展示关系和空间；
- Inspector 编辑当前选中对象；
- 工具栏只承载全局或高频动作；
- 选择、缩放、拖拽、撤销和键盘是核心能力。

**避免：** Inspector 变成第二个设置系统，画布只剩装饰。

### 3.4 Editor–Preview｜编辑—预览

**适用：** 课程、文档、Prompt、规则、消息模板、站点内容。

**关键：**

- 编辑和预览的状态同步；
- 自动保存、草稿和发布明确分离；
- 版本差异可查看；
- 预览使用真实数据和真实边界。

### 3.5 Process–Approval｜流程—审批

**适用：** 合同、发布、权限、数据变更、AI 高风险动作。

**必须展示：**

- 当前阶段；
- 当前责任人；
- 等待原因；
- 前后差异；
- 依据与附件；
- 审批动作和后果；
- 历史记录。

### 3.6 Search–Result｜搜索—结果

**适用：** 企业知识库、全局搜索、客户资料检索。

**重点：**

- 查询范围可见；
- 筛选条件明确；
- 结果摘要支持判断；
- 来源、权限和新鲜度可查看；
- 无结果时提供修正路径；
- 搜索和生成答案不要混为一谈。

### 3.7 Settings–Configuration｜设置—配置

**适用：** 系统设置、组织、权限、模型、集成、规则。

**重点：**

- 按影响对象和风险分组；
- 明确默认值；
- 说明变更生效范围与时间；
- 可测试、可回滚；
- 高级配置渐进披露；
- 不要把所有配置塞进一个超长表单。

### 3.8 Command–Execution｜命令—执行

**适用：** 快捷操作、批量命令、开发者工具、运维动作。

**重点：**

- 命令名称精确；
- 作用范围可见；
- 参数可检查；
- 执行进度和结果可追踪；
- 高风险命令需要确认或审批；
- 历史可复用。

### 3.9 Conversation–Workspace｜对话—工作区

**适用：** 目标开放、需要连续澄清、输入难结构化的任务。

**原则：**

- 对话用于澄清和控制；
- 结构化工作区承载正式结果；
- 结果可以保存、编辑、比较、引用和落库；
- 不把所有业务对象长期困在消息气泡中。

---

## 4. 页面规格必须包含的结构

每个核心页面至少定义：

1. 页面任务；
2. 核心对象；
3. 用户角色；
4. 页面入口；
5. 页面模式；
6. 信息优先级；
7. 主操作和次级操作；
8. 筛选、排序、搜索与视图；
9. 状态；
10. 权限；
11. 响应式策略；
12. 数据量和性能；
13. 埋点；
14. 验收场景。

将上述内容写入当前任务说明、功能规格或项目文档；不得只存在于临时对话中。

---

## 5. 跨页面一致性

同一对象在不同页面应保持：

- 名称一致；
- 身份表达一致；
- 状态词一致；
- 核心动作名称一致；
- 时间和金额格式一致；
- 详情打开方式可预测；
- 返回和关闭行为可预测；
- 权限不足表达一致；
- AI 输出进入同一正式对象模型。

一致性不是每页长得一样，而是同一概念在任何地方都不要求用户重新学习。


---


# 03｜Layout, Density & Visual Hierarchy

本文件不指定具体色彩、字体和圆角，而定义视觉组织的判断标准。

---

## 1. 工作对象优先于产品外壳

企业产品通常包含：

- 全局导航；
- 页面导航；
- 工具栏；
- 内容区；
- Inspector；
- 状态和通知。

视觉权重应该从工作对象向外递减。用户到达页面后，导航应退后，不能持续与内容竞争。

### 审核

- 首屏最先看到的是业务对象，还是品牌和导航？
- 侧栏是否比当前内容更显眼？
- 工具栏是否占用过高空间？
- 辅助面板是否压缩了主任务区域？
- AI 区域是否无理由占据主要宽度？

---

## 2. 以内容目标选择布局

布局不是固定 12 栏网格的套用。先判断任务：

- 长文阅读：限制阅读宽度，提供章节和定位；
- 表格比较：优先横向空间、固定关键列和可配置列；
- 多步骤任务：突出当前阶段和完成条件；
- 对象编辑：字段分组、上下文和预览；
- 关系编排：画布、选择和 Inspector；
- 监控与运营：异常优先、趋势下钻、行动入口。

每个元素都应与网格、基线、边缘或光学中心形成有意对齐。不要出现“差不多居中”“随手留白”的位置。

---

## 3. 密度分级

项目可以定义自己的 Token，但页面需要明确主密度。

### Compact

适用于：

- 高频运营；
- 大表格；
- 审核队列；
- 开发者和管理工具；
- 数据对比。

特征：更多同屏信息、短行高、紧凑工具栏，但点击目标和焦点仍必须合格。

### Standard

适用于：

- 大多数企业工作页面；
- CRM 详情；
- 课程管理；
- 知识库；
- 设置与配置。

### Comfortable

适用于：

- 新人引导；
- 低频高风险配置；
- 长内容学习；
- 移动场景；
- 需要解释的任务。

### 规则

- 同一页面不要无理由混合多种密度；
- 密度切换应该影响一组 Token，而不是局部手工缩放；
- 高密度不能牺牲键盘、点击目标和可读性；
- 低密度不能通过巨大卡片和空白浪费工作空间。

---

## 4. 关系决定间距

间距不是装饰，而是表达语义：

- 组内元素间距较小；
- 组间间距较大；
- 标题与其内容的距离小于与上一组的距离；
- 操作应靠近其作用对象；
- 错误信息紧邻错误字段；
- 说明文字紧邻被说明对象；
- 全局操作与局部操作必须有空间区分。

项目可以选择具体 spacing scale，但禁止页面内随机出现大量无 Token 数值。

---

## 5. 卡片不是默认容器

适合使用卡片：

- 可独立操作或移动的信息单元；
- 需要明确表面层级的聚合对象；
- 概览中相对独立的模块；
- 可比较的同类对象；
- Dashboard 中需要稳定占位的摘要。

不适合使用卡片：

- 连续表单；
- 同一对象的每个字段组；
- 长文每一个章节；
- 表格每一行；
- 只是为了填满页面的指标；
- 容器内部再次无意义套容器。

卡片数量增加时，应优先考虑列表、表格、分区或更明确的页面模式。

---

## 6. 边框、阴影和表面

原则：

- 先使用间距、对齐和背景层级；
- 边框用于真实边界、输入、选择或分隔；
- 阴影表达浮层，而不是给所有容器增加“高级感”；
- 嵌套表面应有清晰层级，避免视觉泥化；
- 子元素圆角与父容器保持几何协调；
- Hover、Focus、Active 应比静止状态更清楚。

一个元素同时拥有高对比背景、粗边框、明显阴影和发光，通常表示视觉职责没有被理清。

---

## 7. Inspector 与辅助面板

Inspector 适合：

- 当前选中对象的属性；
- 局部配置；
- AI 建议和上下文；
- 证据、历史或调试信息。

规则：

- 默认不压过主工作区；
- 宽度与内容类型匹配；
- 可关闭或调整；
- 选中对象变化时保持稳定结构；
- 不重复主内容；
- 高风险操作仍在明确流程中完成，不藏在侧栏底部；
- 移动端转成独立页面、抽屉或底部面板，不简单挤压。

---

## 8. 表格优先于卡片墙的场景

当用户需要：

- 比较多个对象；
- 扫描一致字段；
- 排序；
- 批量选择；
- 查找异常；
- 查看大量数据；

应优先考虑表格或结构化列表，而不是卡片网格。

卡片更适合差异较大的对象、视觉内容、低数量入口或需要较强独立性的单元。

---

## 9. 数据可视化布局

图表必须服务于一个问题：

- 发生了什么？
- 与什么相比？
- 为什么变化？
- 哪些对象造成变化？
- 下一步做什么？

图表旁应提供：

- 指标定义；
- 时间范围；
- 对比基准；
- 异常解释；
- 下钻入口；
- 数据新鲜度。

没有行动含义的装饰性图表应删除。

---

## 10. 视觉层级自检

把页面转成灰度或模糊查看：

- 是否仍能看出主次？
- 是否有多个区域同时大声说话？
- 是否只有颜色在表达层级？
- 是否能找到当前对象、状态和主操作？
- 是否有明显但无业务意义的大块区域？
- 去掉边框后，结构是否仍然成立？


---


# 04｜Component & Interaction Standards

组件规范的核心不是静态外观，而是行为、状态、语义和工程契约。

---

## 1. 组件文档最低结构

每个正式组件至少说明：

1. Purpose；
2. When to use；
3. When not to use；
4. Anatomy；
5. Variants；
6. States；
7. Content rules；
8. Interaction；
9. Keyboard；
10. Focus management；
11. Accessibility；
12. Responsive behavior；
13. Performance；
14. API / Props；
15. Analytics；
16. Good / Bad examples；
17. Known limitations；
18. Owner。

只提供 Figma 组件和 Props 表，不足以支撑长期一致性和 AI 生成。

---

## 2. 交互基础规则

- 所有核心流程可通过键盘完成；
- 可聚焦元素有清晰 `focus-visible`；
- 打开 Modal、Drawer、Menu 后正确移动和返回焦点；
- 链接使用真实链接语义，保留新标签页和复制地址能力；
- 不阻止粘贴；
- 点击区域与视觉区域匹配；
- 小图标扩大 Hit Target，但不能形成重叠死区；
- Back / Forward 恢复页面状态和滚动位置；
- 看起来可点击的区域就必须可点击；
- 异步更新通过合适的状态和辅助技术通知。

---

## 3. Button 与 Link

### Button

用于在当前上下文中执行动作。

文案优先使用：

- 保存规则；
- 创建课程；
- 发布版本；
- 批准变更；
- 重新同步；
- 生成拜访计划。

避免：

- 确定；
- 提交；
- 继续；
- 处理；
- 点击这里。

### Link

用于导航到另一个位置或对象。不能为了样式方便用 Button 模拟 Link。

### 主次关系

- 每个作用域通常一个 Primary；
- Secondary 支持替代路径；
- Ghost 适合工具栏和低强调操作；
- Destructive 明确风险，不与主操作同色同权；
- Loading 保留原标签，避免按钮宽度跳动；
- 禁用状态应说明原因，能用正常交互处理时不要滥用 Disabled。

---

## 4. Menu、Dropdown、Combobox、Radio

选择组件要根据选项数量和用户任务选择：

- 少量静态互斥选项：优先 Radio，让用户直接看到全部选项；
- 中等数量：Select / Dropdown；
- 大量或动态选项：Combobox + 搜索；
- 多选：Checkbox、Multi-select 或可搜索选择；
- 高频切换视图：Segmented Control / Tabs；
- 布尔即时设置：Switch；
- 需要提交后才生效的布尔值：Checkbox 可能更清楚。

不要用 Dropdown 隐藏 2 个本可直接看到的选项，也不要用 Radio 展示几十个动态值。

---

## 5. Dialog、Drawer、Popover

### Dialog

适合：必须中断当前任务、需要明确决定的短流程。

不适合：复杂长表单、完整详情页、多层导航、需要并行参考主页面的任务。

### Drawer

适合：保持主页面上下文，同时查看详情或完成中等复杂任务。

### Popover

适合：轻量、临时、与触发对象强相关的内容。

### 规则

- 禁止 Modal 中再打开 Modal；
- 关闭必须可预测；
- 未保存内容要保护；
- Esc 行为明确；
- 焦点进入与返回正确；
- 移动端需要独立布局策略；
- 破坏性确认必须写出对象和后果；
- 长内容应转成页面或 Drawer，不靠滚动小弹窗解决。

---

## 6. Tabs

Tabs 适合在同一对象和同一层级下切换视图。

不适合：

- 顺序流程；
- 完全不同业务域；
- 需要同时比较的内容；
- 隐藏必须看到的错误；
- 大量动态对象。

要求：

- URL 可定位；
- 当前 Tab 明确；
- 数量稳定；
- 标签简短且同一分类标准；
- 不把二级导航、筛选和模式切换全部做成 Tabs。

---

## 7. Tooltip 与帮助

Tooltip 用于补充短信息，不承载完成任务所需的唯一内容。

- 首次 Tooltip 有适度延迟；
- 同组后续 Tooltip 可快速出现；
- 支持键盘和屏幕阅读器；
- 纯图标按钮必须有可访问名称；
- 复杂帮助使用 Toggletip、Inline Help、文档或示例；
- 不使用 Tooltip 隐藏字段标签和错误原因。

---

## 8. 拖拽

拖拽适合空间排序、编排和直接操控，但必须提供：

- 清晰把手或可拖区域；
- 拖动中的占位和目标反馈；
- 自动滚动；
- 取消；
- 键盘替代；
- 屏幕阅读器说明；
- 冲突处理；
- 保存或自动保存反馈。

拖拽过程中应避免文本选择、无关 Hover 和误触。

---

## 9. 乐观更新与 Undo

当成功概率高、失败可恢复时：

1. 立即更新界面；
2. 后台提交；
3. 成功后确认；
4. 失败时回滚并说明；
5. 提供 Undo 或重试。

不适合乐观更新：

- 金额结算；
- 权限变更；
- 对外发送；
- 不可逆删除；
- 复杂审批；
- 结果高度不确定的 AI 执行。

---

## 10. 动效

动效只用于：

- 因果反馈；
- 空间连续性；
- 状态变化；
- 少量重要时刻。

要求：

- 可中断；
- 用户输入可以反向或取消；
- 优先使用浏览器和合成线程友好属性；
- 不使用 `transition: all`；
- 不因动效推迟核心操作；
- 支持 Reduced Motion；
- 高频操作使用克制的 Productive Motion；
- 表达性动效只用于低频、重要时刻。

---

## 11. 图标

本通用规范不指定图标库，但要求：

- 一个项目使用统一家族和视觉重量；
- 图标表达对象或动作，而不是装饰；
- 图标不能替代不熟悉的业务词；
- 状态不要只依赖图标；
- 同一操作在全产品使用同一图标；
- 不混用实心、线性、双色和 Emoji；
- 图标与文字在视觉重量上平衡；
- AI 不统一使用 Sparkles、魔法棒或机器人。

---

## 12. 组件扩展原则

新增组件前先判断：

1. 是新语义，还是旧组件的样式变体？
2. 是否会在至少两个稳定场景复用？
3. 是否能通过组合现有组件完成？
4. 是否需要新的交互模式？
5. API 是否可预测？
6. 状态是否完整？
7. 是否会造成设计系统分叉？

不要为了一个页面建立“专属组件库”，也不要为了复用而抽象一个只有名字不同的巨大万能组件。


---


# 05｜Enterprise Product Patterns

企业级产品的质量主要体现在表单、表格、筛选、权限、审批、批量处理和数据状态，而不是首页 Hero。

---

## 1. 表单

### 1.1 结构

- 标签通常放在控件上方；
- Placeholder 不替代 Label；
- 相关字段按任务和对象分组；
- 字段顺序符合用户思考顺序；
- 帮助文字紧邻字段；
- 错误信息紧邻错误源；
- 必填与选填规则一致；
- 默认值明确且可解释；
- 高级配置渐进披露；
- 长表单提供分段、目录或步骤。

### 1.2 验证

- 能在输入过程中可靠判断的错误及时提示；
- 不要用户每输入一个字符就显示红色错误；
- 提交后聚焦首个错误，并提供错误摘要；
- 后端错误保留用户输入；
- 格式化与用户输入法兼容；
- 允许粘贴；
- 日期、金额、电话号码按 Locale 处理；
- 自动修剪常见尾部空格，避免无意义失败。

### 1.3 保存模型

明确采用：

- 显式保存；
- 自动保存；
- 草稿 + 发布；
- 分步提交；
- 即时设置。

自动保存必须显示：正在保存、已保存、失败、离线和冲突状态。

---

## 2. 数据表格

### 2.1 表格职责

表格用于组织、比较和操作结构化数据。它不是普通布局工具。

### 2.2 列设计

- 文本通常左对齐；
- 数值右对齐；
- 数字使用等宽数字；
- 状态、短标签的对齐策略全局一致；
- 第一列通常承担对象身份；
- 关键列可固定；
- 低价值列可隐藏或配置；
- 表头说明单位、时间范围或口径；
- 长文本应截断并可查看完整内容。

### 2.3 行操作

- 高频核心动作可直接显示；
- 低频动作进入更多菜单；
- 行点击与内部按钮不能冲突；
- 批量选择后显示作用范围和批量动作；
- 批量动作执行前说明数量和影响；
- 高风险批量动作提供预览、确认和审计。

### 2.4 大数据

- 服务端分页、排序和筛选；
- 大列表虚拟化；
- 保留滚动和选择状态；
- 显示总数、已加载范围和筛选范围；
- 导出是异步任务时提供任务状态和通知；
- 不一次渲染数千行再依赖浏览器硬扛。

---

## 3. 筛选、搜索和视图

### 3.1 筛选

- 常用筛选直接可见；
- 高级筛选可展开；
- 已应用筛选有清晰摘要；
- 可一键清除；
- 数量和结果及时更新；
- 筛选状态写入 URL；
- 筛选条件使用用户业务语言；
- 不让用户猜“当前为何没有数据”。

### 3.2 保存视图

适用于高频角色：

- 保存筛选、排序、列和分组；
- 区分个人视图与团队视图；
- 明确 Owner 和共享范围；
- 视图变更可恢复；
- 默认视图由角色和任务决定，不是所有人同一个 Dashboard。

### 3.3 搜索

- 搜索范围可见；
- 提供最近搜索或建议时要有价值；
- 无结果时建议修改条件；
- 支持拼写、别名和业务术语；
- 搜索结果展示匹配原因；
- 搜索与 AI 生成回答分开标识。

---

## 4. Dashboard

Dashboard 不是指标卡集合。每个模块必须至少服务于：

- 状态判断；
- 异常发现；
- 趋势理解；
- 对比；
- 行动。

### 设计顺序

1. 明确角色和决策；
2. 定义指标口径；
3. 识别异常和阈值；
4. 设计下钻；
5. 设计行动；
6. 最后选择图表。

### 必须包含

- 时间范围；
- 数据更新时间；
- 指标定义；
- 对比基准；
- 筛选范围；
- 下钻对象；
- 空数据与异常数据状态。

---

## 5. 审批与评审

审批页面的核心不是“同意/拒绝”两个按钮，而是让审批人快速建立判断。

推荐结构：

1. 请求摘要；
2. 影响范围；
3. 修改前后差异；
4. 申请原因；
5. 证据与附件；
6. 风险；
7. 历史和相关审批；
8. 批准、退回、拒绝、转交；
9. 审批意见；
10. 审计记录。

避免：审批人需要在多个系统自行拼接上下文。

---

## 6. 权限

权限体验应回答：

- 用户看不到什么？
- 用户为什么看不到？
- 可以向谁申请？
- 权限何时生效？
- 作用范围是什么？
- 当前是否处于代管、临时或继承权限？

规则：

- 服务端始终做最终校验；
- 前端隐藏不是安全控制；
- 无权限与无数据区分；
- 只读状态明确；
- 权限变更有影响预览和审计；
- 角色与具体权限不要混为一谈；
- 跨组织和跨租户边界必须显式。

---

## 7. 导入、导出和批处理

### 导入

- 提供模板和示例；
- 上传前说明格式、字段和限制；
- 预览映射；
- 校验错误定位到行和字段；
- 支持下载错误报告；
- 明确覆盖、追加、去重策略；
- 大任务异步运行；
- 部分成功必须准确报告。

### 导出

- 明确导出范围；
- 说明字段、格式和权限；
- 大任务进入任务中心；
- 完成后通知；
- 文件有有效期和访问控制；
- 审计谁在何时导出了什么。

---

## 8. 设置与管理后台

- 按业务影响分组，而不是按数据库表分组；
- 说明默认值和推荐值；
- 有搜索时仍保持清楚分类；
- 变更前说明影响范围；
- 支持测试连接、验证配置和预览结果；
- 保存后说明生效时间；
- 有版本、回滚和审计；
- 危险区域与普通设置明确分离；
- 不把所有配置平铺成超长页面。

---

## 9. 新人引导与空状态

新人引导的目标是让用户完成第一个真实任务，而不是播放产品介绍。

优先：

- 示例数据；
- 可撤销的引导任务；
- 在工作现场出现的提示；
- 角色化默认配置；
- 一次只解释当前步骤；
- 完成后的真实成果。

空状态需要说明：

- 为什么为空；
- 这是正常、异常还是无权限；
- 下一步可以做什么；
- 是否有示例、导入或创建入口。

---

## 10. 审计和历史

企业产品的“历史”不是简单日志文本。应记录：

- 谁；
- 何时；
- 对什么对象；
- 做了什么；
- 前后差异；
- 来源；
- 是否由 Agent 或自动化执行；
- 审批和运行引用；
- 失败、撤销和补偿。

历史记录应可筛选、定位和理解，不要只显示不可读的 JSON。


---


# 06｜States, Feedback, Errors & Resilience

可靠性本身就是设计。用户对企业产品的信任，往往来自失败时是否仍然清楚、可控和可恢复。

---

## 1. 状态优先设计

核心异步流程建议至少考虑：

```text
Idle
→ Preparing
→ Loading / Executing
→ Success
→ Partial Success
→ Failed
→ Cancelled
→ Retrying
→ Stale / Conflict
```

长任务还可能包含：

```text
Queued
→ Running
→ Waiting for Input
→ Waiting for Approval
→ Verifying
→ Completed
```

状态必须来自后端真实状态机或可靠契约，不能仅靠前端猜测。

---

## 2. Loading

### 选择方式

- 预计很快完成：延迟显示 Loading，避免闪烁；
- 内容结构已知：Skeleton；
- 单一局部动作：按钮内 Loading；
- 长任务：阶段、进度、已完成步骤和后台运行；
- 不确定时长：说明正在做什么，而不是假进度条。

### 规则

- 保留用户输入；
- 保留布局，避免跳动；
- 保留按钮原文；
- 不在整个页面加遮罩阻塞无关操作；
- 可取消的任务提供取消；
- 超过合理等待时间提供解释和恢复路径。

---

## 3. Empty、No Result、Zero State

三者不同：

- **Empty**：尚未创建数据；
- **No Result**：有数据，但当前筛选或搜索无匹配；
- **Zero State**：用户尚未完成首次配置或启动。

每种状态应给出对应下一步，不能统一写“暂无数据”。

---

## 4. Error

错误信息至少回答：

1. 哪里出错？
2. 可能原因？
3. 用户输入是否保留？
4. 是否已产生部分结果？
5. 下一步怎么做？
6. 是否需要联系谁？
7. 是否有错误引用或日志？

避免只写：

- 请求失败；
- 系统异常；
- 未知错误；
- Error 500。

技术细节可以展开，但首层使用用户语言。

---

## 5. Partial Success

部分成功不能显示成绿色“完成”。必须说明：

- 成功数量；
- 失败数量；
- 跳过数量；
- 每项原因；
- 已经写入的结果；
- 可否重试失败项；
- 重试是否会重复成功项；
- 是否需要回滚。

批量导入、Agent 多步骤运行、同步和发布尤其需要该模式。

---

## 6. 权限状态

区分：

- 看不到对象；
- 能看到但不能编辑；
- 能编辑部分字段；
- 需要审批；
- 权限已过期；
- 数据正在脱敏；
- 跨组织禁止访问。

无权限页面应提供合理解释和申请路径，但不能泄露用户本不应知道的敏感对象信息。

---

## 7. Stale Data 与并发冲突

当对象被其他人修改：

- 明确指出数据已变化；
- 保留用户当前编辑；
- 展示差异；
- 提供刷新、覆盖、合并或另存；
- 高风险对象禁止静默覆盖；
- 审计冲突解决结果。

实时协作页面还需要：

- 在线人员；
- 当前选中或编辑区域；
- 锁和非锁策略；
- 合并规则；
- 离线恢复。

---

## 8. 自动保存

自动保存状态至少包含：

- 已保存；
- 正在保存；
- 保存失败；
- 离线；
- 存在冲突；
- 草稿；
- 已发布。

“保存成功 Toast”不适合频繁自动保存。使用稳定、低噪声的局部状态。

---

## 9. Destructive Action

风险等级决定防护：

| 风险 | 示例 | 推荐保护 |
|---|---|---|
| 低 | 移除筛选、隐藏列 | 立即执行或轻量 Undo |
| 中 | 删除草稿、归档对象 | 确认或 Undo |
| 高 | 删除正式数据、覆盖版本 | 对象 + 影响 + 明确确认 |
| 极高 | 权限、金额、对外发送、批量删除 | Diff + 审批 + 审计 + 补偿 |

确认文案必须使用对象名称和动作后果，不能只写“是否确认”。

---

## 10. 长任务与后台运行

当任务超过一个普通交互周期：

- 创建独立 Run；
- 显示阶段和当前步骤；
- 允许离开页面；
- 完成后通知；
- 支持取消、暂停或停止后续步骤；
- 展示输入、输出、日志和失败原因；
- 可重试时说明重试范围；
- 保留历史和审计。

不要让用户盯着一个无法解释的 Spinner。

---

## 11. Resilience 评审问题

- 网络断开后用户会丢失什么？
- 刷新页面后状态能否恢复？
- 重复点击是否产生重复记录？
- 请求超时后后端是否可能已成功？
- 重试是否幂等？
- 多人同时编辑会怎样？
- API 返回新字段或空字段会怎样？
- 大数据和长文本会怎样？
- 用户离开后任务是否继续？
- 错误是否能被支持人员定位？


---


# 07｜Content Design & Microcopy

界面文案是产品模型的一部分，不是最后补上的装饰。

---

## 1. 用词原则

- 使用用户和业务中的稳定术语；
- 同一个对象只使用一个正式名称；
- 少创造同义词；
- 使用主动语态；
- 优先动词 + 对象；
- 尽量短，但不能牺牲准确性；
- 文案说明结果和后果；
- 技术术语只在目标用户理解时使用；
- 不使用空泛营销词代替功能说明。

---

## 2. 标题

页面标题回答“这里处理什么”。

好的标题：

- 客户详情；
- 课程发布；
- 异常处理队列；
- 知识库权限；
- Workflow 运行记录。

较差标题：

- 工作台；
- 智慧中心；
- 赋能空间；
- 数字驾驶舱；
- AI 助手。

抽象名称可以作为品牌或模块名称，但必须有清楚的任务副标题。

---

## 3. 按钮

按钮文案说明点击后发生什么：

- 保存配置；
- 发布课程；
- 重新运行；
- 申请访问；
- 导出当前结果；
- 批准并发布。

避免：确定、继续、下一步、处理、提交，除非上下文确实不会产生歧义。

带省略号的动作通常表示还需要进一步输入或进入处理过程，例如“重命名…”“生成中…”。

---

## 4. 错误信息

结构：

```text
发生了什么
+ 可能原因
+ 用户如何恢复
+ 必要时提供错误引用
```

示例：

> 无法保存规则。该规则已由其他人更新。查看差异后选择合并或覆盖。

优于：

> 保存失败。

---

## 5. 空状态

空状态不写“暂无数据”就结束。需要根据原因设计：

- 尚未创建：解释价值并提供创建入口；
- 筛选无结果：展示当前条件并允许清除；
- 无权限：说明限制和申请路径；
- 同步中：说明预计流程和刷新方式；
- 异常为零：表达“当前无异常”，不要像系统坏了。

---

## 6. 确认与警告

警告应具体：

- 将删除什么；
- 影响谁；
- 是否可恢复；
- 何时生效；
- 是否保留历史；
- 下一步能否撤销。

避免恐吓式、含糊式文案，也不要把普通信息都写成 Warning。

---

## 7. 状态词

状态名称应：

- 表达对象当前事实；
- 与生命周期对应；
- 互斥或关系清楚；
- 不混合动作和状态；
- 不使用“正常”“异常”之外无法解释的模糊词。

例如课程：草稿、待审核、已发布、已停用、已归档。  
不要同时出现“已完成”“完成中”“完成待发布”等未经定义的词。

---

## 8. 数据、时间与单位

- 同一上下文统一小数位；
- 数字和单位格式一致；
- 时间明确时区和相对/绝对规则；
- 百分比说明分母；
- 指标说明口径；
- 空值、零值和未采集区分；
- 金额说明币种；
- 日期范围说明包含边界；
- 长 ID 可以缩写，但支持复制完整值。

---

## 9. AI 文案

AI 文案描述结果，不神化能力。

优先：

- 提取行动项；
- 检查资料缺口；
- 生成练习反馈；
- 比较方案差异；
- 推荐下一步动作；
- 总结客户关注点。

避免：

- AI 智能赋能；
- 一键生成一切；
- 全知助手；
- 智慧洞察；
- 深度思考中；
- 100% 准确。

当结果存在不确定性时，说明依据、限制和需要用户确认的部分。

---

## 10. 术语治理

建立项目 Glossary：

| 术语 | 定义 | 不使用的同义词 | 示例 | Owner |
|---|---|---|---|---|

组件、接口、文档、报表和 AI Prompt 应使用同一术语表。术语变化属于产品变更，需要迁移和兼容计划。


---


# 08｜Responsive, Accessibility & Performance

可访问性和性能不是上线前检查项，而是界面行为的一部分。

---

## 1. 响应式不是桌面缩小

针对每个断点重新判断：

- 核心任务是什么；
- 哪些信息必须始终可见；
- 哪些可以折叠、延后或转成详情；
- 表格如何转化；
- Inspector 如何呈现；
- 固定工具栏是否遮挡内容；
- 键盘和触控模式有何差异。

### 常见策略

- 桌面 List–Detail → 移动端列表 + 独立详情页；
- 多列表格 → 保留关键列 + 行详情；
- 右侧 Inspector → 底部 Sheet 或独立页面；
- 多列 Dashboard → 按决策顺序纵向排列；
- Hover 操作 → 始终可见或通过明确菜单触达。

---

## 2. 设备与视口覆盖

至少验证：

- 窄屏移动端；
- 常见手机；
- 平板；
- 笔记本；
- 普通桌面；
- 超宽屏；
- 200% Zoom；
- 浏览器显示滚动条为“始终”；
- 长中文、长英文、长数字和高密度数据。

不要只在设计稿默认宽度下评审。

---

## 3. 键盘与焦点

- 所有核心流程可键盘完成；
- Focus Ring 清晰；
- Tab 顺序与视觉顺序一致；
- Dialog / Drawer 有 Focus Trap；
- 关闭后焦点返回触发位置；
- Menu、Tabs、Combobox 遵循成熟 ARIA 模式；
- 快捷键支持不同键盘布局和平台；
- 快捷键可发现、可关闭且不与浏览器冲突；
- 拖拽提供键盘替代。

---

## 4. 触控与点击目标

- 视觉目标过小时扩大 Hit Target；
- 移动端使用更大的触控目标；
- 相邻目标避免重叠；
- 不依赖精细鼠标移动；
- 不出现看起来能点但只能点文字的一小块区域；
- 控件支持触控反馈；
- 不禁用浏览器缩放。

---

## 5. 语义与屏幕阅读器

- 使用正确 HTML 元素；
- 标题层级连续；
- 表单字段有 Label；
- 图标按钮有名称；
- 表格有正确表头关系；
- 状态变化通过合适的 Live Region 通知；
- 图表提供文字摘要和可访问数据；
- 装饰图形从辅助技术中隐藏；
- 错误和字段建立程序关联；
- AI 结果标记其生成来源和状态。

---

## 6. 对比与非颜色表达

项目可自由定义品牌色，但必须：

- 正文和关键控件满足可读对比；
- Hover、Active、Focus 比静止状态更明确；
- 状态同时使用文字、图标、形状或位置；
- 图表考虑色觉差异；
- 禁用状态仍可理解；
- 深浅主题分别验证；
- 半透明和模糊背景不会破坏内容可读性。

---

## 7. Motion Accessibility

- 支持 `prefers-reduced-motion`；
- 不使用自动播放、持续闪烁和大范围视差；
- 重要信息不只通过动画出现；
- 动画可暂停或跳过；
- Loading 不依赖持续高频闪动；
- AI 执行状态完成后停止动态效果。

---

## 8. 感知性能

性能设计目标：

- 输入立即响应；
- 点击立即得到反馈；
- 页面结构尽早稳定；
- 内容逐步出现但不产生大范围跳动；
- 长任务解释当前阶段；
- 用户可以继续处理其他任务。

### 技术实践

- 避免输入时昂贵的受控重渲染；
- 大列表虚拟化或使用 `content-visibility`；
- 固定图片尺寸，避免 CLS；
- 只预加载首屏关键资源；
- 字体子集化；
- 减少主线程长任务，必要时使用 Worker；
- 使用 CSS Grid/Flex 和 Intrinsic Layout，避免 JS 反复测量；
- 在慢网络和低性能设备上测试；
- 记录关键交互延迟，而不是只看 Lighthouse 总分。

---

## 9. 状态持久化

- 筛选、Tab、分页和展开状态尽量通过 URL 或可靠状态保存；
- 刷新不丢失未提交输入；
- Hydration 不导致输入失焦或值丢失；
- 返回页面恢复滚动位置；
- 长任务可以通过 Run ID 恢复；
- 离线和网络重连后状态一致。

---

## 10. 测试矩阵

每个关键页面至少测试：

- 键盘完整流程；
- 屏幕阅读器关键路径；
- 200% Zoom；
- 移动端触控；
- Reduced Motion；
- 深浅主题；
- 慢网络；
- 后端错误；
- 大数据；
- 极端长文本；
- 无权限；
- 并发修改；
- 浏览器返回/前进；
- 自动填充和密码管理器干扰。


---


# 09｜AI Product & Agent Guidelines

AI 是企业产品中的一种能力，不是默认页面形态，也不是视觉风格。

---

## 1. AI 使用决策门

引入 AI 前依次判断：

1. 规则和校验能否解决？
2. SQL、计算和报表能否解决？
3. 搜索、筛选和排序能否解决？
4. 确定性算法能否解决？
5. Workflow 和系统集成能否解决？
6. 只有在任务存在语言理解、模糊判断、内容生成、多源归纳或开放推理时，才考虑 AI；
7. 只有任务需要自主分解、多工具执行和持续状态时，才考虑 Agent。

AI 不应出现在本可以更可靠、更便宜、更可解释地解决的问题里。

---

## 2. AI 的常见价值类型

- 提取：从非结构化内容提取字段；
- 总结：压缩长文和沟通记录；
- 分类：把内容归入业务类别；
- 生成：产生草稿、方案或练习；
- 比较：识别差异、冲突和权衡；
- 推荐：基于上下文提出下一步；
- 解释：把复杂信息转换成用户可理解形式；
- 检查：发现缺口、风险和不一致；
- 编排：把开放目标转化为多步骤执行。

每个 AI 功能必须明确输出属于事实、计算、推断、建议还是草稿。

---

## 3. 最小 AI 表面

从轻到重选择：

| 层级 | 形式 | 适用 |
|---|---|---|
| P0 | 后台自动化 | 去重、分类、路由、索引 |
| P1 | Inline Assist | 单字段补全、改写、提取 |
| P2 | Object Action | 针对客户、文档、课程执行具体动作 |
| P3 | Inspector | 侧栏建议、证据、风险和解释 |
| P4 | Command Surface | 快速表达命令和参数 |
| P5 | Structured Workspace | 复杂结果、比较、编辑和落库 |
| P6 | Agent Run | 多步骤、工具调用、等待和审批 |
| P7 | Conversation–Workspace | 连续澄清 + 正式工作区 |

优先使用最小侵入的形式。不要因为使用 AI 就新增独立聊天页。

---

## 4. 聊天何时适用

适用条件：

- 用户目标开放；
- 输入难以预先结构化；
- 需要多轮澄清；
- 用户在探索而非执行固定流程；
- 结果需要语言协商。

不适用：

- 表单配置；
- 审批；
- 筛选、排序和比较；
- 批量操作；
- 权限；
- 结构化数据维护；
- 高风险执行；
- 用户已经知道目标和参数。

最佳模式通常是 **Prompt + GUI**：自然语言提供弹性，结构化控件提供范围、参数、预览和确认。

---

## 5. AI 组件最低组成

一个完整 AI 功能通常需要组合以下能力：

### 输入

- 触发入口；
- 当前业务对象；
- Context Scope；
- 参数和约束；
- 用户补充输入；
- 权限范围。

### 运行

- 当前阶段；
- 已使用的工具或数据源；
- 进度或步骤；
- 等待输入；
- 等待审批；
- 取消和后台运行。

### 结果

- 结构化结果；
- 来源和证据；
- 置信或不确定性；
- 验证状态；
- 可编辑内容；
- 前后差异；
- 应用、保存、复制、重新生成或反馈。

### 控制

- 人工审核；
- 拒绝或修改；
- 接管；
- 撤销或补偿；
- 审计。

---

## 6. AI 组件规格

每个 AI 组件至少定义：

1. Purpose；
2. User Problem；
3. Trigger；
4. Context；
5. Inputs；
6. Outputs；
7. Anatomy；
8. Variants；
9. States；
10. Actions；
11. Permissions；
12. Trust；
13. Human Control；
14. Motion；
15. Content；
16. Accessibility；
17. Analytics；
18. API / Schema；
19. Testing；
20. Anti-patterns。

---

## 7. 可信与证据

AI 输出应根据风险提供：

- 来源对象；
- 来源位置；
- 版本；
- 新鲜度；
- 权限范围；
- 规则或工具；
- 计算过程；
- 不确定性；
- 未覆盖信息；
- 验证结果。

明确区分：

- Fact；
- Rule；
- Computation；
- Inference；
- Recommendation；
- Draft。

AI 推断不能伪装成事实。来源引用必须能被用户打开或验证。

---

## 8. Human-in-the-loop

根据风险选择：

- 建议，不自动应用；
- 草稿，用户编辑后保存；
- 预览差异后确认；
- 审批后执行；
- 自动执行但可撤销；
- 自动执行并抽样审计。

以下动作通常需要明确人工闭环：

- 对外发送；
- 评分和考核；
- 权限；
- 金额；
- 合同；
- 正式知识发布；
- 删除；
- 批量写入；
- 影响客户或员工权益的决策。

---

## 9. AI 状态

至少考虑：

```text
Idle
Preparing
Collecting context
Planning
Executing
Waiting for input
Waiting for approval
Verifying
Completed
Partial
Needs review
Failed
Cancelled
```

不要使用无限旋转的“思考中”替代真实阶段。

流式输出适合让用户尽早看到内容，但最终结果必须经过结构化、验证和稳定落库。逐字打印不适合所有场景。

---

## 10. Generative UI

企业产品不应直接执行模型生成的任意 HTML、JavaScript 或 CSS。

推荐架构：

```text
业务对象
+ 受控组件注册表
+ 强类型 Schema
+ 权限和策略校验
+ 前端 Renderer
+ Fallback
+ Audit
```

模型只能选择批准的组件和参数。未知组件、非法属性和高风险动作必须被拒绝或降级。

---

## 11. AI 视觉原则

- AI 不需要统一蓝紫渐变；
- 不默认使用 Sparkles、魔法棒和机器人；
- 不用持续发光表示智能；
- AI 入口的视觉权重由业务价值决定；
- 运行时动态效果在完成后停止；
- 优先显示任务语义和结果；
- 当用户需要知道 AI 参与时，明确标记并提供解释；
- 不要让 AI 面板挤压核心工作区。

---

## 12. 场景示例

### CRM

AI 适合：总结沟通、提取五要素缺口、生成下一步动作草稿。  
不适合：替代 CRM 字段、直接自动修改关键阶段而不提供证据。

### 销售训练

AI 适合：客户角色扮演、练习反馈、个性化补练。  
不适合：只提供聊天而没有课程、练习、记录、评分依据和主管干预。

### 知识库

AI 适合：检索增强回答、摘要、对比和资料发现。  
不适合：绕过权限、隐藏来源、把旧资料当最新结论。

### 异常发现

优先规则、计算和工作流识别确定性异常；AI 用于文本归因、解释和未知模式辅助，不应替代可明确编码的规则。


---


# 10｜Design System Engineering & Governance

设计系统不只是组件库，而是产品判断、工程约束和团队知识的可执行载体。

---

## 1. 系统分层

建议分为：

```text
Principles
→ Foundations / Tokens
→ Primitives
→ Components
→ Patterns
→ Page Archetypes
→ Product Decisions
→ Exemplars
→ Lint / Tests / Evals
```

### Principles

解释为什么做出某类选择。

### Foundations

项目特定的颜色、排版、间距、圆角、动效、图标和主题。通用规范不替项目决定这些值。

### Components

可复用 UI 和行为单元。

### Patterns

表格筛选、审批、空状态、长任务、权限等组合决策。

### Page Archetypes

List–Detail、Dashboard–Drilldown、Canvas–Inspector 等页面骨架。

### Product Decisions

与本产品对象、术语、权限和流程有关的具体选择。

---

## 2. 目录建议

```text
repository/
├── AGENTS.md
├── DESIGN.md
├── design-system/
│   ├── foundations/
│   ├── components/
│   ├── patterns/
│   ├── page-models/
│   ├── copy/
│   ├── glossary/
│   ├── accessibility/
│   ├── resilience/
│   ├── decisions/
│   ├── exemplars/
│   └── coverage-gaps.md
├── tooling/
│   ├── lint/
│   ├── visual-tests/
│   ├── a11y-tests/
│   └── evals/
└── apps/
```

---

## 3. 规则放在哪里

### Lint

适合能够可靠、低误报地通过代码识别的问题：

- Icon-only Button 缺少名称；
- 导航使用 Div Click 而不是 Link；
- 禁止 `transition: all`；
- 缺失 Label；
- Dialog 缺少标题；
- 使用未经批准组件；
- 直接执行生成 HTML。

### Agent Guidance

适合需要页面和产品上下文的判断：

- 是否应该使用 Dashboard；
- 是否需要 Modal；
- 卡片是否过多；
- 信息层级是否合理；
- AI 是否必要；
- 是否应使用 List–Detail。

### 人类决策

适合：

- 新产品政策；
- 业务语义；
- 高风险自动化；
- 新设计方向；
- 大量例外的规则；
- 无充分证据的标准。

不要把所有问题都交给 Lint，也不要让所有问题都依赖人工评审。

---

## 4. Decision Record

每条正式规则建议使用稳定 ID，并记录：

```text
Status
Scope
Decision
Rationale
Evidence
Exceptions
Bad example
Good example
Assumptions
Open decisions
Owner
Review date
```

规则没有证据或边界时，很容易变成审美口号。

---

## 5. Exemplars

Exemplar 不是“漂亮截图”，而是一个值得复用的已交付决策：

- 解决了什么问题；
- 为什么采用该模式；
- 哪些部分可复用；
- 哪些部分是特定场景；
- 已知缺陷；
- 关联 PR、设计稿、测试和数据。

不要把一个 Shipped 页面视为天然完美。例子可以包含值得保留的决策和需要避免的缺陷。

---

## 6. Coverage Gaps

明确记录系统暂时没有答案的问题：

- 新的复杂表格模式；
- 移动端审批；
- Agent 多人协作；
- 大模型成本可视化；
- 某类数据图；
- 特殊行业可访问要求。

缺口可见比临时拍脑袋好。Agent 遇到缺口时应提出，而不是自行发明“公司标准”。

---

## 7. Figma 与代码

- Token 语义一致；
- 组件名称一致；
- Variant 和 Props 尽量可映射；
- 状态 Story 完整；
- 文档由明确 Owner 维护；
- 设计稿不使用代码中不存在的“幽灵组件”；
- 代码不通过局部 CSS 创建无法回流的分叉；
- 变更同时更新设计、代码、文档和测试。

---

## 8. Registry 与 MCP

为了让 AI Agent 准确使用系统，建议提供：

- 可读组件源码或清晰 API；
- 组件 Registry；
- Token；
- 使用与禁用规则；
- 页面模式；
- 正反例；
- Glossary；
- MCP 或可检索文档；
- 版本信息；
- 变更记录。

AI 需要的不只是组件外观，还需要其组合方式、行为和背后的判断。

---

## 9. 变更治理

1. 收集重复评审意见和真实问题；
2. 保留来源和上下文；
3. 分离“证据收集”和“规则判断”；
4. 由 Owner 审核是否成为规则、示例、Lint、Eval 或不采纳；
5. 在最窄的文件中更新；
6. 增加测试或 Eval；
7. 版本化发布；
8. 观察是否真正减少问题；
9. 删除失效规则。

设计系统不是只增不减的档案库。

---

## 10. 成熟度

### Level 1：样式统一

有基础 Token 和少量组件。

### Level 2：组件统一

有 Storybook、状态和 API。

### Level 3：模式统一

表格、表单、审批、错误和页面模式稳定。

### Level 4：判断可复用

有 Decision、Exemplar、Glossary 和治理。

### Level 5：机器可执行

有 Lint、视觉测试、A11y、Agent Skill、MCP 和 Evals。

不要在 Level 1 时宣称“企业级设计系统已经完成”。


---


# 11｜AI Coding Agent Workflow

本文件定义如何让 Codex、Claude Code、Cursor、Kimi Code 等 Agent 遵守产品设计，而不是只生成能运行的页面。

---

## 1. 为什么代码上下文不够

代码只能告诉 Agent 过去交付了什么，不能完整告诉它：

- 为什么选择这个模式；
- 哪些历史实现是妥协；
- 哪些规则有例外；
- 哪些评审意见已成为标准；
- 当前产品的业务对象和术语；
- 哪些状态和风险最重要。

因此设计判断必须进入仓库，成为 Agent 可加载、可引用和可测试的上下文。

---

## 2. 单一入口，内部路由

建议建立 `.agents/skills/product-design/`，并由根目录 `AGENTS.md` 明确触发：

> 当任务改变用户看到、理解、选择或执行的内容时，必须加载 product-design Skill。

Skill 内先判断请求模式：

- `shape`：梳理问题、流程和页面模式；
- `implement`：实现页面或组件；
- `review`：只审计，不擅自改造；
- `copy`：只优化界面语言；
- `harden`：补齐状态、可访问、响应式和性能；
- `govern`：更新设计规则、例子或 Eval。

这样可以防止“审计请求变成全面重构”或“文案优化变成页面改版”。

---

## 3. Agent 开始任务前必须报告

- 当前请求模式；
- 目标用户和任务；
- 核心对象；
- 目标页面模式；
- 已加载的规范文件；
- 现有组件和页面模式；
- 已知限制；
- 是否涉及 AI；
- 需要验证的状态；
- 计划修改的文件。

报告不需要冗长，但必须证明 Agent 不是从默认 Dashboard 模板开始。

---

## 4. 实现流程

### Step 1：发现

- 阅读 PRD、领域模型、权限和现有页面；
- 搜索重复组件和模式；
- 检查设计系统；
- 使用真实数据结构；
- 找出状态和失败路径。

### Step 2：形态选择

- 选择页面模型；
- 说明不选择其他模型的原因；
- 明确主任务、主操作、信息层级；
- 若涉及 AI，先通过 AI Decision Gate。

### Step 3：实现

- 优先正式组件；
- 不创建无必要的局部设计系统；
- 状态与 API 契约一致；
- URL 保存可分享状态；
- 键盘、焦点和响应式同步完成；
- 不在最后阶段才补错误态。

### Step 4：验证

- 正常、空、慢、错、权限、冲突；
- 窄屏、宽屏、Zoom；
- 键盘和屏幕阅读器；
- 大数据和长文本；
- 性能；
- AI 证据、审批和恢复。

### Step 5：报告

- 修改了什么；
- 关键设计判断；
- 使用了哪些正式组件；
- 覆盖了哪些状态；
- 证据；
- 未解决缺口；
- 新增规则是否需要进入设计系统。

---

## 5. 多 Agent 协作

可按职责并行：

1. Product / IA Agent：任务、对象、流程、页面模式；
2. Interface Agent：布局、组件和交互；
3. Resilience Agent：状态、错误、权限和恢复；
4. Accessibility Agent：键盘、语义和响应式；
5. Content Agent：术语、文案和状态词；
6. Engineering Agent：组件复用、性能和测试；
7. Reviewer Agent：按统一 Rubric 做独立审计。

主 Agent 负责统一决策，不得把并行结果简单拼接成 Frankenstein 页面。

---

## 6. Agent 禁止行为

- 未阅读项目规范就生成页面；
- 默认建立 Sidebar + KPI Cards + Charts；
- 无业务依据新增图表和指标；
- 通过大量卡片掩盖信息架构；
- 自行发明品牌色、字体和组件；
- 为“科技感”添加渐变、光球和粒子；
- 把所有 AI 能力做成聊天；
- 只实现正常态；
- 用 Mock 数据掩盖 API 和状态问题；
- 因发现小问题偏离主任务做无边界重构；
- 把一个截图或外部产品当成普遍标准；
- 修改设计系统规则但没有证据和人类审核。

---

## 7. 机械规则与判断规则

- 代码可可靠判断、误报低、修复明确 → Lint；
- 需要产品和页面上下文 → Agent Guidance；
- 新标准、高风险策略、业务语义 → 人类决策；
- 无论哪类，都应有例子或 Eval 防止回归。

---

## 8. Evals

对 Agent 设计能力的 Eval 应分开评估：

1. 是否正确触发并加载 Skill；
2. 是否正确应用规则；
3. 是否保留已有正确行为；
4. 是否改善任务质量；
5. 是否避免过度修改；
6. 是否覆盖状态和可访问性；
7. 是否能在未见过的页面上泛化。

不要只比较像不像一个已交付截图。已交付实现本身可能存在缺陷。


---


# 12｜Project Implementation Playbook

本文件给出从零开发或重构一个企业项目的建议流程。

---

## Phase 0｜问题与范围

### 产出

- 用户与角色；
- 业务目标；
- 当前流程；
- 关键对象；
- 风险与权限；
- 成功指标；
- 非目标；
- 项目边界。

### Gate

- 不是为了“做一个系统”而做系统；
- 核心任务和业务结果可被验证；
- 不依赖假设性的全量功能列表。

---

## Phase 1｜领域模型与信息架构

### 产出

- 对象模型；
- 对象关系；
- 生命周期；
- 权限矩阵；
- 主导航；
- 页面地图；
- URL 策略；
- 关键流程。

### Gate

- 导航使用稳定分类标准；
- 核心对象身份和状态清楚；
- 页面不是功能堆叠；
- 角色差异有明确策略。

---

## Phase 2｜项目 Foundation

在项目的正式文档、Token、组件库和代码约束中明确：

- 品牌与语气；
- Token；
- 字体和图标；
- 表面与主题；
- 密度；
- 组件技术栈；
- 可访问目标；
- 浏览器与设备支持；
- 性能目标；
- Design System Owner。

本阶段不要求把整个组件库做完。只建立足以支持核心路径的基础。

---

## Phase 3｜核心页面规格

优先设计端到端主线，而不是按菜单逐页制作。

例如销售训练：

```text
新人首页
→ 学习课程
→ 完成小测
→ AI 补练
→ 查看反馈
→ 主管干预
→ 后台发布和回滚
```

每个页面必须在功能规格或任务说明中明确任务、对象、状态、权限和数据。

---

## Phase 4｜交互原型

原型应验证：

- 页面之间的连续任务；
- 主操作；
- 筛选和下钻；
- 错误和恢复；
- 权限；
- 长任务；
- 移动或窄屏；
- AI 上下文、结果和审批。

不要只制作视觉静态稿。交互原型可以由讲师点击讲解，也可以用于真实用户任务测试。

---

## Phase 5｜工程实现

### 建议顺序

1. App Shell 与路由；
2. 对象模型和数据契约；
3. 核心组件和页面模式；
4. 主路径；
5. 状态、权限和恢复；
6. 响应式和可访问；
7. 性能；
8. AI；
9. 审计与分析。

AI 不应在业务对象、权限和基础流程未稳定前成为架构中心。

---

## Phase 6｜Hardening

- 真实数据；
- 极端数据；
- 并发；
- 慢网络；
- 离线；
- 错误；
- 权限；
- 导入导出；
- 大列表；
- 键盘；
- Zoom；
- 屏幕阅读器；
- 安全；
- 审计；
- 可观测性。

---

## Phase 7｜发布

- Feature Flag；
- 小范围用户；
- 新旧版本快速对比；
- 数据和反馈；
- 回滚方案；
- 培训和文档；
- Support Playbook；
- 发布后复盘。

---

## Phase 8｜治理

把重复问题转化为：

- 组件改进；
- 页面模式；
- Decision Record；
- Lint；
- Agent Skill；
- Eval；
- Coverage Gap。

项目完成不是规范结束，而是规范开始获得真实证据。

---

## 推荐交付物

```text
PRD
Domain Model
Permission Matrix
Information Architecture
Page Specs
Interaction Prototype
Project Foundation
Component Stories
API / Schema
State Matrix
Test Plan
Accessibility Report
Performance Report
Decision Records
Release Checklist
```


---


# 13｜Design Review & QA Checklist

使用 PASS / PARTIAL / FAIL / BLOCKED，并附证据。不要用“感觉不错”替代验收。

---

## 1. 一票否决

出现任一项，不得发布：

- 越权读取或写入；
- 高风险动作无确认、审批或审计；
- 用户输入在失败后丢失；
- 部分失败显示为全部成功；
- 核心任务无法键盘完成；
- 关键内容在 Zoom 或窄屏不可达；
- 无法停止持续或高成本任务；
- 直接执行模型任意生成的代码或 UI；
- 正式结果伪造来源、状态或人工批准；
- 生产写入绕过业务 API；
- 无法通过稳定 URL 定位核心对象；
- 无任何错误、权限和恢复设计。

---

## 2. 产品判断｜20 分

- [ ] 用户和场景明确；
- [ ] 页面任务明确；
- [ ] 核心对象明确；
- [ ] 主操作明确；
- [ ] 页面模式选择有依据；
- [ ] 非目标明确；
- [ ] 取舍明确；
- [ ] 指标与业务结果对应；
- [ ] AI 不是默认答案；
- [ ] 页面没有无依据功能。

---

## 3. 信息架构｜10 分

- [ ] 导航分类标准一致；
- [ ] 层级可预测；
- [ ] 对象状态和位置清楚；
- [ ] URL 保存关键状态；
- [ ] 同一对象跨页面表达一致；
- [ ] 没有重复导航；
- [ ] 设置与业务域分离；
- [ ] 返回和关闭行为稳定。

---

## 4. 视觉层级与密度｜10 分

- [ ] 工作对象高于产品外壳；
- [ ] 一页一个主要视觉焦点；
- [ ] 辅助信息退后；
- [ ] 结构主要由对齐、间距和关系形成；
- [ ] 没有卡片墙；
- [ ] 没有无意义大留白；
- [ ] 密度与任务匹配；
- [ ] 图表支持行动；
- [ ] AI 入口没有无理由抢占注意力。

---

## 5. 组件与交互｜15 分

- [ ] 使用正式组件；
- [ ] Button 与 Link 语义正确；
- [ ] 选择控件匹配选项规模；
- [ ] Modal、Drawer、Popover 使用合理；
- [ ] Focus 管理正确；
- [ ] 点击目标合理；
- [ ] Links 支持浏览器标准行为；
- [ ] Undo、确认和审批与风险匹配；
- [ ] 动画有目的、可中断；
- [ ] Icon 使用一致；
- [ ] 拖拽有替代交互；
- [ ] 没有死区和隐藏操作。

---

## 6. 企业模式｜15 分

- [ ] 表单标签、帮助和错误完整；
- [ ] 保存模型明确；
- [ ] 表格对齐、列和批量操作合理；
- [ ] 筛选状态可见且可分享；
- [ ] Dashboard 有异常、原因和下钻；
- [ ] 审批有差异、证据和影响；
- [ ] 权限状态可理解；
- [ ] 导入导出支持错误报告和异步任务；
- [ ] 设置有影响范围、测试和回滚；
- [ ] 审计可理解。

---

## 7. 状态与韧性｜15 分

- [ ] Loading 不闪烁、不阻塞无关区域；
- [ ] Empty / No Result / Zero State 区分；
- [ ] Error 可恢复；
- [ ] Partial Success 准确；
- [ ] 无权限与无数据区分；
- [ ] 并发冲突可处理；
- [ ] 自动保存状态完整；
- [ ] 长任务可后台运行和恢复；
- [ ] 请求幂等或防重复；
- [ ] 刷新和返回不丢失关键状态。

---

## 8. 文案｜5 分

- [ ] 术语一致；
- [ ] 按钮说明结果；
- [ ] 错误说明恢复；
- [ ] 空状态说明原因和下一步；
- [ ] 状态词对应生命周期；
- [ ] 数据口径、单位和时间明确；
- [ ] 无空泛 AI 营销词。

---

## 9. 响应式、可访问、性能｜10 分

- [ ] 窄屏重新编排；
- [ ] 200% Zoom 可用；
- [ ] 键盘完整；
- [ ] Focus 清晰；
- [ ] 语义 HTML；
- [ ] 状态非颜色唯一表达；
- [ ] Reduced Motion；
- [ ] 大列表策略；
- [ ] 输入和点击及时响应；
- [ ] 慢网络和低性能设备验证。

---

## 10. AI｜仅在适用时

- [ ] AI 必要性有论证；
- [ ] 选择最小 AI 表面；
- [ ] Context Scope 可见；
- [ ] 输出类型明确；
- [ ] 来源、证据和限制可查看；
- [ ] 高风险动作有人类闭环；
- [ ] 运行状态真实；
- [ ] 可以取消、接管、撤销或补偿；
- [ ] AI 不是唯一完成基础任务的方式；
- [ ] 未直接执行任意生成 UI 或代码。

---

## 11. 建议评分

- 90–100：可发布；
- 80–89：可受控发布，必须有缺口计划；
- 70–79：仅内部试用；
- 60–69：结构性问题，需返工；
- <60：不应进入开发或发布。

任何一票否决项优先于总分。


---


# 14｜Anti-patterns

以下问题在 AI 生成和企业后台中尤其常见。

---

## 1. Dashboard Theater

**表现：** 首屏塞入 KPI、折线、饼图、排行榜，但用户无法据此行动。  
**修复：** 从角色决策开始，删除不能回答问题或不能下钻的指标。

## 2. Card Wall

**表现：** 每个字段、图表、动作和状态都有独立卡片。  
**后果：** 层级扁平、扫描困难、页面变长。  
**修复：** 使用分区、列表、表格和清晰页面模型。

## 3. Sidebar Dominance

**表现：** 导航亮度、图标和颜色比工作区更强。  
**修复：** 到达目标页后，导航退后，工作对象成为焦点。

## 4. Modal Chain

**表现：** 弹窗打开弹窗，用户失去上下文。  
**修复：** 使用 Drawer、页面、步骤或内联编辑。

## 5. Icon Soup

**表现：** 每个文字旁都有图标，每个图标有彩色底板。  
**修复：** 仅在识别对象、提高扫描或执行通用动作时使用图标。

## 6. Excessive Whitespace

**表现：** 通过巨大标题、卡片和留白制造高级感。  
**修复：** 根据任务和使用频率选择密度，让用户在一屏完成更多判断。

## 7. Hidden State

**表现：** 筛选、自动保存、权限、数据范围和 AI 上下文不可见。  
**修复：** 把会影响结果的状态显示在用户能理解的位置。

## 8. Generic Actions

**表现：** 按钮都是“确定”“处理”“继续”。  
**修复：** 使用动词 + 对象，说明结果。

## 9. Destructive Action Without Recovery

**表现：** 删除、覆盖、批量修改直接执行。  
**修复：** 根据风险提供 Undo、Diff、审批、审计和补偿。

## 10. Responsive Shrink

**表现：** 桌面页面按比例压到手机。  
**修复：** 重新排序任务、减少并列信息、转换页面模式。

## 11. Happy-path-only Design

**表现：** 只有 6 条漂亮 Mock 数据。  
**修复：** 强制覆盖空、慢、错、权限、冲突、长文本和大数据。

## 12. Chat Everywhere

**表现：** 所有 AI 功能都变成 Ask Anything。  
**修复：** 使用 Inline、Object Action、Inspector、Structured Workspace 或后台自动化。

## 13. AI Visual Theater

**表现：** 蓝紫渐变、光球、扫描线、Sparkles、持续“思考中”。  
**修复：** 直接呈现任务、阶段、证据和结果。

## 14. AI as Source of Truth

**表现：** 推断被写成事实，来源不可验证。  
**修复：** 区分事实、规则、计算、推断和建议；提供来源和人工确认。

## 15. Frankenstein Interface

**表现：** 多个 Agent 或模板输出直接拼接，密度、术语、状态和组件各不相同。  
**修复：** 主 Agent 统一页面模型、设计系统和验收规则，再合并实现。

## 16. Local Design System

**表现：** 每个页面新建一套 Button、Card、Badge 和 CSS。  
**修复：** 扩展正式组件；确需新语义时记录 Decision。

## 17. Copying External Products Literally

**表现：** 复制 Linear、Stripe 或 Vercel 视觉，但业务对象和流程不匹配。  
**修复：** 学习其判断：注意力、密度、结构和可靠性，而不是皮肤。

## 18. Settings Dump

**表现：** 所有数据库字段变成一个设置页面。  
**修复：** 按用户任务、影响范围和风险重组，提供默认、测试和回滚。

## 19. Permission by Hiding

**表现：** 前端隐藏按钮就认为安全。  
**修复：** 服务端校验，前端准确表达只读、部分权限和申请路径。

## 20. Premature Abstraction

**表现：** 为一次页面需求创建巨大万能组件。  
**修复：** 先完成稳定模式，再基于两个以上真实场景抽象。


---


# 15｜ToB Restrained Style Guide

本文件描述 Linear、Vercel 等成熟 ToB/开发者产品中值得借鉴的“克制感”，但不限定具体品牌色、字体或圆角。

---

## 1. 克制不是素，也不是空

克制的企业界面仍然可以精致、有辨识度。区别在于：

- 精致来自对齐、状态、反馈和速度；
- 品牌来自稳定选择，而不是遍地装饰；
- 高级来自信息被正确组织；
- 动效来自因果和空间，而不是持续表演；
- 新颖集中在少数关键时刻。

“白底 + 灰边框 + 大留白”不是克制，只是缺乏设计判断。

---

## 2. 视觉优先级

### 工作对象最强

当前客户、项目、任务、文档、流程、异常或运行结果是页面主角。

### 产品外壳退后

导航、工具栏和辅助面板帮助定位，但在用户到达目标后应降低权重。

### 状态按重要性表达

正常状态通常安静；风险、阻塞和等待处理才需要提高注意力。

### 品牌不覆盖任务

品牌表达集中在应用入口、关键空状态、里程碑、官网和少数标志性组件，而不是每个按钮和卡片。

---

## 3. 信息密度

- 允许同屏出现足够多的信息；
- 通过对齐和层级提高扫描效率；
- 让次要信息退后，而不是全部删除；
- 表格、列表和 Inspector 各司其职；
- 不为“高级感”增加滚动；
- 熟练用户的高频操作尽量短；
- 新人通过解释和默认值获得帮助，而不是把所有页面永久放大。

---

## 4. 表面与边界

- 能通过关系表达的，不额外加框；
- 边框少而明确；
- 浮层才需要明显 Elevation；
- 嵌套表面有清楚层级；
- 不使用三层卡片；
- 背景差异微妙但可辨；
- Focus、Selection 和 Active 比静态状态更明确。

---

## 5. 排版

本规范不强制具体字体或字重，但建议：

- 字体数量少；
- 标题不过度夸张；
- 数据和代码使用适合扫描的数字样式；
- 页面层级主要依赖大小、位置和间距，不靠连续加粗；
- 长文限制阅读宽度；
- 表格和工具区使用紧凑、稳定的排版；
- 中文不使用无意义字距和全大写式装饰。

---

## 6. 图标与装饰

- 图标是识别和操作工具，不是填空；
- 减少不必要的彩色图标背景；
- 图标尺寸与文字重量协调；
- 同一对象和动作始终使用同一语义；
- 插画和装饰只在能解释产品或建立品牌时出现；
- 不给每个功能入口配一个“科技感”图形。

---

## 7. 动效

### Productive Motion

用于 Hover、菜单、展开、选择、保存和数据更新。特点是短、稳、无阻塞。

### Expressive Motion

用于首次体验、关键里程碑、重要发布和品牌时刻。特点是低频且有意义。

规则：

- 高频越高，动效越少；
- 可中断、可反向；
- 起点和终点符合空间关系；
- 不自动播放；
- 不用动效掩盖加载慢；
- 完成后回归安静状态。

---

## 8. 文案

- 准确；
- 短；
- 动作导向；
- 术语一致；
- 错误提供出口；
- 不使用“赋能”“智能化”“智慧中枢”等空话；
- 不夸大 AI；
- 不把技术概念强加给普通用户。

---

## 9. 什么产生“高级感”

1. 同一对象在所有页面一致；
2. 页面打开后用户立即知道当前状态；
3. 键盘、焦点、返回和滚动符合预期；
4. 内容加载时布局稳定；
5. 错误后输入不丢失；
6. 列表、筛选和 URL 可恢复；
7. 权限和审批清楚；
8. 动效不拖慢高频操作；
9. 图表可以下钻到行动；
10. AI 结果有来源、控制和正式落点；
11. 极端数据仍然稳定；
12. 每一个视觉强调都拥有业务理由。

---

## 10. 风格审计问题

- 是否因为“像 ToB”而做得过于普通？
- 是否因为“想高级”而降低了效率？
- 是否因为“想有 AI 感”而增加了视觉噪声？
- 是否存在无意义的卡片、图表、图标和徽章？
- 是否能在不改变品牌色的情况下明显改善层级？
- 是否能通过删除一个元素让页面更强？
- 是否在一个关键位置拥有真正的产品辨识度？


---
