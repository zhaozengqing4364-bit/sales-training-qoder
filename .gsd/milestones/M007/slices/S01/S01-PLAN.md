# S01: 教练健康状态真相收口

**Goal:** 把当前 `/practice/{sessionId}` learner route 上的 coach-health 真相收口到同一个 runtime authority：degraded / resumed 要在不中断训练的前提下明确可见，且 reconnect 之后前端与 runtime/diagnostic surfaces 不再对恢复状态说谎。
**Demo:** After this: After this: on the current `/practice/{sessionId}` learner route, coach degraded / resumed is explicitly visible without interrupting training, and reconnect no longer causes the UI/runtime surfaces to lie about recovery state.

## Tasks
- [x] **T01: Reconnect now emits normalized coach-health state from the live handler instead of replaying stale raw snapshots.** — Lock the highest-risk boundary first: backend snapshot/restore plus frontend websocket restore must agree on one coach-health truth source before any learner-page UI work. Steps: 1. Verify `StepFunRealtimeHandler` and `EnhancedSalesHandler` persist non-healthy `coach_health` into reconnect snapshots and restore it before `reconnected` is emitted, keeping `healthy -> degraded -> resumed -> healthy` semantics intact. 2. Adjust handler or reducer code only where the current restore path can lie; preserve backend runtime snapshot as the single authority and do not introduce polling or client-only derivation. 3. Extend focused tests so degraded, resumed, malformed payload, and omitted-after-recovery cases are all explicit across backend and frontend. Must-haves: reconnect clears stale degraded state only when the snapshot truly omits `coach_health`; StepFun and classic handlers follow the same contract; `/knowledge-check` keeps exposing live coach health for inspection. Failure modes: stale reconnect snapshot marks UI healthy too early; invalid `coach_health.status` crashes or renders bogus state; classic and StepFun drift apart. Load profile: reconnect storms must stay O(1) with no extra fetch loop or repeated diagnostics polling. Negative tests: malformed `coach_health` payload, omitted `coach_health` after a prior degraded state, resumed state surviving restore, and live knowledge-check inspection returning the same degraded payload shape.
  - Estimate: 1h
  - Files: backend/src/sales_bot/websocket/stepfun_realtime_handler.py, backend/src/sales_bot/websocket/enhanced_handler.py, backend/src/common/api/practice.py, web/src/hooks/websocket/message-handlers.ts, backend/tests/unit/test_stepfun_realtime_persistence.py, backend/tests/unit/test_enhanced_handler_coach_health.py, backend/tests/integration/test_voice_runtime_session_snapshot.py, web/src/hooks/websocket/message-handlers.test.ts
  - Verify: backend/venv/bin/python -m pytest -c backend/pyproject.toml backend/tests/unit/test_enhanced_handler_coach_health.py
backend/venv/bin/python -m pytest -c backend/pyproject.toml backend/tests/unit/test_stepfun_realtime_persistence.py -k "restore_session_state"
backend/venv/bin/python -m pytest -c backend/pyproject.toml backend/tests/integration/test_voice_runtime_session_snapshot.py -k "live_coach_health"
npm test -- --run 'web/src/hooks/websocket/message-handlers.test.ts'
- [x] **T02: Surfaced degraded/resumed coach-health on the learner page shell and fixed the repo-root Vitest shim so the planned `web/src/...` verification command executes correctly.** — Make degraded/resumed explicitly visible on the learner route itself, especially on mobile where the current signal is hidden until the bottom sheet is opened. Steps: 1. Add a compact, non-blocking page-shell strip/chip near the primary practice controls that mirrors the same `coachHealth` object already passed to `RightPanelContent`. 2. Keep the richer right-panel card in place so the learner still gets explanatory copy without losing stage, score, action-card, or fuzzy-detection surfaces; extract a tiny shared presentation helper only if duplication would cause copy/style drift. 3. Extend learner-page tests so the page-shell status is asserted even when `RightPanelContent` is mocked, and keep right-panel tests proving degraded/resumed copy does not hide the active coaching guidance. Must-haves: degraded/resumed is visible without opening the analysis panel; healthy state stays quiet/non-distracting; no second state source is introduced. Negative tests: mobile/desktop render parity, healthy state not showing a warning banner, and degraded/resumed copy coexisting with stage and score guidance.
  - Estimate: 1h
  - Files: web/src/app/(user)/practice/[sessionId]/page.tsx, web/src/app/(user)/practice/[sessionId]/page.test.tsx, web/src/components/practice/RightPanelContent.tsx, web/src/components/practice/RightPanelContent.test.tsx
  - Verify: npm test -- --run 'web/src/components/practice/RightPanelContent.test.tsx' 'web/src/app/(user)/practice/[sessionId]/page.test.tsx'
