# Knowledge

- Admin completed-session previews must batch-load `ConversationMessage` rows and run `SessionEvidenceService.build_projection(...)` instead of reusing the legacy 0.4/0.3/0.3 weighting; otherwise admin tables drift from `/practice/{sessionId}/report` even when both point at the same session.
- If local runtime UAT for `/admin/users/{id}` or `/api/v1/admin/users/{id}/sessions` suddenly looks like a frontend/CORS regression, check Alembic first: missing revision `20260317_2310_020` breaks projection reads with `conversation_messages.transcript_metadata does not exist` and can surface in the browser as failed session-preview requests.
- Report-page focused tests need an explicit assertion that `导出报告` is absent; otherwise the dead affordance can drift back into `web/src/app/(user)/practice/[sessionId]/report/page.tsx` without breaking the existing positive-path assertions.
- Standard PPT 这条链的 live contract 在 `/api/v1/presentations`，不是旧的 `/api/v1/admin/presentations`：当前 admin 详情页与用户演练入口都直接消费 `api.presentations`，而 legacy admin backend 仍引用过期 schema；做 replace/version_number/status 修复时应扩展 live surface。
- 本地要跑 S04 presentation/runtime UAT 时，`cd backend && PYTHONPATH=src venv/bin/uvicorn main:app --port 3444` 可能因缺少 `redis` Python 包在启动阶段崩溃并报 `redis package is required for SessionStateService`；先补齐该本地依赖，否则浏览器验证会被环境问题卡住而不是代码回归。
- S04 的 task-level auto verification 不要把 backend/web 检查串成一条 `Verify: ... && cd ../web && ...`；auto-mode 会把它拆成独立检查，导致裸 `pytest ...` 和 `cd ../web` 从 repo root 运行后误报失败。把 backend 与 web 命令写成两条独立校验更稳。
- `backend/tests/integration/test_knowledge_flow.py` 依赖 `/api/v1/auth/dev-login` 夹具；若你改用 repo-root `backend/venv/bin/python -m pytest -c backend/pyproject.toml ...` 运行，可能因为 cwd / env 装载差异让 dev-login 变成 `403`、进而把会话创建打成 `401`。这组知识链回归请继续从 `backend/` 目录下按计划命令运行。
- 本地 `20706b4b-bb22-484a-8f2f-8ecacc43bb3b` 这份 ready standard deck 在 admin 详情页会批量请求缺失 thumbnail 并产生大量 `404 Thumbnail not found` 噪声；它不影响 replace blocker / version/status 合同，但会污染浏览器 console/network 错误列表，排查时别把它误判成 replace 回归。
- S05 改销售语义时，优先保留 `pass_flags.pass_3min_flow/pass_5turn_defense/pass_4step_structure` 和 `SessionReport.logic_score/accuracy_score/completeness_score` 这些既有键名，只在 write layer 里重映射它们的含义；如果直接改 key，report/replay/history/admin 多个消费面会被迫一起改，容易重新引入事实漂移。
- S05 的 persona objection tuning 现在把价格 / 竞品 / ROI 证据类问题并入 `stepfun_knowledge_helpers` 的 widened entity-query path（默认 `top_k=7`、snippet 420、keyword candidate limit 48）；如果旧测试还盯着 5/360，先确认是不是在断言过期基线，而不是把它误报成检索回归。
- 本地做 S05 sales realtime/browser UAT 时，如果 `/practice/{sessionId}` 能连上 backend 但 websocket 一直 `1006` 重连，并且 backend 日志报 `connecting through a SOCKS proxy requires python-socks`，先在 `backend/` 下执行 `venv/bin/python -m pip install python-socks` 再重启 `PYTHONPATH=src venv/bin/uvicorn main:app --port 3444`；否则你会误以为是 ScorePanel 或 report 语义回归，实际是本地 StepFun 上游代理依赖缺失。
- S05 之后如果 `test_apply_latest_scores_to_session_supports_legacy_overall_and_marks_zero_turn_not_evaluable` 还在断言 `90/82/80`，那是过期基线：旧 `score_snapshot.overall + 专业度/沟通技巧/销售流程` 现在会先归一化到 sales rollup，所以 zero-turn legacy fallback 应回落成 `84/84/84` 这类 overall-based sales 分数，而不是继续沿用旧 generic 维度。
- 本地 S05 report/runtime UAT 时，`/practice/{sessionId}/report` 页会并行拉 optional comprehensive-report / highlights 增强接口；这些接口出现 `404/400/500` 或 backend `REPORT_GENERATION_FAILED` 噪声时，先看 unified `/api/v1/practice/sessions/{id}/report` 与页面上的“综合洞察暂不可用 / 高光片段暂不可用”降级文案是否正常，不要把 optional enhancement 失败误判成 sales evidence contract 回归。
- `web/src/app/(user)/practice/[sessionId]/report/page.test.tsx` 里如果要断言“综合洞察暂不可用 / 高光片段暂不可用”文案，必须用 `findByText` / `waitFor` 等待 post-report effect 完成；这些提示是在 unified report 首屏渲染后，随 enhanced-report / highlights 异步请求失败才出现，直接用同步 `getByText` 很容易把真实 UI 降级能力误报成回归。
- 写 projection-backed admin/history 测试时，如果想让 `SessionEvidenceService.build_projection(...)` 真正读取 `score_snapshot.dimension_scores`，就把 `PracticeSession.logic_score/accuracy_score/completeness_score` 设为 `None`；这些列一旦有值，projection 会优先使用 session 行分数而不是消息快照，测试很容易误以为 route 仍在走 legacy SQL。
- 本地用 browser route mock 验证 `/admin/users/{id}` 这类页面时，别直接 mock `http://localhost:3444/api/v1/...` 的 cross-origin `/progress` 请求：Playwright 路由在这个项目里容易把它打成 `ERR_FAILED`/CORS 样噪声，页面只会落到 network error。要验证 inline empty/error state，优先在页面里临时覆写 `window.fetch` 只拦截 progress URL，再点页面上的 `刷新`。
- 本地验证 `/support/runtime` 的 empty/error 分支也别直接 route mock `http://localhost:3444/api/v1/support/runtime/*`：跨 origin mock 同样容易掉进 `ERR_FAILED`/CORS 样噪声。先在已登录页面里临时覆写 `window.fetch` 只拦截 support runtime overview/faults，再点顶部 `刷新`，更容易拿到真实的局部降级 UI。
- S07 本地 browser/report UAT 要让 frontend 和 backend 走同一个 loopback host（`localhost` 对 `localhost`，或 `127.0.0.1` 对 `127.0.0.1`）；如果混用，`/auth/dev-login` 写下的 host-only cookie 不会跟着 shared `/practice/{sessionId}/report` 请求走，页面会假性 401，看起来像 report 回归其实是本地 host 对不齐。
- S07 本地 runtime proof 不要偷懒走 presentation StepFun websocket 的 `type:"text"` shortcut：它能把会话跑到 completed，但当前不会给 user message 落 `transcript_metadata.page_number`，最后 `/practice/{sessionId}/report` 只会得到 `missing_page_metadata` degraded payload。要证明页级 evidence 真落库，继续用真实音频 chunk + `page_change` 路径。
- auto-mode 的最终 verification gate 可能直接从 repo root 执行 `venv/bin/alembic upgrade head` 与 `venv/bin/python -m pytest -c pyproject.toml tests/...`，不会替你 `cd backend`：这个仓库要保持 repo-root `alembic.ini`、repo-root `pyproject.toml` 和 `tests -> backend/tests` shim 可用，同时 `backend/src/common/config.py` 也要能在 repo-root 运行时补读 `backend/.env`，否则 slice 内部验证全绿、最终 gate 仍会因找不到 Alembic / pytest 配置而假性失败。
- `/support/runtime` 的 authoritative release-health line 必须基于 persisted session evidence + runtime diagnostics 做 blocking / warning 分类，而不是只看 `PracticeSession.status`：`status="scoring"`、optional enhancement 噪声、或 degraded PPT/report 会话如果脱离 canonical evidence 语义去判断，很容易被误报成“已完成且健康”或“核心回归”。
- 本地/auto-mode 如果并行跑多个 backend `pytest` 命令（尤其带默认 `pytest-cov` 配置），可能在 coverage combine 阶段互相删掉 `.coverage.*` 临时文件，表现成“所有测试都过了但命令最终 INTERNALERROR / FileNotFoundError 退出”；这类 backend suites 需要顺序跑，别把多个 pytest job 并发起来。
- `web/src/hooks/websocket/message-handlers.ts` 里的 `score_update` 去重不能只看 `overall_score + turn_count`：sales 实时链路会在同一轮内刷新 `dimension_scores`、`stage_name` 和 `suggestions`，如果前端把这些同轮细化更新吞掉，ScorePanel 会静默停留在旧阶段/旧建议。
- `cd web && npm test -- --run '...RightPanelContent.test.tsx'` 这类 Vitest 命令即使把一个不存在的测试文件路径混进去也可能继续返回 0，只报告实际匹配到的文件；跑 S02/S03 这类 slice gate 时要看输出里的 Test Files 列表，别把“命令绿了”误当成缺失的 targeted file 也被执行了。
- sales practice 页的新一轮用户 final transcript 到来时，前端 reducer 必须主动清掉上一轮的 `actionCard` / `fuzzyDetections`；否则即使后端 arbiter 已把同轮 coaching 节流住，旧提示也会在 UI 上跨 turn 残留，看起来像 pacing 仍然失效。
- StepFun sales realtime 的重连快照只该持久化最小 `feedback_pacing_state`（上一条 action 的 signature + turn）；如果 restore 直接重放 `_latest_action_card` 之类读侧诊断字段，会把同一轮动作卡 burst 回放成假回归。
- S03 classic 路由里如果 `action_card` 还停留在 suggestion/pass-flags 文案，先看 `RealtimeFeedbackArbiter.decide()` 调 `build_action_card(...)` 时有没有把 `stage_context` / `score_context` 继续传下去：`CapabilityProcessor` 已经会原样转发 raw `stage_data` 和 `score_payload`，真正容易漏的是 arbiter 的最后一跳；一旦补上，这个改动也会立刻影响现有 StepFun action-card 断言，属于 T03 预期 carry-forward，不是 classic 回归。
- StepFun sales parity要同时保住两条线：对外继续维护稳定的 `_latest_score_snapshot` / `score_update` snapshot 形状，对内另外传一份 raw `score_context_for_arbiter`（保留 `dimensions[*].delta/trend`）和 `_latest_stage_data`（保留 `guidance` / `key_actions` / `progress`）；不要为了 richer arbitration 去改公开 snapshot 结构。
- `SessionEvidenceService` 做 sales read-side alignment 时，不要直接拿“最后一条带 `score_snapshot` 的消息”；要倒序找“最后一条仍然能让 `resolve_sales_report_alignment(...)` 得到 `alignment_used=True` 的消息”。否则后续只落了 `overall_score` 的 partial snapshot 会把 report/replay/history/admin 重新打回 stale `effectiveness_snapshot`。
- `backend/tests/unit/test_replay_service.py` 这类 `MagicMock` session fixture 如果没显式设 `presentation_id = None`，`resolve_scenario_type(...)` 会把 truthy mock 属性误判成 presentation session，进而掉进 `presentation_review` 分支报 `[PRESENTATION_REVIEW_BUILD_FAILED:]`；写 sales replay 单测时把 `presentation_id` 和 `voice_policy_snapshot` 都显式置空更稳。
- M002 这类实时教练链路要同时守住“稳定公开 contract”和“更丰富的 backend-only 语义上下文”两条线：classic / StepFun / report / replay / admin 应共享 `resolve_sales_coaching_focus(...)` 与 `resolve_sales_report_alignment(...)` 这类内部 seam，对外尽量继续复用既有 `score_update` / report keys，而不是为了语义对齐扩展 public payload。
- 这个仓库做里程碑 close-out diff 验证时不要默认写 `main`：当前集成基线分支是 `001-ai-practice-system`。先探测可用 integration branch，再做 `git merge-base` / `git diff --stat`，否则会把不存在的 `main` 误报成“没有实现代码”。
- 写 GSD 计划里的 shell 校验命令时，凡是引用 `web/src/app/.../[id]/...`、`[sessionId]`、`(user)` 这类 Next.js 字面路径，都要给路径加引号；否则 `bash` 会把 `[]` 当 glob、把 `()` 当语法，任务/切片验证会在真正检查文件前先假性报错。
- 如果某次 auto-mode gate 已经用坏掉的校验命令生成了 `T##-VERIFY.json`，仅修正文档里的 `Verify:` 行还不够；要同步重写或重跑对应的 VERIFY artifact，否则后续 gate 仍可能继续复用那份陈旧失败记录。
- M003 的 accepted proof/UAT 边界里，`/api/v1/practice/sessions/{id}/knowledge-check` 与 `/api/v1/practice/sessions/{id}/report` 继续由 `backend/src/common/api/practice.py` 持有，但 replay 真实入口是 `/api/v1/sessions/{id}/replay`（`backend/src/common/conversation/api.py` → `backend/src/common/conversation/replay.py`）并共享 `SessionEvidenceService` 事实线；后续计划、测试和 live UAT 不要把 replay 错绑回 `practice.py`。
- M003/S02 的 Persona 压力模型里，`normalize_persona_policy(...)` 对“原始 legacy 平铺字段”会给 `customer_pressure.source="legacy_sales_focus_extensions"`，但 create/update 路径会把派生后的 nested `customer_pressure` 持久化回 `persona_policy`，所以下一次 GET 会看到 `source="explicit"`；只有数据库里仍缺 nested 模型的老行才该在 `policy-health` 里命中 `pressure_model_legacy_only`。
- M003/S02 之后要检查某个 sales session 冻结了哪套 Persona 压力合约，优先看 `PracticeSession.voice_policy_snapshot.customer_pressure` 和 `voice_policy_snapshot.source.customer_pressure_source`，不要再从 `instructions` 文本反推；编译器现在把 nested `customer_pressure` 显式镜像到 runtime snapshot 顶层。
- repo-root gate 现在能直接跑 `venv/bin/python -m pytest -c pyproject.toml tests/integration/test_knowledge_flow.py`，因为 `/api/v1/auth/dev-login` / `get_dev_user()` 已改成用带默认值的 development env 解析；后续别再写裸 `os.getenv("ENVIRONMENT") ==/!= "development"`，否则 repo root 会重新掉回 403 → 401 的假回归。
- Sales realtime 这条链里，新增的 per-turn 结构化事实如果只塞进 `analysis_data`，会先被 `stepfun_message_helpers.normalize_message_persistence_payload(...)`、再被 `MessageStorageService.save_message/update_analysis(...)` 两层白名单过滤掉；要复用现有 evidence store，需同时扩展这两层，或把事实嵌到 `transcript_metadata` 这种现成 pass-through 容器里。
- M003/S03 的 unresolved objection ledger 只有在“最新一条 persisted `transcript_metadata.objection_ledger` 仍是 `closure_state="open"`”时，才应该覆盖 report/replay/admin/history 的 `main_issue` / `next_goal`；一旦最新 ledger 变成 `gap_acknowledged` 或 `evidence_provided`，`SessionEvidenceService` 应回退到通用 sales alignment，而不是继续强压旧异议。
- M003/S03 的 learner runtime 断线重连 / final transcript reset 要清掉 `actionCard` 与 `fuzzyDetections`，但保留 `scores.suggestions`；这条 suggestion 现在承担“当前仍卡住的证明”提示，如果一起清空，UI 会看起来像 unresolved objection 被忘掉了。
- M003/S04 的 claim-truth contract 要挂在现有 `effectiveness_snapshot.claim_truth` 上，并且 `SessionEvidenceService` 读取最新 objection ledger 时不能只看 `closure_state="open"`：`gap_acknowledged` / `evidence_provided` 虽然不该继续覆盖 `main_issue` / `next_goal`，但仍要参与 `unsupported_claim` / `evidence_verified` 判定。
- `StepFunRealtimeHandler` 的 live `claim_truth` 目前走独立 `_latest_claim_truth` 状态并通过 `/api/v1/practice/sessions/{id}/knowledge-check` 先读 live handler、再回退到 completed-session projection；不要把它直接塞进 persisted `score_snapshot` 指望 diagnostics/reconnect 读取，因为 `stepfun_message_helpers.normalize_score_snapshot(...)` 会保留稳定公开 shape 并丢掉额外字段。
- `cd web && npx tsc --noEmit` 目前会先报 `src/app/admin/knowledge/[id]/page.tsx` 里缺失 `api.reprocessKnowledgeDocument` 的老问题；做 M003/S04 这类 report/replay 前端改动时，如果 focused Vitest 已绿而 typecheck 只剩这一条，先把它记作无关阻塞，别误判成 claim-truth UI 回归。
- 本地做 M003/S05 的 browser live proof 时，如果用浏览器重放合成语音去驱动真实 `/practice/{sessionId}` 录音链路，`navigator.mediaDevices.getUserMedia` 返回的音频不要立刻播放；至少延后约 1s 再开始送样本，否则 StepFun 容易先落 `timeout_proceeded_without_transcription_completion`，前端只会看到 `当前会话已开启知识库强制模式，但本轮语音转写尚未完成...` 这种假降级。
- M003/S05 当前 live same-session proof 里，canonical `/practice/{id}/report` 可以在 `status="scoring"` 时继续基于 persisted evidence projection 正常产出，但 `/sessions/{id}/replay` 与 `/sessions/{id}/highlights` 仍会被 completed gate 拦住；如果 backend 日志同时出现 `report_generation_failed [NO_STAGE_RESULTS]` / `no_scoring_context_available`，把它记成“report 可读、replay 阻塞”的真实运行时降级，而不是把 replay 401/400 误判成前端跳转问题。
- 里程碑 close-out 前先把 roadmap 里的 slice ID 和 `.gsd/milestones/<MID>/slices/` 目录、`S##-PLAN.md` / `S##-SUMMARY.md` / `S##-UAT.md` 做一轮文件系统对账；运行上下文里写着“all slices are done”并不能替代这个检查。
- 如果 roadmap 想把实现严格限制在少数业务代码目录，先把共享 contract seam（例如 `backend/src/common/effectiveness/*`、`web/src/lib/api/*`）写进边界；否则实现虽然沿着真实业务链路落地，close-out 仍会因为字面目录约束而被判成未完全达标。
- `tests/integration/test_sales_realtime_reconnect_flow.py` 如果还断言 persisted StepFun reconnect snapshot 带 `latest_action_card`，那是过期基线；当前 sales reconnect 合同只保留最小 `feedback_pacing_state` 与必要 runtime context，close-out proof 应改看 `test_stepfun_realtime_persistence.py` 这条事实线。
- 改 `/api/v1/sessions/{id}/replay` / `highlights` 的 payload 时，别只改 `backend/src/common/conversation/replay.py`：FastAPI 的 `response_model` 会按 `backend/src/common/conversation/schemas.py` 过滤字段，`stage_name`、`context`、`learning_evidence` 这类新增键如果不进 schema，服务里明明构出来了也到不了前端。
- Replay/highlight rich-evidence UI 现在会在 session-level conclusion 卡片和 per-turn evidence 卡片里重复展示 `issue_type` / `goal_type` / stage label；前端 focused tests 应该用 `getAllByText` 或 textContent matcher，别再假设这些标签只出现一次。
- 本地做 M004/S02 这类 report/replay browser proof 时，别默认复用机器上已有的 `:3000` 页面：这台环境里 `http://127.0.0.1:3000` 可能是别的应用，真正的 repo 前端需要单独起在 `:3445`（或先核对标题/路由能否命中 `web/src/app/(user)/practice/[sessionId]/report/page.tsx`），否则你会把 чужой app 的 401/404 误判成当前仓库回归。
- M004/S03 之后如果 report/replay 合同测试还在直接断言 `main_issue` / `next_goal` 整体相等，先确认 replay 侧是不是已经合法附带了 `replay_anchor`：completed-session report 仍保留基础 issue/goal 事实，而 replay 会在同一对象上加定位元数据；这类断言应剥掉 `replay_anchor` 或单独校验它，别把合法深链扩展误报成 evidence drift。
- `PresentationReportService._load_report_context()` 现在会额外查询一轮 `ForbiddenWord`；写 presentation report 单测时如果还在手工堆 `db.execute.side_effect`，要补上第 6 个结果（通常是 `_ScalarsResult([])`），否则测试会掉进空白的 `[PRESENTATION_REVIEW_BUILD_FAILED:]`，看起来像逻辑坏了，其实只是 fixture 少了一次查询。
- 这台环境里的全局 Volta `npm` 可能坏在 `@npmcli/config/lib/definitions.js` 缺失，直接跑 `npm test` / `npm run dev` 会先崩在 wrapper；web 验证要先改用 `cd web && pnpm dlx npm@11.6.1 ...`，必要时再用 `cd web && pnpm dlx npm@11.6.1 ci` 修复 `node_modules` 后再判断是不是前端回归。
- auto-mode 的 web verification gate 可能直接从 repo root 跑 `npm test -- --run 'src/app/.../page.test.tsx'`，而不是先 `cd web`；这个仓库要保留 repo-root `package.json` 的 `test` shim（委托 `pnpm --dir web exec vitest run`），否则 gate 会在找不到根目录 `package.json` 前就假性失败。
- `backend/tests/conftest.py` 里的 `main` / `User` 最好继续按 fixture 内 lazy import；如果在文件顶层就 `from main import app`，focused unit suites 也会被 `chromadb` / `langchain` / runtime wiring 一起拖进 pytest collection，环境稍有漂移就会把与 FastAPI app 无关的纯 service 单测误打成失败。
- M004/S05 的 live PPT learner-loop proof 里，当前 shipped UI 只在 `/history` 行上暴露 PPT 的 sibling `报告` / `回放` 路由；`/practice/{sessionId}/report` 目前有 `按目标再练一轮`，但没有 sales-style `定位问题片段` CTA。做 browser/UAT 时别把“report 里找不到 replay 按钮”误判成回归，应按当前 shared history route family 分开验证 report 与 replay。
- repo-root `venv/bin/python -m pytest -c pyproject.toml ...` 目前可能在 pytest plugin autoload 阶段先因为 `langsmith` → `urllib3.response` 缺失而崩掉，哪怕后端 slice suites 本身是绿的；做 M004/S05 这类 focused backend gate 时，优先用计划里的 `cd backend && venv/bin/python -m pytest -c pyproject.toml ...`，并把 repo-root venv 当成单独的环境噪声面，别误判成 replay/evidence 回归。
- M004 之后如果 learner-facing 文案需要同时出现在 report / replay / history / highlights，上游仍应从 `web/src/lib/session-evidence.ts` 这类共享 helper 派生 issue/goal cue，而不是在单页里各自翻译 `main_issue` / `next_goal`；否则学习词汇会很快分叉。
- M004 的定向再练 focus 不能只靠 report/replay query state 传递：必须经 `POST /api/v1/practice/sessions` 写入 `voice_policy_snapshot.focus_intent`，再由 `runtime_descriptor.focus_intent` 投影到 `/practice/{sessionId}`；这样刷新和 reconnect 才不会丢 learning intent。
- M004 的 replay deep-link 合同里，`resolved` / `degraded` / `missing` anchor 状态都要在 replay 页面保留可见 banner；如果改成静默 fallback，live UAT 与 learner 自查都会失去“当前看到的是精确命中还是近似定位”的诊断面。
