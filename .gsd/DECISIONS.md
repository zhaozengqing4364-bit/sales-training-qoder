# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M001 | scope | 首发环境优先级 | 先把桌面端稳定性做满 | 用户明确要求第一版先保证桌面端真实可用，不把移动端 / 企业微信首发绑进 M001 | Yes — 当桌面端闭环已证明并计划扩展使用环境时 | agent |
| D002 | M001 | data | 训练材料事实源 | 公司标准 PPT 与产品资料由后台知识库 / 资产管理维护，不写死在 prompt | 训练内容需要持续更新；业务侧必须能自己上传、更新、替换，并在下一次训练生效 | No | agent |
| D003 | M001 | scope | PPT 第一版教练方式 | 第一版先做完整讲完后的统一复盘，实时打断后置 | 用户把会后统一总结定义为硬门槛，把实时纠偏定义为高价值增强项 | Yes — 当实时纠偏在技术与体验上都被证明成立时 | agent |
| D004 | M001 | pattern | 主管第一版使用方式 | 先提供单次报告 + 连续变化供主管线下辅导，不在系统内做任务派发 | 用户明确接受第一版主管先看报告、线下带教；避免把组织化管理复杂度提前注入主链路 | Yes — 当单次报告和趋势视图已可信后 | agent |
| D005 | M001 | scope | 首发集成边界 | 第一版先独立使用，不新增外部系统集成 | 当前最重要的是证明训练闭环成立；过早接 SSO / CRM / 外部文档系统会放大验收复杂度 | Yes — 当独立系统闭环已稳定且明确需要外部接入时 | agent |
| D006 | M001 | arch | 规划顺序原则 | 先证明训练闭环成立，再建设实时教练、知识真实性、复盘增强与规模化治理 | 现有仓库已有大量骨架，真正短板在闭环可靠性与结果可信度，而不是页面数量 | No | agent |
| D007 | M001/S01 | runtime | 销售训练生命周期权威写入口 | 以 `SessionLifecycleService` + `POST /practice/sessions/{id}/lifecycle` 作为销售训练生命周期唯一权威写入口；保留旧 `DELETE /practice/sessions/{id}` 时必须委托同一结束实现 | 当前结束、副作用与 live sync 在多个入口分叉，必须先收口才能稳定多轮与报告跳转 | Yes — 若未来确有外部兼容需求，可保留多入口但仍需共享同一实现 | agent |
| D008 | M001/S01 | websocket | 销售重连恢复边界 | 销售 StepFun runtime 只恢复最小可继续会话状态（`session_status` / `ai_state` / `turn_count` / 最近活动）并复用 `SessionStateService` + `reconnected` 协议，不序列化上游 StepFun 连接内部状态 | 降低恢复复杂度，同时让 sales 与 presentation 共用同一恢复协议和前端消费面 | Yes — 若后续证明必须恢复更多 runtime 字段，再在同一协议上扩展 | agent |
| D009 | M001/S01/T01 | lifecycle | 会话终态单写入口 | `POST /practice/sessions/{id}/lifecycle` 的 `end` 与旧 `DELETE /practice/sessions/{id}` 统一委托 `backend/src/common/api/practice.py` 内同一终态 helper，再由各入口只决定返回 lifecycle payload 还是 report payload | 终态副作用（sales summary、presentation score flush、report trigger、live handler sync、terminal close）必须共享实现，测试和日志才能收敛到一个排查面 | Yes — 若未来新增兼容入口，也必须继续复用同一 helper | agent |
| D010 | M001/S01/T02 | websocket | Sales StepFun 重连快照边界 | Sales StepFun 只持久化 `session_status` / `ai_state` / `turn_count` 与继续训练所需的最小 runtime 字段（`current_request_id`、最近 stage、最新 scoring/action card），重连时主动清空活跃 response / tool call / grounding 缓冲等不可恢复上游状态 | StepFun 上游连接和流式中间态不可安全序列化；把恢复边界收窄到“可继续训练”而不是“继续同一流”，才能保证断线重连与终态清理稳定可测 | Yes — 若后续证明需要恢复更多字段，必须继续沿用最小可恢复 + 显式清理不可恢复态的模式 | agent |
| D011 | M001/S01/T03 | frontend-lifecycle | 训练页前端生命周期权威面 | 训练页只把服务端 `status` / `reconnected` / `session_ended` 作为 `sessionStatus` / `aiState` / 报告跳转的权威来源；本地只做音频清理，结束失败必须留在训练页暴露重试/重连 | 本地乐观改状态会把 pause/resume/end 漂移伪装成报告页问题；把跳转与状态面绑定到服务端终态，失败才能对用户和后续排障可见 | Yes — 若未来扩展新的服务端 lifecycle 事件，客户端可以消费新合约，但仍不能回到本地猜测终态 | agent |
| D012 | M001/S02 | arch | 训练会话事实基线 | 以 `PracticeSession` + `ConversationMessage` 的共享 session evidence projection 作为 report / replay / history / trends 的事实源，`ComprehensiveReport` 退为可选增强视图 / 缓存 | 当前分数与证据在 quick report、replay、history、trends、comprehensive report 之间漂移；先统一读模型，才能证明“报告事实源一致”且不依赖 staged evaluation 是否存在 | Yes — 若后续迁移到专用 evidence 表，可保留同一 projection contract 并让底层存储替换 | agent |
| D013 | M001/S02/T01 | data | StepFun 销售证据写入规范 | 逐轮与终态写入一律规范化到 `score_snapshot.overall_score`；历史 `overall` 仅做兼容输入；零轮/薄证据会话写入显式 `evaluable=false` + `not_evaluable_reason=INSUFFICIENT_TURN_DATA`，不再把 summary 失败当作终态语义 | 下游 report / replay / history / trends 只有在写入层先收稳键形与不可评估事实，才能共享同一事实基线并区分“证据不足”与“summary 真异常” | Yes — 若未来迁移到专用 evidence 表或扩展不可评估原因枚举，仍应保持同一显式终态语义 | agent |
| D014 | M001/S02/T02 | reader-contract | 共享 session evidence projection 读合同 | 会话级 overall 一律按 `logic/accuracy/completeness` 三项平均输出；若顶层分数缺失，则从最新 message `score_snapshot` 回填三项并同步暴露 `evidence_completeness` / `legacy_score_key_used` 诊断；逐轮 snapshot 只对外暴露 canonical `overall_score` 读形 | report / replay / history 必须共用同一 top-line 公式和 legacy fallback，不然即便同源读表仍会继续各算各的；同时把 completeness 与 legacy fallback 暴露出来，后续 agent 才能快速判断是旧数据还是 consumer 绕过 projection | Yes — 若后续引入专用 evidence 表或改变顶层 overall 公式，需要以同一 projection contract 版本化迁移，而不是让 consumer 各自重算 | agent |
| D015 | M001/S02/T03 | analytics-contract | 历史/统计/趋势聚合口径 | `HistoryService` 负责先把 completed session 投影成统一 history summary，再由 statistics/trends 只聚合 `evaluable=true` 的 completed sessions；history list 仍保留非 completed 行，但仅对 completed 行暴露 projection evidence 字段 | 主管视图既要和 report/replay 用同一事实基线，又不能让 aborted / non-evaluable 会话把平均分和趋势线直接拉歪；把 completed/evaluable gating 固化在同一 service，后续 consumer 和前端才能稳定共享语义 | Yes — 若后续前端需要展示 non-evaluable 趋势点，可在同一 projection contract 上扩展显式点类型，但不能回到另一套 score 公式 | agent |
| D016 | M001/S02/T04 | frontend-reader-contract | Web 报告/回放/历史消费边界 | report / replay / history 页面只把统一 evidence contract 当作 top-line score、evaluable、not_evaluable_reason、stage_summary、evidence_completeness 的事实源；`ComprehensiveReport` 与 highlights 仅做可缺失的增强内容 | 后端读模型已经统一后，前端若继续并行拉多接口再本地补分，用户仍会看到“假一致”的多套事实；把增强内容降级为可选层，才能让浏览器消费面和后端基线真正对齐 | Yes — 若后续引入版本化前端 evidence contract，三页可以一起迁移，但仍必须继续遵守“一条事实线 + 可选增强层” | agent |
| D017 | M001/S03/T02 | reporting | How supervisor-facing admin surfaces should present and open single-session results | Project completed admin sessions through SessionEvidenceService-backed preview fields and deep-link both admin user detail and manager-lite not_passed entries to the shared /practice/{sessionId}/report page | This keeps supervisors and learners on the same evidence baseline, removes legacy weighted-score drift from admin previews, and avoids creating a separate supervisor-only report surface that could diverge from the canonical session report. | Yes | human |
| D018 | M001/S03/T02 | architecture | How supervisor-facing single-session previews and drill-in CTAs should be sourced | Reuse SessionEvidenceService projection for completed admin session previews and deep-link all supervisor entry points to /practice/{sessionId}/report | This keeps admin users detail, manager-lite not_passed cards, and the learner report page on one authoritative evidence line instead of mixing legacy 0.4/0.3/0.3 summaries with a separate supervisor-specific view. | Yes | agent |
| D019 | M001/S04 | presentation-materials | How standard PPT replacement should work without mutating in-flight presentation sessions | Replace standard PPTs in place on a stable `presentation_id`, increment `version_number`, rebuild page-scoped coaching data, and block replacement while non-terminal sessions reference that presentation | Current uploads create a new `presentation_id`, which breaks the promise that the next training automatically uses the updated standard PPT. Keeping a stable identity preserves existing user entrypoints, while blocking active-session replacement avoids mid-session context drift in a runtime that reads `Page` rows live. | Yes | agent |
| D020 | M001/S04 | knowledge-materials | What authority should prove that updated sales knowledge materials took effect | Use the persona-centered chain `persona_policy -> VoiceRuntimePolicyService.resolve_effective_policy(...) -> PracticeSession.voice_policy_snapshot -> /practice/sessions/{id}/knowledge-check` as the only sales-material effectiveness proof line | This is already the live runtime authority for knowledge bindings and retrieval diagnostics. Reusing it prevents agent-level KB drift and ensures 'next new session uses updated material' is proven by the same snapshot and report surfaces that the product already trusts. | Yes | agent |
| D021 | M001/S04/T02 | architecture | Which presentation API surface should the standard-PPT replace flow use | Implement replace/version/blocker behavior on the live /api/v1/presentations surface and keep the existing admin pages consuming that contract | The shipped admin presentation detail page and user training selector already depend on api.presentations, while the legacy /admin/presentations backend is schema-stale and not the active runtime path. Extending the live surface kept the change small, preserved the stable presentation_id flow end-to-end, and avoided wiring new UI onto a dead contract. | Yes | agent |
| D022 | M001/S04 | knowledge-diagnostics | How knowledge material diagnostics should distinguish infrastructure failure from normal retrieval outcomes | Return 503 for admin knowledge search backend outages and expose explicit knowledge-check statuses for search_failed and no_knowledge_base instead of collapsing them into miss/not_found semantics. | Admins need to self-diagnose whether a document update is merely not matching, not ready yet, or blocked by the retrieval/embedding infrastructure. Distinguishing these states keeps the admin knowledge page and session report diagnostics trustworthy and makes 'next new session uses updated material' debuggable without shell access. | Yes | agent |
| D023 | M001/S05 planning | architecture | Which layer owns S05 销售价值表达 / 异议处理语义，以及哪些消费面需要同步调整 | 把 S05 实现为 write-layer sales rubric + persona instruction contract slice：在 StepFun realtime scoring/effectiveness 写入层和 persona policy 编译层落地新语义，只做最小 live/report consumer 对齐，保持 SessionEvidenceService 与 report/replay/history 继续作为只读事实消费面 | S02 已经把 report/replay/history/trends 收口到统一 evidence projection，S04 已把材料绑定冻结到 voice_policy_snapshot/knowledge-check。若在 read-side 或 comprehensive-report enhancement 再发明第二套 sales scorer，会重新引入事实漂移。把语义改动压在 active StepFun write path，并只同步 ScorePanel/report 标签，既能让用户真的看到新训练目标，又不需要重做统一事实线。 | Yes | agent |
| D024 | M001/S05/T01 | architecture | How S05 should change sales scoring semantics without breaking the unified evidence contract | Preserve existing top-level report/evidence field names and legacy pass_flag subkeys, but remap their meaning to sales value-articulation, evidence-benefit, and objection-progression semantics via shared write-layer helpers | Report/replay/history/admin consumers already depend on SessionReport.logic_score/accuracy_score/completeness_score and pass_flags.* keys. Renaming keys in S05 would create unnecessary read-side churn and cross-slice regressions. Keeping the contract stable while moving the semantics in realtime scoring, StepFun flush, and terminal fallback lets the product switch to sales-specific facts without reintroducing projection drift. | Yes | agent |
| D025 | M001/S05/T02 | architecture | How sales-focused persona prompts should steer knowledge retrieval without creating a second materials path | Normalize sales-focus persona extensions centrally in persona_policy, compile them into the existing voice instruction contract, and widen price/competitor/proof retrieval through stepfun_knowledge_helpers query tuning. | This keeps the S04 authority line intact (`persona_policy -> resolve_effective_policy -> voice_policy_snapshot`) while making sales objection prompts materially stronger. Reusing the existing helper avoids a parallel retrieval contract and keeps knowledge-check/runtime diagnostics on one inspection surface. | Yes | agent |
| D026 | M001/S06 planning | analytics-contract | How S06 continuous-change aggregation and admin score summaries should be sourced | Implement S06 as a projection-backed supervisor snapshot in `HistoryService`, and have `GET /api/v1/admin/users/{id}/progress` plus the score-bearing fields in `GET /api/v1/admin/users/{id}/stats` read that snapshot instead of route-level 0.4/0.3/0.3 SQL averages. | S02/S03 already proved that report, replay, history, and completed-session previews must share one evidence line. Leaving `/progress` or `/stats` on raw weighted SQL would keep mixed truths on the same admin page. Reusing `HistoryService` and the existing `SessionEvidenceService` projection also lets S06 aggregate repeated `main_issue.issue_type` / `next_goal.goal_type` buckets and explicit not-evaluable counts without inventing a second supervisor-only scorer. | Yes | agent |
| D027 | M001/S06/T01 | architecture | How admin user detail score-bearing stats and supervisor progress should aggregate completed sessions | Route both `/api/v1/admin/users/{id}/stats` score fields and `/api/v1/admin/users/{id}/progress` through new HistoryService projection-backed helpers instead of route-local weighted SQL | Admin user detail already mixes completed-session previews built from SessionEvidenceService with legacy route-local score math, which produced drifting truths on the same page. Centralizing the score summary and supervisor snapshot in HistoryService keeps stats/progress aligned with the shared evidence projection, exposes not-evaluable semantics explicitly, and lets granularity/repeated-issue logging live in one place. | Yes | agent |
| D028 | M001/S07 planning | architecture | How S07 should deliver PPT post-session review without breaking the unified evidence baseline | Keep `/api/v1/practice/sessions/{id}/report` as the canonical single-session report entrypoint, add `scenario_type` plus a presentation-specific `presentation_review` payload sourced from `PresentationReportService`, and make the shared web report page branch on `scenario_type` instead of reusing sales-only pass flags or knowledge-check surfaces. | S02 already established one authoritative evidence line for report/replay/history/trends. Letting PPT core facts live only in `/evaluation/.../report` or remapping them into sales-shaped keys would recreate truth drift. Reusing `PresentationReportService` preserves the existing deterministic PPT scorer, while a scenario-aware shared contract lets the user-facing `/practice/{id}/report` remain the only canonical entrypoint for both sales and presentation sessions. | Yes | agent |
| D029 | M001-S07-T01 | presentation-reporting | What component owns normalized PPT post-session review facts and how legacy runtime page evidence is persisted | Use PresentationReportService as the single builder for normalized presentation review payloads, and pass legacy current_page through transcript_metadata.page_number on the existing MessageStorageService.update_analysis path | Shared report and enhanced report need one presentation-review authority to avoid diverging heuristics, while reusing the existing message-analysis update path keeps legacy and StepFun runtimes on the same persisted page-evidence fact line with explicit degraded diagnostics when historical page metadata is missing. | Yes | agent |
| D030 | M001/S08 planning | architecture | Which product surface and truth source should own M001/S08 release health and final launch proof | Extend `/support/runtime` with an evidence-backed release-health read model sourced from persisted session evidence, shared runtime diagnostics, and canonical report semantics, then use that surface plus `S08-UAT.md` as the M001 launch gate instead of relying on raw `SystemLog` counts or the generic `release_verification` subsystem. | The current support/runtime surface can report false-green health because it counts `status="scoring"` as completed and misses most milestone-critical anomalies. Reusing `SessionEvidenceService`, knowledge-check semantics, and presentation degraded reasons keeps S08 on the same evidence line as report, trends, and PPT review while giving support/admin a small, truthful, in-product release surface. This is the smallest safe change that closes observability and launch proof without creating a second verification console. | Yes | agent |
| D031 | M001/S08/T01 | observability | How support/runtime should classify release health once the evidence-backed reader replaces the old SystemLog overview | Use persisted session evidence plus shared runtime diagnostics as the primary release-health truth line; treat `stuck_scoring`, `projection_failed`, `not_evaluable_completed`, `knowledge_search_failed`, and `kb_lock_blocked_*` as blocking anomalies, while `presentation_degraded_missing_page_metadata`, `optional_report_failed`, `upstream_unstable`, and raw SystemLog rows remain warning-level surfaces. | The release surface must stop reporting false-green health when sessions are stuck in scoring or canonical evidence/report semantics are broken. Blocking severity is reserved for anomalies that invalidate the core training/report truth line, while degraded-but-readable PPT reports, optional enhanced-report failures, transient upstream instability, and legacy logs stay visible without outweighing the canonical evidence signal. | Yes | agent |
| D032 | M002/S01 planning | architecture | S01 realtime sales-contract scope across runtime modes | Treat both StepFun and user-selectable classic voice mode as supported S01 runtime surfaces, align both to the same five-dimension practice-page sales contract, and keep the report contract as the existing three sales rollups. | The agent launch page still exposes classic mode, so a StepFun-only fix would leave a visible drift path on the training page. Aligning classic mode through the shared sales effectiveness helper closes that gap without reopening the M001 report/evidence baseline, which intentionally compresses realtime dimensions into three read-side rollups. | Yes | agent |
| D033 | M002/S01/T01 | realtime-scoring | Which helper is authoritative for classic sales realtime action-card semantics | Use build_sales_effectiveness_metrics(...) + evaluate_pass_flags(...) in CapabilityProcessor instead of reconstructing pass flags from communication/structure heuristics | Classic voice mode is still a user-reachable runtime surface. Reusing the shared sales effectiveness helper keeps its action-card semantics aligned with StepFun and the existing three-rollup report contract without changing websocket field names or read-side consumers. | Yes | agent |
| D034 | M002/S01/T02 | frontend-runtime | What defines idempotence for practice-page sales score_update payloads | Treat the full ScoreUpdate payload (overall_score, turn_count, stage_name, suggestions, dimension_scores, session_id) as the frontend idempotence boundary instead of deduping only on overall_score + turn_count. | Sales realtime feedback can refine stage guidance and dimension-level coaching within the same turn. If the practice page drops those same-turn refreshes, the ScorePanel silently shows stale sales advice even though the backend contract already advanced. | Yes | agent |
| D035 | M002/S02 planning | realtime-feedback | Where S02 should enforce prompt pacing, single-turn action priority, and stale-hint clearing | Use one shared realtime feedback arbitration helper after capability execution for both classic and StepFun runtime paths, keep capability-local cooldowns intact, persist only the minimum pacing state needed across reconnects, and make the frontend treat `action_card` as the primary coaching surface while clearing turn-bound hints on new final transcripts. | The current problem is cross-channel competition after fuzzy/stage/score signals are generated, not the low-level capability math itself. A shared post-capability arbiter keeps S01's canonical sales payload stable across both runtime modes, avoids duplicating priority logic in two handlers, and gives S02 a single seam for cooldowns, dedupe, and reconnect-safe pacing. Frontend precedence and transcript-driven clearing then prevent stale or duplicate coaching from lingering into later turns without inventing a second heuristic policy on the client. | Yes | agent |
| D036 | M002/S02/T01 | architecture | How realtime coaching signals are arbitrated across backend runtimes | Introduce a shared realtime-feedback arbiter that preserves fuzzy/stage/score payloads as context, prefers scoring guidance over low-severity fuzzy detections for the primary action card, and suppresses duplicate action cards only when the same signature repeats within the same turn. | S02 needs one backend seam before classic and StepFun can converge on the same pacing rules. The arbiter removes competing coaching from the primary surface without breaking S01 payload fields or FuzzyDetectionCapability's local cooldown semantics, and its minimal signature/turn state is small enough to persist for reconnect-safe StepFun reuse in T02. | Yes | agent |
| D037 | M002 S02 T02 | realtime coaching | What reconnect-safe StepFun metadata should be persisted alongside read-side realtime diagnostics | Persist a nested `feedback_pacing_state` derived from `RealtimeFeedbackPacingState` while keeping `_latest_score_snapshot` and `_latest_action_card` as read-side diagnostics | The arbiter only needs last action signature plus turn number to suppress same-turn replay bursts after restore. Persisting that minimal state keeps StepFun aligned with classic pacing without bloating the reconnect snapshot or replacing the existing score/action diagnostics that later slices still inspect. | Yes | agent |
| D038 | M002/S03 planning | realtime-coaching | How S03 should unify next-turn coaching across stage, score, and action-card surfaces | Introduce one shared backend coaching-focus resolver in `common.effectiveness`, keep the websocket `action_card` payload stable, feed rich stage/score context from both classic and StepFun runtimes into the arbiter, and defer replay/report schema changes to later slices. | The current drift comes from `build_action_card(...)` choosing next-turn rules from pass flags alone while StepFun strips the richer stage and score context already available on the classic path. Centralizing the rule in backend shared logic keeps the practice page as a renderer, reuses S02's single-arbiter architecture, avoids reopening S01/S02 payload contracts, and gives S04 a stable seam for later report alignment without forcing an early persistence migration. | Yes | agent |
| D039 | M002/S03/T01 | realtime-coaching | How T01 should introduce the shared stage-aware coaching-focus rule without breaking current runtime callers | Expose resolve_sales_coaching_focus in common.effectiveness and have build_action_card switch to that helper only when rich stage/score context is supplied; keep the legacy fuzzy/suggestion + pass-flags fallback for unwired callers until T02/T03 pass full context. | This isolates S03/T01 to the shared backend rule seam, keeps the websocket action_card shape unchanged, lets the new focused coaching contract land with tests immediately, and avoids forcing classic/StepFun runtime wiring changes before the later tasks that explicitly own them. | Yes | agent |
| D040 | M002/S04 planning | architecture | How S04 should align realtime coaching with report/replay conclusions | Use a shared read-side alignment helper in `common.effectiveness`, apply it as a projection-only override in `SessionEvidenceService` for completed sales sessions, keep the public report/replay/websocket contracts stable, and make replay render the existing `main_issue` / `next_goal` directly instead of introducing new persistence or client-side heuristics. | S03 already established one authoritative realtime coaching seam in `resolve_sales_coaching_focus(...)`, while the remaining drift lives in read-side consumers that reuse stale `effectiveness_snapshot` values. A projection-only override closes report/replay/history/admin drift with the smallest blast radius, avoids a DB migration, preserves the S01/S03 websocket contract, and lets replay visibly confirm the same conclusion family the report shows. | Yes | agent |
| D041 | M002/S04/T01 | effectiveness | How the shared sales report-alignment seam should expose diagnosable state without changing the public report contract | Add `resolve_sales_report_alignment(...)` in `common.effectiveness` that returns existing `main_issue` / `next_goal` payloads plus internal `alignment_used`, `stage_key`, `focus_type`, and `fallback_reason` diagnostics, and reuse the same report vocabulary map for both direct sales evaluation and the new alignment helper. | S04 needs one read-side seam that later projection/logging tasks can inspect for override/fallback behavior without adding new API keys, websocket fields, or persistence columns. Returning internal diagnostics now keeps downstream observability simple while ensuring the aligned helper and the existing sales evaluator cannot drift onto different issue/goal vocabularies. | Yes | agent |
| D042 | M002/S04/T02 | reader-contract | How SessionEvidenceService should choose persisted sales evidence when newer messages only carry partial snapshots | Projection-side sales alignment should scan backward for the latest message whose persisted stage+score pair can actually align (`alignment_used=True`) and use that message for `main_issue` / `next_goal` overrides; if no such message exists, keep the existing snapshot and log the fallback reason. | Completed sales sessions can contain newer messages that only persist `overall_score` while the last full-dimension sales evidence lives on an earlier turn. Using the newest raw snapshot would silently disable alignment and reintroduce report/replay drift even though persisted aligned evidence still exists. Selecting the latest alignable message preserves the shared sales-first conclusion baseline while keeping insufficient-evidence fallbacks explicit and unchanged. | Yes | agent |
| D043 | 2026-03-25 hard steer after M003-M005 replanning | roadmap | What planning boundary should govern future roadmap and slice work after the 2026-03-25 hard steer | Anchor milestone and slice planning to real business-code entrypoints, core modules, and runnable product directories; do not elevate environment/tooling artifacts into milestones unless the explicit goal is environment migration; if the real code entry chain cannot be located, stop and do inventory/spike before execution | The user explicitly overrode the prior roadmap direction because it was drifting away from the actual user value chain. Future plans must start from existing admin/user entrypoints and authority modules, keep acceptance tied to those runtime surfaces, and avoid burning milestone scope on Conda/.env/lockfile-style work that does not directly move the product loop. | Yes | human |
| D044 | M005 S01 planning after hard steer | roadmap | What sequencing should govern M005 planning on the current admin business chain | Start M005 by replacing legacy weighted admin analytics semantics with the current projection/evidence line on existing admin routes before adding richer manager workflow or governance features | `backend/src/common/analytics/admin_analytics_service.py` still shows legacy weighted-score logic, while admin user detail and learner/report surfaces already rely on projection-backed semantics. If new manager workflow or governance work lands before that truth line is corrected, the organization layer will amplify bad signals instead of helping users. | Yes | agent |
| D045 | M005 S01 planning after hard steer | roadmap | What sequencing should govern M005 planning on the current admin business chain | Start M005 by replacing legacy weighted admin analytics semantics with the current projection/evidence line on existing admin routes before adding richer manager workflow or governance features | `backend/src/common/analytics/admin_analytics_service.py` still shows legacy weighted-score logic, while admin user detail and learner/report surfaces already rely on projection-backed semantics. If new manager workflow or governance work lands before that truth line is corrected, the organization layer will amplify bad signals instead of helping users. | Yes | agent |
| D046 | M004 S01 planning after hard steer (appended after decision-id collision) | roadmap | Which product surfaces should own M004 learning-loop work after the 2026-03-25 hard steer | Build M004 learning-loop enhancements on the existing report, replay, history, and highlight entrypoints instead of creating a new learning center or a second evaluator | The hard steer requires future planning to stay on real business-code entrypoints and user value chains. The repo already has authoritative report/replay/highlight routes and a unified session-evidence line; deepening those existing surfaces compounds user value without splitting facts or adding a parallel learning product. | Yes | agent |
| D047 | M003 S01 planning after hard steer | roadmap | What sequencing should govern M003 planning on the current realism chain | Start M003 by inventorying the real admin Persona/knowledge entry chain and locking one objection-query evidence vocabulary on current runtime/report surfaces before any richer Persona model or retrieval work proceeds | The hard steer requires planning to begin from real business entrypoints and to stop if the code path cannot be located. In this repo, the highest-risk failure mode is assuming admin config already affects runtime in a provable way. Locking the actual config → runtime → report chain first prevents later M003 slices from drifting into prompt theory or tooling noise. | Yes | agent |
| D048 | M003/S01 override propagation | roadmap | How the 2026-03-25 override resolves remaining M003 planning drift after the earlier hard steer | Treat M003 as the current admin Persona detail / admin knowledge detail → practice session creation → voice_policy_snapshot/runtime retrieval → knowledge-check/report/replay business chain. This supersedes any residual M003 wording from earlier planning that still used invented live status names, abstract prompt-first scope, or environment/tooling work outside that runnable chain. | The earlier hard steer set the right direction, but the remaining M003 documents still mixed real routes/modules with non-live vocabulary such as `no_bound_kb` / `weak_evidence` and left enough ambiguity for roadmap scope to drift again. Narrowing the milestone to the confirmed business chain keeps acceptance tied to real code, protects user-value focus, and prevents future slices from burning scope on Conda/.env/lockfile-style work that does not move the product loop. | Yes | human |
| D049 | M003/S01/T03 | reader-contract | Which backend routes own the accepted M003 proof surfaces for report/knowledge-check versus replay | Keep session creation, knowledge-check, and report proof on `backend/src/common/api/practice.py`, but treat replay proof as the current `GET /api/v1/sessions/{id}/replay` route in `backend/src/common/conversation/api.py` backed by `backend/src/common/conversation/replay.py` over `SessionEvidenceService`. | The current learner report page already depends on the unified `/practice/sessions/{id}/report` plus knowledge-check routes, while the learner replay page is served through the conversation replay API and only shares the read-side fact line via `SessionEvidenceService`. Flattening replay into `practice.py` would point later tests and live UAT at the wrong backend seam and make placeholder ownership look real. | Yes | agent |
| D050 | M003/S02/T01 | architecture | How Persona pressure behavior should be represented during the M003/S02 migration | Store a canonical nested `customer_pressure` model inside `persona_policy`, keep `sales_focus` / `value_axes` / `objection_axes` / `expected_customer_questions` as derived compatibility projections, and flag raw rows that still lack the nested model as `pressure_model_legacy_only` in policy-health audits. | T02 and T03 need one structured contract they can freeze into session snapshots and expose on admin surfaces, but the current runtime/compiler/tests still read the flat sales-focus fields. Deriving the flat fields from the nested model preserves the existing authority chain while giving audits a clear way to distinguish truly legacy rows from rows that have already been normalized and persisted. | Yes | agent |
| D051 | M003/S02/T02 | runtime-contract | How the M003/S02 runtime snapshot should expose Persona pressure behavior | Expose `customer_pressure` as a top-level effective-policy/session-snapshot field, keep `persona_policy.customer_pressure` as the canonical nested source, and tag `source.customer_pressure_source` for inspection | T02 needs one runtime-facing pressure contract that the compiler, session creation path, and later inspection surfaces can read without reinterpreting prose instructions or live admin config. Mirroring the nested model onto the effective policy keeps the existing persona authority chain intact while making frozen session snapshots directly auditable. | Yes | agent |
| D052 | M003/S02/T03 | admin-persona UX | How current admin Persona pages should expose and persist the pressure model | Reuse the existing `/admin/personas/policy-health` audit on the list page, and edit/persist `persona_policy.customer_pressure` on the detail page while also mirroring the flat legacy sales-focus fields for compatibility. | The task explicitly forbids a new management surface. Using the existing policy-health endpoint keeps audit on the current list page, while the detail page remains the single editing surface. Writing both the nested pressure model and the compatibility mirrors avoids downstream drift for consumers that still read the legacy flat keys. | Yes | agent |
| D053 | M003 S03 T01 | architecture | How to persist the cross-turn unresolved-objection ledger without adding a new store | Reuse existing message transcript metadata and runtime snapshot state: persist the structured ledger under ConversationMessage.transcript_metadata["objection_ledger"] and mirror the same normalized dict in StepFun session runtime_state["objection_ledger"]. | This keeps the ledger on the current runtime/evidence chain, avoids schema churn, and uses a read-side field that already passes through replay/report projections without changing the public score contract. Reconnect recovery also stays minimal because the same normalized dict can ride inside the existing session snapshot. | Yes | agent |
| D054 | M003/S03/T02 | runtime-state | What reconnect state should preserve unresolved-objection pressure for sales realtime sessions | Persist the normalized objection ledger plus feedback pacing state, and stop depending on replaying the last action card from reconnect snapshots | The ledger carries the unresolved business gap across topic drift and reconnect, while feedback pacing state suppresses same-turn duplicate coaching. Persisting only these reconnect-safe facts avoids replaying stale action-card UI state while keeping the runtime pressure contract intact on the next turn. | Yes | agent |
| D055 | M003/S03/T03 | read-side | How read-side sales issue/goal surfaces should resolve when an unresolved objection ledger is still open | Prefer the latest open transcript-metadata objection ledger over generic score-stage alignment when projecting main_issue and next_goal | The websocket runtime already persists one normalized unresolved-objection ledger specifically to survive topic drift and reconnect. If report/replay/history fall back to the last score-stage alignment, completed sessions can drift back to next-step or generic gaps and stop explaining the proof request that actually blocked the conversation. Reusing the existing main_issue/next_goal fields with ledger-backed text keeps the contract stable while carrying the real blocker forward. | Yes | agent |
| D056 | M003/S03 | realtime-coaching | How realtime feedback should stay anchored to one unresolved objection when score focus drifts later in the same session | Inject objection-ledger-derived synthetic objection stage/score context into the shared feedback arbiter, while keeping the persisted/public `score_update` snapshot on the real scoring contract | The product needs the AI customer to keep pressing the same unresolved proof gap across topic drift and reconnect, but changing the public score payload would reintroduce contract drift across practice, report, replay, and admin. Using ledger-derived override context keeps pressure stable at the arbiter seam without inventing a second runtime store or mutating the outward score contract. | Yes | agent |
| D057 | M003/S04/T01 | architecture | How to carry S04 claim-truth semantics through the current sales evaluator and read-side projection | Store the canonical truth state as `effectiveness_snapshot.claim_truth` and let `SessionEvidenceService` overlay it from score-alignment and objection-ledger facts without renaming `main_issue` or `next_goal`. | This keeps the truth contract on the existing evaluator/session-evidence authority line, preserves stable report/replay keys for current consumers, and lets later runtime/report/replay work read one explicit status (`unsupported_claim`, `weak_evidence`, `evidence_pending`, `evidence_verified`) from the same snapshot even when issue/goal wording is overridden by newer evidence. | Yes | agent |
| D058 | M003/S04/T02 | runtime-diagnostics | How S04 runtime claim-truth should surface without creating a new debug contract or mutating the stable persisted score_snapshot shape | Emit claim_truth on the existing StepFun score_update websocket payload, cache the latest live claim_truth on the handler/reconnect snapshot, and have /practice/sessions/{id}/knowledge-check read live handler state first with completed-session projection fallback. | The product already has two accepted runtime inspection seams: score_update on the live practice page and knowledge-check on the report/runtime diagnostics path. Reusing them exposes the new truth contract immediately, keeps _latest_score_snapshot and persisted score_snapshot stable for existing consumers, and avoids trying to persist extra score fields through StepFun message helpers that intentionally normalize the public snapshot shape. | Yes | agent |
| D059 | M003/S04/T03 | frontend-reader-contract | How learner-facing report and replay should source and display S04 claim-truth states | Read claim-truth from the unified completed-session `effectiveness_snapshot.claim_truth` on report/replay and format it through a shared frontend helper, instead of borrowing `/knowledge-check` runtime diagnostics as the primary truth line. | The completed-session report and replay routes already share SessionEvidenceService-backed evidence contracts. Sourcing the learner-facing truth badge from `effectiveness_snapshot.claim_truth` keeps those read surfaces on the same canonical fact line, while letting runtime-only diagnostics like kb-lock chain failure remain separate in `/knowledge-check` instead of leaking infrastructure state into completed-session coaching copy. | Yes | agent |
| D060 | M003/S05/T03 | observability | What counts as shippable degradation versus release-blocking failure for the M003 same-session objection-heavy proof | Projection-backed canonical report degradation is shippable when it stays truthful and explicit, but replay/highlights blocked by a session stuck in scoring is release-blocking. | The S05 live same-session proof showed the report route still builds a truthful evidence projection and the report page keeps the learner on an inspectable evidence surface even when enhanced insights/highlights fail. The same proof also showed replay/highlights remain blocked with SESSION_NOT_COMPLETED after report_generation_failed [NO_STAGE_RESULTS], and replay is one of the accepted M003 proof surfaces. Explicit replay failure is useful observability, but it does not satisfy final milestone acceptance. | Yes | agent |
| D061 | M004/S01/T01 | architecture | How explanation-rich replay/highlight evidence is carried on the backend authority line | Expose replay/highlight learning context through a nested `learning_evidence` object derived from `SessionEvidenceService` projection, while preserving flat compatibility fields (`sales_stage`, `stage_name`, `context`, `suggested_response`). | The nested object gives later slices one stable contract for deep links and retry intent without introducing a second scorer or freeform learning generator, and keeping the flat fields avoids breaking the existing replay/highlight UI while that adoption happens incrementally. | Yes | agent |
| D062 | M004/S01/T02 | frontend-contract | How the replay/highlight UI should consume the new learning-evidence contract | Bind replay page and highlight components to the shared web API `HighlightItem`/`ReplayLearningEvidence` types instead of maintaining separate local highlight interfaces. | T01 established a nested backend learning-evidence payload. Reusing the shared API types on the replay/highlight surfaces keeps the frontend on the same authority line, preserves fallback fields like `stage_name` and `suggested_response`, and reduces drift risk for the follow-up report/history work in T03. | Yes | agent |
| D063 | M004/S01/T03 | frontend-contract | How report and history surfaces should derive user-facing learning cues for completed sessions | Derive issue/goal coaching cues from the shared session-evidence contract (`main_issue` / `next_goal`) through a shared frontend helper, while treating highlights and enhanced reports as optional overlays. | Replay/highlight already use the unified evidence line for explanation-rich coaching. Reusing the same issue/goal vocabulary on report and history prevents conflicting copy when highlights are absent or enhanced report generation degrades, and keeps the current entrypoints aligned without adding a second truth line. | Yes | agent |
| D064 | M004 S02 T01 | architecture | Where replay deep-link anchors should live for report issue/goal navigation | Attach a nested `replay_anchor` payload to `replay.main_issue` and `replay.next_goal`, derived from existing replay messages and timeline markers, with `status`/`degraded_reason` for fallback visibility. | This keeps anchor resolution on the same backend authority line that already owns replay conclusions, avoids introducing a parallel deep-link resolver or second evidence model, and lets the replay page surface degraded states such as missing highlights or markers without silently dropping the target. | Yes | agent |
| D065 | M004/S02/T02 | frontend-routing | How report-page replay deep links should target the existing replay route | Encode report deep links as replay-route query params (`focus`, `message_id`, `turn`, `anchor_status`, `anchor_reason`, `marker_type`, `marker_timestamp_ms`) and let highlight cards fall back to `focus+turn` when only a turn target is available. | T02 needs a stable handoff from the current report page into the existing replay route without adding a second evidence model. Reusing the replay anchor payload from the replay API keeps issue/goal navigation aligned to the canonical SessionEvidenceService projection, while a lightweight turn-only fallback keeps highlight-card navigation working on the current HighlightList seam until T03 consumes the query contract. | Yes | agent |
| D066 | M004/S02/T03 | frontend-routing | How the current replay page should consume report deep-link anchors | Parse the existing replay query params on the replay page, scroll to the requested turn when it still exists, and render a persistent resolved/degraded/missing anchor banner based on the query plus timeline marker lookup. | This keeps report→replay navigation on the existing route, makes degraded paths visible instead of silently failing when highlights or markers drift, and gives focused replay tests a stable UI contract for resolved and missing-target cases. | Yes | agent |
| D067 | M004/S03/T01 | architecture | How the M004/S03 retry focus should travel from completed-session evidence into a new practice session | Expose a sales-only `retry_entry.focus_intent` on report responses and persist accepted create-session focus under `voice_policy_snapshot.focus_intent` on the new session | This keeps the authority line on the existing report and practice APIs, avoids inventing a second retry-launch store, and gives the later practice-page/runtime descriptor work a stable carry-forward seam on the already-frozen session snapshot. | Yes | agent |
| D068 | M004/S03/T03 | frontend-runtime | How the learner practice page should read carry-forward retry focus | Expose carry-forward retry focus on `runtime_descriptor.focus_intent` and have the practice page consume that typed runtime descriptor field instead of reading raw `voice_policy_snapshot` directly. | The existing `/practice/sessions/{id}` session fetch already returns a canonical runtime descriptor. Extending that descriptor keeps the learner practice entry chain on one stable metadata surface, reuses the sanitized focus intent persisted at create-session time, and avoids adding a second fetch or frontend-only snapshot parsing path. | Yes | agent |
| D069 | M004/S03/T02 | retry-launch | How replay should source and execute the focused retry CTA in M004/S03 | Load retry metadata from canonical report `retry_entry` and reuse the existing `api.practice.createSession` + `/practice/{sessionId}` entry chain on both report and replay pages | This keeps report and replay aligned to one retry-launch contract, avoids inventing a replay-specific frontend flow, and lets `focus_intent` travel in the create-session body while scenario selection remains visible on the existing practice-route query params. | Yes | agent |
| D070 | M004/S04/T01 | reader-contract | How PPT page-level learning issues should travel on the existing report authority line | Attach per-page issue clusters under presentation_review.page_summaries[*].issue_clusters, and expose only aggregate cluster count/types through diagnostics and projection completeness. | T02/T03 need page-local evidence they can render directly on the current report and replay routes without inventing a second PPT evidence surface. Reusing page_summaries keeps the contract aligned to the existing presentation review line, while aggregate diagnostics/completeness fields preserve compact observability for logs and degraded-state inspection. | Yes | agent |
| D071 | M004/S04/T02 | ui-contract | How the shared PPT report route should surface page-level learning evidence | Render page-level issue details inside each existing `presentation_review.page_summaries[*]` card and drive the aggregate overview from `presentation_review.diagnostics.page_issue_cluster_count/page_issue_types` instead of creating a second PPT learning panel or relying on raw `issue_counts` alone. | The backend authority line already exposes page-scoped issue clusters with supporting evidence plus aggregate cluster diagnostics. Reusing those fields keeps the current report route truthful to the shared contract, shows learners exactly which page needs rework and why, and avoids a second frontend-only PPT evidence shape that would drift from replay/report consumers. | Yes | agent |
| D072 | M004/S04/T03 | architecture | How the current replay authority line should carry PPT page-level evidence and page targeting | Extend the existing replay payload with scenario_type/presentation_id/presentation_review for presentation sessions and consume page routing through replay query params (page, page_anchor_status, page_anchor_reason) on the current replay route. | T03 needed learners to move from the current PPT report conclusions into page-specific replay context without creating a second PPT-only replay API or UI surface. Reusing the existing replay contract keeps report/replay on one authority line, lets the replay page show explicit degraded page-anchor states, and preserves the current SlideViewer/transcript route instead of splitting facts across parallel endpoints. | Yes | agent |
| D073 | M004 S05 T01 | architecture | How the shared replay route should represent presentation sessions | Make `/api/v1/sessions/{id}/replay` mirror the shared report route: keep `scenario_type`/`presentation_id`/`presentation_review`, and null sales-only conclusion fields for presentation sessions. | The replay service had drifted off the shared session-evidence contract, which broke the replay route entirely and would have let PPT sessions leak stale sales-only fields even after the immediate NameError was fixed. Matching the report-route branch keeps report → replay → retry on one vocabulary and one route family for both sales and presentation. | Yes | agent |
| D074 | M005 S01 T01 | analytics | What semantic source the admin analytics overview/trends/leaderboard should aggregate from | Aggregate admin analytics from HistoryService / SessionEvidenceService projection summaries and expose evaluability metadata, instead of computing legacy weighted 0.4/0.3/0.3 scores in SQL. | This keeps admin analytics on the same completed-session truth line as learner report/replay and admin user drill-ins, prevents weighted-score drift, and gives downstream UI slices stable fields for evaluability, issue-family, and score-basis messaging. | Yes | agent |
| D075 | M005/S02/T01 | admin-workflow | How to persist the first manager workflow record on current admin APIs | Use a dedicated manager_interventions table with create/list/patch endpoints, and keep /admin/interventions/remind backward-compatible by optionally updating the latest open intervention instead of replacing the existing route. | The slice needs durable focus, reminder, and resolving-session linkage without introducing a generic task platform or breaking the current manager-lite reminder button. A small dedicated table keeps the lifecycle explicit, supports later user-surface wiring and result linkage, and lets the existing reminder entrypoint participate in persistence when an intervention already exists. | Yes | agent |
| D076 | M005/S02/T02 | ui-workflow | Which existing admin surface should own the new supervisor intervention workflow. | Use `/admin/users/[id]` as the primary create/inspect surface for manager interventions, and keep manager-lite as a deep-link launcher with prefilled focus query params. | The task goal was to close the loop on current admin surfaces with the smallest safe change. Keeping write/read authority on the user-detail page avoids a second workflow console, while query-param deep links let manager-lite seed the same focus/note context without duplicating form state or API orchestration. | Yes | agent |
| D077 | M005 S02 T03 | architecture | How manager intervention results link back to later training outcomes | Derive `manager_intervention_results` on the `/api/v1/admin/users/{id}/sessions` read path from persisted interventions plus `HistoryService`/session-evidence projections instead of mutating intervention rows during admin reads. | The admin user detail page already consumes the sessions endpoint and report drill-ins. Keeping result linkage as a read-side projection reuses the current evidence line, avoids GET-time writes and stale reminder/due-state side effects, and still lets supervisors inspect the latest meaningful outcome for each intervention on the existing surface. | Yes | agent |
| D078 | M005/S03/T01 | architecture | How asset-governance summaries should be exposed on current admin backend routes | Compute one shared runtime-backed governance summary per asset via RuntimeStatusService and merge route-specific change/local-health signals on the existing list endpoints for knowledge bases, personas, presentations, and voice runtime profiles. | The same release-health reader already owns typed anomaly classification across completed, scoring, knowledge-lock, and degraded presentation sessions. Reusing that seam keeps governance surfaces on the canonical runtime/report diagnostics line, while allowing each route to add asset-local change facts such as failed knowledge documents or persona policy drift without inventing a second anomaly model. | Yes | agent |
| D079 | M005/S03/T03 | architecture | How to expose asset-governance change context on existing analytics and user-detail inspection surfaces | Attach linked_asset_changes references to support/runtime fault diagnostics and render those references directly on the current analytics and user-detail pages instead of creating a new drill-in API. | The support/runtime fault line already carries the canonical anomaly context. Extending that payload with recent asset-change references keeps the linkage evidence-based, lets analytics and user drill-ins stay on their current data chain, and avoids adding another admin surface or parallel governance endpoint. | Yes | agent |
| D080 | M005/S03 | admin-governance-ui | How M005/S03 should surface asset governance on the current admin UI | Reuse the existing knowledge, persona, presentation, and voice-runtime pages with one shared inline governance summary component instead of adding a separate governance console | Operators already manage these assets on the current list/detail pages. Keeping impact, recent-change, and health context inline preserves the working admin flow, keeps the new governance_summary contract readable across all four asset types, and avoids a second governance surface drifting away from the routes that actually mutate those assets. | Yes | agent |
| D081 | M005/S04/T01 | analytics-contract | How the weekly operating pack should derive blocker families and current risk membership | Normalize blocker families through HistoryService issue-family aliases, and treat the latest evaluable completed session per user as the source of truth for current not-passed risk membership. | The operating pack has to drill into the same issue-family vocabulary used by manager interventions and user-detail focus links. Looking at any historical failure within the window would keep users in the risk list even after a later pass, which drifts from the supervisor/report evidence line. Normalizing raw main_issue.issue_type values and anchoring risk status to each user’s latest evaluable completed session keeps cohort buckets, manager actions, and learner drill-ins aligned. | Yes | agent |
| D082 | M005/S04/T02 | admin-analytics | How the admin analytics page should consume the weekly operating-pack contract alongside the existing time-range filters | Keep the weekly operating pack on a fixed 7-day `/api/v1/admin/analytics/operating-pack` read and bind its `manager_lists` directly into the existing admin analytics page, while leaving overview/trends/leaderboard on the user-selected analytics range. | The operating pack is meant to answer this week’s manager questions—current risk, improvement, repeated blocker families, and department issue shape. Holding that panel to a fixed 7-day window preserves a stable operating cadence and matches the backend aggregation semantics, without removing the broader 30/90/all-time trend controls that the existing analytics route already provides. | Yes | agent |
| D083 | M005/S04/T03 | admin-ux | How admin weekly operating drill-ins preserve context across manager-lite, users list, and user detail surfaces | Use focusBucket plus focusIssueFamily/focusNote query params as the shared drill contract for admin users drill-ins. | The weekly operating pack now has multiple bucket types and real issue-family labels. Carrying explicit bucket and issue-family params keeps the users list/detail surfaces on the same evidence vocabulary and prevents manager-lite from collapsing every risk drill-in to a default evidence-gap focus. | Yes | agent |
| D084 | M005/S05 | uat-boundary | How M005/S05 should prove the admin operating chain is operationally usable | Use the shipped /admin/analytics weekly pack and export surface, keep admin-only RBAC on that same route family, and review outcomes on the canonical /practice/{sessionId}/report and /practice/{sessionId}/replay pages instead of introducing a separate acceptance-only workflow. | T01's focused regression pack, T02's live analytics→drill-in→reminder→report/replay proof, and T03's export/permission guardrails together show the current shipped surfaces already cover the real supervisor operating loop. Keeping UAT on those routes prevents a second acceptance-only flow from drifting away from the product paths managers and learners actually use. | Yes | agent |
| D085 | Post-M005 planning | architecture | How to package the abstraction/refactor follow-up after M005 | Plan one dedicated milestone (M006) focused on hardening current admin/evidence seams for reuse, rather than splitting the work into multiple parallel milestones or adding new admin surfaces first. | The identified work items share one goal: turn the validated M005 governance chain into a lower-drift, lower-cost extension surface. Keeping them in one milestone preserves cross-slice continuity between shared contracts, shared parsers, service extraction, and regression proof, while avoiding premature product-surface expansion. | Yes | collaborative |
| D086 | M006 S01 T02 | frontend contract | How admin user-detail drill-ins recover missing not-passed guidance text | Derive the not-passed focus note on the read side from the shared drill-in helper whenever `focusIssueFamily` is present but `focusNote` is omitted. | This keeps the destination page aligned with the shared launcher defaults, preserves the shipped risk-bucket copy even for partial/manual URLs, and avoids duplicating fallback-note logic inside the page component. | Yes | agent |
| D087 | M006/S01/T03 | frontend-patterns | Where admin linked-asset parsing and labels should live on the frontend | Centralize linked-asset change parsing, filtering, and label formatting in `web/src/lib/admin/linked-assets.ts`, and have `/admin/analytics` plus `/admin/users/[id]` consume that helper. | Both shipped admin pages were carrying nearly identical runtime-diagnostics parsers and asset-label maps. Keeping one shared helper removes drift risk while preserving the existing support/runtime contract and current UI copy. | Yes | agent |
| D088 | M006/S01/T01 | frontend-patterns | Where admin users drill-in URL generation should live on the frontend | Centralize admin user drill-in URL generation and default-note resolution in `web/src/lib/admin/drill-in.ts`, and have manager-lite plus `/admin/users` launchers call that helper while preserving the current query-string contract. | M005 established `focusBucket` + `focusIssueFamily` + `focusNote` as the shared admin drill contract. Keeping URL generation in one helper removes page-local drift between manager-lite and the weekly users list without changing the shipped `/admin/users/[id]?focusBucket=...` route family. | Yes | agent |
| D089 | M006/S02/T01 | architecture | Where to anchor the shared governance and linked-asset backend contract for M006/S02 | Define shared AssetGovernanceSummary and LinkedAssetChangeReference models in backend/src/common/db/schemas.py, then wrap runtime/support routes with explicit response envelopes that reuse those models without changing the JSON payload shape. | The admin knowledge, persona, presentation, runtime-profile, and support-runtime surfaces all need the same nested governance/change contract. Anchoring the shared models in common backend schemas keeps downstream Python models and OpenAPI surfaces aligned while preserving the shipped payload keys for existing clients and tests. | Yes | agent |
| D090 | M006/S02/T02 | frontend-contracts | Where frontend governance and linked-asset admin payloads are normalized | Normalize governance_summary and linked_asset_changes only inside web/src/lib/api/client.ts, and have admin helpers/components consume the shared typed contracts from web/src/lib/api/types.ts without re-parsing unknown payloads. | The previous setup normalized some admin payloads in the API client but still re-parsed unknown objects inside the governance card and linked-asset helper, which duplicated logic and let weak shapes leak past the client boundary. Keeping normalization in one place preserves a single source of truth, makes tests more focused, and lets downstream admin pages treat governance data as already-typed state. | Yes | agent |
| D091 | M006-S03-T01 | architecture | Where supervisor intervention write-side lifecycle rules live after the M006/S03 extraction | Create a dedicated `ManagerInterventionWriteService` under `backend/src/admin/services/` and keep `/api/v1/admin/interventions` handlers as transport/auth wrappers over that service. | The intervention write path already contains coupled state transitions for create/update/remind plus latest-open lookup. Centralizing those rules behind one service keeps the API payload stable while giving downstream result-resolution work a single lifecycle authority. | Yes | agent |
| D092 | M006/S03/T02 | architecture | How supervisor intervention result semantics are separated from HistoryService | Extract latest-evaluable intervention outcome rules into common.analytics.manager_intervention_results and keep HistoryService as the query/projection orchestrator that delegates to the resolver. | The latest-evaluable-after-creation rule is workflow-specific read semantics, not generic history aggregation. Pulling it into a dedicated resolver makes the seam explicit, reusable for downstream admin analytics, and easy to lock with delegation-focused tests while preserving the shipped API payload contract. | Yes | agent |
| D093 | M006/S03/T03 | frontend-contract | How pending supervisor intervention results should render on `/admin/users/[id]` | Show `最近结果：等待新训练` without a report drill-in until a follow-up completed session exists. | A pending intervention means there is no completed session after the intervention creation time. Reusing an earlier report link would fabricate result evidence and break the shared `manager_intervention_results` semantics that now distinguish pending from improved/still-blocked/not-evaluable outcomes. | Yes | agent |
| D094 | M006/S04/T01 | architecture | How backend asset-type metadata is resolved for support/runtime governance and linked-change payloads | Introduce a shared support.services.asset_registry registry that owns the current asset-type label, admin-path builder, and runtime-record reference extraction hooks, and make RuntimeStatusService consume that registry instead of inline per-type conditionals for those concerns. | This keeps the four shipped asset types on one source of truth, reduces duplicated label/path/ref logic between governance indexes and linked-asset enrichment, and makes future asset-type additions a registry-first change instead of editing multiple service conditionals. | Yes | agent |
| D095 | M006 S04 T02 | architecture | How frontend admin surfaces resolve current asset labels and admin routes for supported asset types | Use a shared frontend asset metadata registry as the single source of truth for knowledge base, persona, presentation, and runtime-profile labels plus admin list routes, and route linked-asset helpers and governance overviews through it. | This keeps the frontend aligned with the backend asset registry seam introduced in T01, removes duplicated string/path conditionals from analytics, user detail, and governance pages, and makes future asset-type additions flow through one metadata contract instead of scattered page-specific assumptions. | Yes | agent |
| D096 | M006-S05-T01 | architecture | How the remaining admin analytics/users read-model glue should be shared across pages in M006/S05. | Keep the admin seam route-shaped by splitting shared page adapters into `web/src/lib/admin/read-models.ts` and `web/src/lib/admin/runtime-faults.ts` instead of introducing a generic dashboard abstraction. | The slice contract explicitly required extracting shared pure adapters while preserving the current route semantics. A route-shaped split lets analytics, users list, and user detail share operating-pack, runtime-fault, and session/intervention derivations immediately without widening scope or creating a second state-management layer. | Yes | human |
| D097 | M006/S05/T02 | architecture | How the admin users page should consume the weekly operating-pack seam during the page migration | Reuse `web/src/lib/admin/read-models.ts` for users-page operating-pack and display-label derivation instead of adding a new users-specific hook/store abstraction | The existing shared admin seam is already route-shaped around current analytics/users/user-detail needs. Extending it with the users-page manager-lite fallback and display-label helpers removes duplication without creating a second client-state abstraction that downstream slices would have to reconcile. | Yes | agent |
| D098 |  | verification | What verification bar future shared admin read-model seam changes must satisfy | Use the current full M005 admin regression pack (backend analytics/support/users/interventions/asset-governance/RBAC/contract suites plus web analytics/asset-governance/user-detail/manager-lite suites) as the canonical acceptance bar for admin read-model seam refactors. | The T03 proof showed the shared admin seam is only trustworthy when the entire shipped admin route family stays green together; helper-only or page-local checks would miss cross-route contract drift. | Yes — if the shipped admin route family or its authority seams materially change, the canonical pack should be updated in one place. | agent |
| D099 | M007 planning | architecture | M007 的产品边界和 proof 边界应落在哪些现有 surfaces 上 | M007 只在现有 `/practice/{sessionId}`、`/practice/{sessionId}/report`、`/practice/{sessionId}/replay` 路由族及其现有 runtime/report/replay APIs 上完成 coach degraded/resumed 真相、same-session live proof 与 closure 对账，不新增第二套 debug surface 或 milestone-only API。 | 这次工作的目标是把 realtime coaching 真正封板，而不是通过新增页面、第二套观测台或临时 API 制造看似通过的验证。坚持在当前 learner/runtime/report/replay 路由族上收口，才能保证最终 proof 直接对应用户真实体验和后续维护面。 | No | human |
| D100 | M007/S01 planning | architecture | S01 的 coach-health authority 和 learner visibility 应如何收口 | 继续以 backend runtime snapshot / websocket `coach_health` 为唯一 authority，只在 `/practice/{sessionId}` page shell 上镜像同一个 `coachHealth` 对象，并保留 `/knowledge-check` 作为诊断面而不是轮询驱动。 | 现有 StepFun/classic handlers、`reconnected` snapshot 和 `/knowledge-check` 已经共享同一份 coach-health 语义。S01 的真实缺口是 reconnect truth hardening 与 learner-visible surface，而不是缺少第二套 API/轮询。沿用同一 authority 能直接交付 R009，并避免 reconnect、panel UI、diagnostics 三条线再次漂移。 | Yes | agent |
| D101 | M007 S01 T01 | runtime-contract | What reconnect payload should be sent after restoring coach-health runtime state | Emit a freshly rebuilt session snapshot from the restored handler state instead of echoing the raw persisted snapshot | The raw persisted snapshot can contain stale or malformed runtime_state values that the handler already normalized locally. Rebuilding the snapshot before emitting `reconnected` keeps StepFun, classic, and the frontend reducer aligned on one runtime authority and preserves omission semantics for recovered healthy state. | Yes | agent |
| D102 | M007/S02 planning | architecture | How S02 should achieve same-session live-to-report/replay coherence | Close same-session continuity by emitting one evaluator-backed live conclusion summary from the backend runtime/`/knowledge-check` seam, render that summary on the learner route without frontend-only family inference, and keep report/replay parity on the existing `SessionEvidenceService` projection with replay still completion-gated. | Research showed report and replay already share the canonical completed-session authority, while the real gap is live runtime continuity and classic parity. Making the backend emit one shared live summary avoids a second API or page-local mapping, keeps the learner route on the same authority line as `/knowledge-check`, and preserves the existing replay completion gate instead of masking lifecycle truth. | Yes | agent |
| D103 | M007 S02 T01 | architecture | How live same-session sales conclusions are derived and exposed during active runtime | Use one evaluator-backed live_session_summary object (main_issue, next_goal, claim_truth, alignment metadata) as the active-session authority, and let websocket score_update plus /knowledge-check read that summary instead of recomputing route-local mappings or falling back to persisted snapshots while a live handler is active. | StepFun and classic were drifting because one path emitted only claim_truth and the other emitted score updates before objection-ledger alignment. Centralizing the live summary on the evaluator keeps live coaching, runtime diagnostics, and knowledge-check on the same semantics and prevents stale completed-session snapshots from overriding an active session. | Yes | agent |
| D104 | M007/S02/T02 | frontend-runtime | What frontend state and rendering seam owns the learner-visible same-session sales conclusion during active practice | Persist a normalized `liveSessionSummary` in practice websocket reducer state from `score_update.data.live_session_summary`, render learner issue/goal/claim-truth copy from that object via shared `session-evidence` helpers, and preserve it across final-transcript cleanup while clearing only turn-bound `actionCard`/`fuzzyDetections`. | The backend already emits one evaluator-backed live conclusion contract. Keeping an explicit reducer field tied to that payload makes the learner cue inspectable in state, prevents page-level remapping from stage/score text, and avoids stale transient coaching affordances from masquerading as the stable same-session conclusion. | Yes | agent |
| D105 | M007/S02/T03 | reader-contract | How same-session report/replay parity is evaluated while replay keeps route-specific deep-link metadata | Compare report and replay on the canonical issue/goal/claim-truth family after stripping replay-only `replay_anchor`, while separately asserting the anchor remains present and replay stays blocked until completion | S02/T03 proved replay adds legitimate deep-link decoration on top of the SessionEvidenceService projection rather than a second conclusion authority. Locking parity at the canonical family level preserves the current completion gate and keeps report-readable-during-scoring semantics from being misread as replay drift. | Yes | agent |
| D106 | M007/S03 planning | milestone-governance | Which milestone owns the remaining realtime-coaching closure authority after M007 absorbed unfinished M002 remediation work | Preserve M002 as the historical failed close-out/foundation milestone, and treat M007 as the sole current authority for unfinished R009 closure work and final proof, with historical `M002/S07-S08` intent mapped forward to `M007/S01-S04`. | Current runtime/product truth already moved to M007 through S01-S02, while M002 artifacts remain accurate only as preserved historical failed-closeout records. Making the authority switch explicit prevents future agents from chasing nonexistent M002 remediation slices, preserves the failed 2026-03-25 audit as history, and gives S04 one unambiguous milestone story for final validation and close-out. | Yes | agent |
| D107 | M007/S03 | milestone-governance | How generated GSD state should be handled after milestone authority switches | Align canonical docs first, then audit generated STATE.md/state-manifest.json separately; never hand-edit generated surfaces to fake agreement. | S03 proved the M002→M007 authority switch can be fully correct in PROJECT/REQUIREMENTS/DECISIONS and preserved milestone artifacts while STATE.md and state-manifest.json still lag. Treating generated surfaces as evidence rather than editable primary sources keeps close-out honest and leaves S04 with a clear generator-backed reconciliation target. | Yes | agent |
| D108 | M007/S04 planning | runtime-lifecycle | How S04 retires the remaining realtime-coaching closure blocker without masking lifecycle truth | For M007/S04, fix the fire-and-forget report trigger's own-session persistence seam first, keep replay/highlights completion-gated until persisted `completed`, and treat generator-backed close-out as a separate final step after live proof passes. | Research and code inspection both point at the background trigger's missing own-session persistence as the mechanical blocker behind `status="scoring"` wedges. Relaxing replay gating would hide that bug instead of retiring it, and combining product proof with generator-backed close-out in one task would blur whether failures come from runtime behavior or GSD state rendering. | Yes | agent |
| D109 | M007/S04/T01 | architecture | How the report-generation trigger should persist state on fire-and-forget execution | Commit report/finalization writes only when ReportGenerationTrigger owns the async DB session; keep injected-session execution flush-only | The production fire-and-forget path creates its own AsyncSessionLocal, so it must durably commit processing, report_status/report_error, and sales completion transitions itself. Caller-owned paths still follow the repository's explicit-commit policy and must not auto-commit from inside the trigger. | Yes | agent |
| D110 | M007/S04/T03 localhost closure proof | observability | What evidence line determines whether the StepFun same-session closure blocker is retired on localhost | Treat persisted session status plus same-session report/replay/highlights unlock as the closure authority; classify concurrent kb_not_ready and trigger-side no_scoring_context_available/report_generation_failed logs as optional noise when that authority line still passes | The fresh localhost proof produced the old trigger-side noise and KB-lock diagnostics, but the same session still advanced to completed and unlocked the shipped report/replay/highlights family on one canonical SessionEvidenceService projection. Judging closure by the raw trigger logs alone would misclassify a passing persisted route-family proof as a blocker and would contradict the learner-visible product truth. | Yes | agent |
| D111 | M007/S04/T04 | milestone-governance | How to correct stale milestone metadata when `state-manifest.json` still exports blank titles or obsolete statuses after a close-out render | Fix the underlying `.gsd/gsd.db` milestone rows, then rebuild `state-manifest.json` and `STATE.md` through the engine’s `writeManifest(...)` and `rebuildState(...)` helpers instead of editing the rendered files directly. | In this repo, the public GSD completion/plan tools can advance milestone status and render summaries, but they do not backfill an already-existing blank milestone `title`, and a historical milestone row can stay on an obsolete status such as `active` even after canonical docs moved on. Because `state-manifest.json` exports the milestone table verbatim, the honest fix is to correct the DB rows first and then rerender the generated surfaces through engine code. Editing the manifest or STATE markdown directly would hide the DB drift without fixing the source of truth. | Yes | agent |
| D112 | M008 planning | scope | What product line should own the next growth phase after M007 closes | Prioritize the auditable training chain (`knowledge base → retrieval → training → audio evidence → report provenance`) before expanding supervisor workflow, PPT realtime interruption, mobile, or external integrations. | The user explicitly redirected the roadmap away from wider workflow expansion and toward eliminating fake-credible training outputs. Until retrieval truth, audio auditability, and report provenance are hard, larger workflow or channel expansion would rest on an evidence chain the user does not trust. | Yes | human |
| D113 | M008 planning | architecture | Where the auditable retrieval/audio/report chain should be exposed | Keep M008-M010 on the current `knowledge-check` / report / replay route family; do not introduce a new audit console or debug-only route as the primary proof surface. | The current project already has strong shipped-route authority seams. The user's core complaint is that current user-visible outputs can feel credible without hard evidence, so the fix must land on the same surfaces users already consume. A separate audit console would hide the trust problem instead of solving it. | Yes | collaborative |
| D114 | M008 planning | reporting | Whether M008 infers `used_in_reasoning` from current retrieval evidence | Do not infer or expose `used_in_reasoning` in M008; limit the contract to retrieval occurrence, hit/miss/failure, and weak-evidence facts. | Current runtime evidence can prove that retrieval happened and what it returned, but not reliably that the model used the retrieved content in reasoning. Pretending otherwise would recreate the fake-credibility problem this milestone is trying to remove. | Yes | agent |
| D115 | M008 planning | architecture | How retrieval facts are persisted for M008 | Persist a bounded, provider-neutral retrieval ledger inside `voice_policy_snapshot.runtime_metrics.knowledge_retrieval` on the existing runtime metrics write path. | This preserves the current runtime persistence seam, avoids inventing a new table or route for a small factual ledger, and gives later report/provenance work one durable source of truth without exposing provider-specific raw payloads. | Yes | agent |
| D116 | M008 planning | reporting | Whether M008 infers `used_in_reasoning` from current retrieval evidence | Do not infer or expose `used_in_reasoning` in M008; limit the contract to retrieval occurrence, hit/miss/failure, and weak-evidence facts. | Current runtime evidence can prove that retrieval happened and what it returned, but not reliably that the model used the retrieved content in reasoning. Pretending otherwise would recreate the fake-credibility problem this milestone is trying to remove. | Yes | agent |
| D117 | M008/S01 planning | architecture | How S01 should shape and prove the persisted retrieval ledger | Store a bounded `recent_attempts` ledger under `voice_policy_snapshot.runtime_metrics.knowledge_retrieval`, with each entry normalized to query/status/result_count/retrieval_mode/error summary plus bounded transformed result summaries, and prove S01 on current session snapshot/detail surfaces while keeping existing `knowledge-check` flat counters backward-compatible. | D115 fixed the persistence seam but not the exact ledger boundary. Choosing a bounded `recent_attempts` list prevents snapshot bloat, preserves provider neutrality, and keeps the same transformed result shape available for later slices. Keeping S01 proof on current session/detail surfaces avoids prematurely redesigning report semantics that belong to S02 while still making the persisted truth inspectable on shipped routes. | Yes | agent |
| D118 | M008/S01/T02 | observability | How StepFun retrieval ledger entries should reach persisted session snapshots | Route one normalized ledger_event through the existing knowledge runtime-metrics update path and reject malformed persisted recent_attempts during snapshot merge instead of creating a second persistence channel or half-writing invalid ledger state. | The slice requires one retrieval attempt to map to one bounded snapshot update while keeping flat counters readable and copy-on-write semantics intact. Reusing the current runtime-metrics seam preserves the existing warning-only handler behavior, and rejecting malformed recent_attempts at merge time prevents partial session-row corruption. | Yes | agent |
| D119 | M008 S01 T03 | observability | How current session diagnostics read retrieval truth from persisted snapshots | Keep knowledge-check and related current-session readers backward-compatible by preferring the latest valid knowledge_retrieval.recent_attempts entry only as a fallback for missing or stale flat last_* fields, while leaving the existing counter/status contract and route family unchanged. | T02 already persists the provider-neutral retrieval ledger in runtime_metrics. Using that bounded ledger as a read-side fallback fixes snapshot drift for hit/miss/search_failed truth without introducing a new audit route, report redesign, or a second persistence seam. | Yes | agent |
| D120 | M008/S02 | architecture | How retrieval facts are normalized from persisted ledger for report/knowledge-check parity | Single `build_retrieval_facts(voice_policy_snapshot)` pure function in `runtime_diagnostics.py` as the canonical normalizer, preserving `knowledge_base_ids` and `result_summaries` that the existing lean normalizer `_normalize_knowledge_retrieval_attempt` drops | Without a single normalizer, report projection and knowledge-check diagnostics would each derive their own retrieval fact shape from the raw ledger, causing vocabulary drift. The pure function approach avoids side effects and makes parity trivially testable — both routes call the same function on the same persisted data. | Yes | agent |
| D121 | M008/S02 | architecture | How retrieval facts reach knowledge-check and report without recomputation | Copy-on-write overlay in `SessionEvidenceService.build_projection()` (sales-gated) → passthrough in `build_session_runtime_diagnostics()` when `live_runtime_active=False`; live sessions always derive truth from the live handler, not from persisted snapshots | The projection layer is already the canonical read model for completed sessions. Adding retrieval_facts as a copy-on-write overlay means (1) the persisted session.effectiveness_snapshot is not mutated, (2) both report and knowledge-check routes consume the same projection-derived payload, and (3) live sessions continue to use live handler truth without stale projection interference. | Yes | agent |
| D122 | M008/S03 planning | frontend architecture | What data source should the learner report page use for retrieval truth in M008/S03 | Treat `PracticeSessionReport.effectiveness_snapshot.retrieval_facts` as the report page's primary retrieval-truth source and keep `/api/v1/practice/sessions/{id}/knowledge-check` supplemental-only on that page. | S02 already proved report and knowledge-check parity through the shared `build_retrieval_facts()` normalizer and projection overlay. Reading retrieval truth from the canonical report payload prevents the learner UI from blanking whenever the supplemental request fails, keeps the user-visible route family aligned with D113, and preserves `claim_truth` as a separate orthogonal signal instead of merging it into diagnostics status copy. | Yes | agent |
| D123 | M009 planning | architecture | How M009 OSS integration is structured in the current FastAPI backend | Build a new OSS signing service in backend/src that uses the already-installed oss2 Python SDK with env-configured ALI_OSS_* credentials. Backend only issues signed PUT/GET URLs and registers metadata; browser does all actual audio transfer to/from OSS. | The current FastAPI backend has no OSS client code despite oss2 being installed and ALI_OSS_* credentials documented in oss配置.md. A new signing service follows the same pattern as the legacy NestJS OssService (documented in oss配置.md) but adapted for FastAPI. Server bandwidth is explicitly constrained — no audio content may transit through FastAPI. | Yes | agent |
| D124 | M009 planning | architecture | Where M009 audio segment metadata is persisted | Create a new session_audio_segments table for durable per-segment metadata. Use voice_policy_snapshot.runtime_metrics.audio_audit only for bounded live summary (latest segment, counts, upload status). PracticeSession.audio_url becomes the session-level manifest pointer. | voice_policy_snapshot is proven suitable for bounded runtime facts (D115, D117, D120) but inappropriate for unbounded asset catalogs. A dedicated table gives proper query support, status tracking, and failure attribution per segment without bloating the session snapshot. PracticeSession.audio_url already exists as a seam and can point to the manifest entry. | Yes | agent |
| D125 | M010 planning | evidence-contract | How M010 conclusion provenance should be shaped — conclusion-level vs per-suggestion | Attach provenance only to the canonical conclusion family (main_issue, next_goal, claim_truth); do not attempt per-suggestion provenance in M010. | The milestone context explicitly leans toward conclusion-level first. Per-suggestion provenance would explode scope into a second replay/highlight redesign without proving the core trust chain first. | Yes | agent |
| D126 | M010/S01 planning | evidence-contract | How conclusion evidence should be produced for report vs knowledge-check without creating two separate provenance paths | Implement one shared build_conclusion_evidence_bundle() in SessionEvidenceService.build_projection() and mirror its output through build_session_runtime_diagnostics(), so both projection-backed and diagnostics-backed routes speak the same provenance vocabulary. | Report/replay use SessionEvidenceService.build_projection(); knowledge-check uses build_session_runtime_diagnostics(). These already share build_retrieval_facts() but derive main_issue/claim_truth independently. A single shared builder prevents vocabulary drift at the source. | Yes | agent |
| D127 | M010/S02 planning | degradation-taxonomy | How M010 should unify degradation semantics across route families | Introduce a layered degradation taxonomy with canonical tokens (retrieval_degraded, transcript_degraded, audio_degraded, enhanced_report_degraded) as a new additive degradation_detail field; keep existing evidence_completeness and degraded_reasons backward-compatible. | Current degradation signals are scattered: evidence_completeness.missing_fields is coarse, audio degraded_reasons are audio-specific, and presentation degraded_reasons are presentation-specific. A new additive field with canonical tokens lets all three routes share one vocabulary without breaking existing consumers. | Yes | agent |
| D128 | M010/S03 planning | frontend-contract | How frontend should consume conclusion provenance without recreating page-local truth derivation | Render provenance and degradation through web/src/lib/session-evidence.ts shared helpers; do not let report or replay pages derive provenance from local stage names, highlight text, or action-card copy. | This repo already burned by page-local heuristics (D016, D104). session-evidence.ts is the established anti-drift seam. If M010 adds provenance derivation in page components, it will immediately drift from the backend contract. | Yes | agent |
| D129 | M010 planning | evidence-contract | How M010 backend provenance should be structured — one shared builder ( not a second. | Build one shared conclusion-evidence builder in shared conclusion-evidence projection (, and mirror it for runtime_diagnostics; no new truth source. | M009 showed this pattern works well ( the report/replay/history route already uses it pattern. The keeping the second builder and shared shape requires the page-local heuristics. Adding a page-local proof from not an writing provenance code, M010's provenance should build backend parity-first, frontend next, approach is the risky, trust drift, silent degradation. | Yes | agent |
| D130 | M010 planning | evidence-contract | How M010 degradation tokens are be surfaced vs the monitoring and diagnostics | Unified degradation taxonomy must have:4 layers (retrieval, transcript, audio, enhanced_report) each with one canonical token and human-readable explanation, not by route-specific wording. | M009 showed this pattern works: but this repo has already been burned by conflating enhanced-report failures with "canonical evidence signals. The Research warned against route-local mapping from a page-level heuristics, re-deriving the report/replay conclusions support, from stale stage/suggestion text or the solution vocabulary. S03 makes proven report↔replay parity by conclusion-level first, report/knowledge-check parity through the same shared `build_conclusion_evidence_bundle` builder. Knowledge-check diagnostics would route through the now-in `runtime_diagnostics.py` instead of `build_session_runtime_diagnostics()`. | Yes | agent |
| D131 | M010 planning | milestone-planning | Slice ordering for M010 | S01 backend contract first → S02 degradation taxonomy → S03 frontend rendering. Do not reverse this order. | The research showed this repo has been repeatedly burned by frontend work that preceded backend contract hardening. If S03 starts before S01, the frontend will necessarily invent page-local provenance derivation that drifts from whatever backend model S01 eventually produces. S01 must prove the contract before any UI work begins. | Yes | agent |
| D132 | M010 planning correction | evidence-contract | Correction for malformed M010 backend provenance planning decision | Supersede malformed decision D129: M010 backend provenance uses one shared conclusion-evidence builder on the existing authority seam, shared by projection-backed report/replay and mirrored into knowledge-check diagnostics; no second truth source is introduced. | D129 was saved with corrupted text. The intended decision is already reflected in the roadmap and other M010 decisions: provenance must be built once on the existing backend authority seam and reused across route families. | Yes — supersedes malformed D129 | agent |
| D133 | M010 planning correction | degradation-taxonomy | Correction for malformed M010 degradation-taxonomy planning decision | Supersede malformed decision D130: M010 degradation semantics use a four-layer additive taxonomy — retrieval_degraded, transcript_degraded, audio_degraded, enhanced_report_degraded — with one canonical token set shared by report, replay, and knowledge-check. | D130 was saved with corrupted text. The intended choice is the additive four-layer taxonomy already captured in the milestone roadmap and D127; this correction makes the durable record readable and usable for later slices. | Yes — supersedes malformed D130 | agent |
| D134 | M010/S01 planning | evidence-contract | Where to place the shared conclusion-evidence builder function | Place `build_conclusion_evidence_bundle()` as a static method on `SessionEvidenceService` in `session_evidence.py`, called inside `build_projection()` so report and replay automatically carry the bundle. Knowledge-check reads it from the already-built projection_effectiveness_snapshot. | The builder needs access to SessionEvidenceProjection fields (messages, effectiveness_snapshot, retrieval_facts, session). Keeping it on SessionEvidenceService avoids a circular import with runtime_diagnostics.py and maintains the existing authority seam pattern where projection is the single truth source. Knowledge-check already copies retrieval_facts from projection_effectiveness_snapshot; the same pattern works for conclusion_evidence. | Yes | agent |
| D135 | M010/S01/T01 | reader-contract | How conclusion-evidence provenance is surfaced on existing completed-session routes | Build `conclusion_evidence` inside `SessionEvidenceService.build_projection()` and expose it additively on report/replay payloads while keeping presentation sessions `null` | The shared projection is already the authority line for completed-session sales conclusions. Building provenance there keeps report and replay on the same evidence bundle, preserves backward compatibility for existing consumers, and avoids introducing route-local provenance logic that would drift. | Yes | agent |
| D136 | M010/S02 planning | degradation-taxonomy | Which field carries the authoritative M010/S02 layered degradation taxonomy | Use additive `evidence_degradation` on projection-backed sales-session payloads, and mirror only its canonical layer tokens into `evidence_completeness.degraded_reasons` for compatibility. | S02 needs one route-family truth for report, replay, and knowledge-check without breaking admin/history readers that still depend on `evidence_completeness.degraded_reasons`. Making `evidence_degradation` authoritative keeps the new four-layer contract explicit while preserving backward-compatible coarse completeness semantics and presentation-specific degraded reasons. | Yes | agent |
| D137 | M010/S02 | compatibility | How existing admin/history readers consume M010/S02 degradation semantics | Mirror canonical sales evidence_degradation tokens into evidence_completeness.degraded_reasons, but leave presentation degraded_reasons untouched | Admin analytics and history projection already aggregate degraded_reasons. Mirroring the authoritative evidence_degradation layer tokens preserves existing readers while keeping one new cross-route taxonomy, and avoids overwriting presentation-specific degradation semantics. | Yes | agent |
| D138 | M010/S02 | compatibility | How existing admin/history readers consume M010/S02 degradation semantics | Mirror canonical sales evidence_degradation tokens into evidence_completeness.degraded_reasons, but leave presentation degraded_reasons untouched | Admin analytics and history projection already aggregate degraded_reasons. Mirroring the authoritative evidence_degradation layer tokens preserves existing readers while keeping one shared cross-route taxonomy, and not overwriting presentation degraded_reasons avoids regressing PPT-specific degradation semantics. | Yes | agent |
| D139 | M010/S03/T01 | frontend architecture | How learner-facing conclusion provenance and evidence degradation are rendered on report/replay pages | Normalize conclusion_evidence and evidence_degradation through shared session-evidence.ts helper formatters before page rendering | The backend contract now carries shared provenance/degradation payloads across report, replay, and knowledge-check. Keeping token-to-copy mapping and malformed-fragment filtering inside session-evidence.ts gives report and replay one frontend authority seam, prevents page-local truth drift, and localizes failures to helper normalization versus route wiring. | Yes | agent |
| D140 | M010/S03/T02 | frontend-reader-contract | How replay should source conclusion provenance and evidence degradation after S03 parity work | Replay renders provenance/degradation exclusively from replayData.conclusion_evidence and replayData.evidence_degradation via shared session-evidence helpers, while the report snapshot remains retry-metadata-only. | This keeps report and replay on the same helper-owned learner vocabulary without introducing a second parser or letting stale report snapshots overwrite canonical replay payload truth. Focused cross-page tests now fail if either wording or authority source drifts. | Yes | agent |
| D141 | M010/S03 slice close-out | requirement | R027 | validated | M010/S03 completed the learner-facing half of the provenance contract: fresh slice-close verification passed `npm --prefix web test -- --run "src/app/(user)/practice/[sessionId]/report/page.test.tsx" "src/app/(user)/practice/[sessionId]/replay/page.test.tsx"` (33/33), proving report and replay both render canonical conclusion provenance and four-layer degradation from shared session-evidence helpers without page-local truth derivation or replay fallback to stale report snapshots. | Yes | agent |
| D142 | Post-runtime KB-QA hardening; moving from patch fixes to systemized knowledge-answering architecture | architecture | What execution substrate to use for the configurable knowledge-grounded answering system | Adopt Haystack as the retrieval/reranking/evaluation execution substrate while keeping project-owned database-driven control plane, audit trail, answerability logic, and runtime/report/replay contracts | Haystack provides mature pipeline composition, retriever/ranker components, and evaluation primitives that fit the Python backend well, but it should not replace project-specific entity resolution, business ranking policy, answerability, or audit/debug surfaces. Using it as the execution substrate lets the project stop hardcoding retrieval flow in handlers while preserving full control over configurable behavior in the database. | Yes | collaborative |
| D143 | M011/S01/T01 | architecture | How the initial KnowledgeAnswerEngine seam should isolate Haystack and expose engine output | Use a project-owned KnowledgeAnswerEngine with project-owned request/result schemas and a soft-imported Haystack pipeline factory seam. | This keeps the engine constructable before retrieval logic lands, prevents Haystack types from leaking into runtime/report/replay contracts, and establishes stable observability fields (audit_run_id, retrieval_summary, unsupported_claims) on the result contract from the first slice. | Yes | agent |
| D144 | M011/S01/T03 | architecture | How the knowledge-answer engine consumes active control-plane configuration from the database | Expose a repository-owned normalized snapshot DTO that carries config_version_id, config_version_name, and profile_source instead of returning SQLAlchemy ORM rows | This keeps ORM types out of the engine seam while preserving the observability fields downstream debug APIs need to explain which active config version and source produced a run. | Yes | agent |
| D145 | M011/S01/T02 | architecture | How to close the first knowledge-answer control-plane slice when ORM models already exist but schema history is missing | Treat the missing Alembic revision as the real task gap and protect ORM/migration alignment with a focused migration-presence regression | M011/S01/T02 found the control-plane ORM rows already present in backend/src/common/db/models.py, so adding duplicate model work would have faked progress while leaving deployment/schema history broken. The slice instead added the real Alembic revision for the knowledge config and answer-run audit tables, plus a regression test that fails if the migration file disappears or stops declaring the expected tables. | Yes | agent |
| D146 | M011-S02-T01 | query-understanding | How entity resolution should behave in M011/S02 | Use deterministic alias/canonical string matching with explicit resolution trace, and defer fuzzy NLP | The slice goal is to fix retrieval misses before ranking with the smallest auditable change. Deterministic matching is enough to normalize known aliases, preserve canonical pass-through, and expose exact match evidence for downstream intent/planner debugging without introducing opaque NLP behavior. | Yes | agent |
| D147 | M011/S02/T02 | architecture | How M011/S02 should encode intent-classifier rules and planner output before Haystack execution lands | Keep intent classification and retrieval planning on project-owned DTOs; support regex, keyword_contains, and entity_keyword_contains rules, then emit profile-owned progressive query steps instead of reusing legacy websocket helper state. | T02 needed a stable seam between the DB-backed control plane and the upcoming Haystack adapter. Encoding entity+keyword gating in a project-owned classifier result plus planner output keeps ORM rows and old websocket retrieval helpers out of the engine path, while preserving auditable traces and compatible rewritten-query behavior for downstream execution. | Yes | agent |
| D148 | M011/S02/T03 | architecture | How the new knowledge-engine retrieval seam should integrate with the existing StepFun internal knowledge search runtime | Load the active knowledge-engine config snapshot inside the StepFun runtime, use entity resolution → intent classification → retrieval planning → Haystack adapter → business reranker when config is available, and fall back to the legacy rewritten-query loop when it is not. | This keeps the shipped StepFun entrypoint on a single real retrieval path, preserves existing runtime guardrails and error semantics, exposes explainable execution/ranking observability, and avoids breaking sessions when the control-plane snapshot is absent or not yet configured. | Yes | agent |
| D149 | M011/S03/T01 | architecture | How coverage-based answerability should consume evidence slots while preserving compatibility | Evaluate slot coverage from row-level or metadata slot_hits/coverage_slots and fall back to hit-count verdicts when no answerability profile is configured | Downstream retrieval and rerank seams already pass plain row dicts with evolving metadata. Accepting slot coverage from both top-level and metadata keeps the answerability seam tolerant to minor row-shape differences, while count fallback preserves current callers until later slices wire profile-backed slot annotations everywhere. | Yes | agent |
| D150 | M011/S03/T02 | pattern | How the evidence-driven answer assembler distinguishes supported claims from unsupported claims in the initial deterministic assembly seam | Treat rows with a normalized snippet as citation-backed supported claims, and downgrade rows that only carry content text without a snippet into unsupported_claims while emitting a fixed learner-safe blocked_text for blocked answerability | Downstream realtime/report/replay surfaces need a stable, audit-friendly contract where final_text is assembled only from explicitly quoteable evidence, unsupported_claims preserves filtered assertions for diagnostics, and blocked runs do not leak backend failure detail into learner-facing copy. | Yes | agent |
| D151 | M011/S03 close-out | architecture | How S03 exposes knowledge-answer audit and answerability fields to existing realtime/runtime/replay consumers | Use a dedicated compatibility mapper to expose audit_run_id, citations, and answerability on the existing payload seams instead of forcing those consumers onto raw engine DTOs. | S03 verification proved the shipped integration points are the existing StepFun search payload, runtime diagnostics, websocket response payloads, and replay transcript metadata. Reusing a dedicated compat mapper keeps those surfaces stable, preserves learner/runtime contracts, and prevents raw engine result shapes from leaking into handlers and downstream readers. | Yes | agent |
| D152 | M011/S03 close-out | observability | How S03 persists knowledge-answer execution traces for later runtime diagnostics, report, replay, and debug inspection | Persist one ordered KnowledgeAnswerRun plus KnowledgeAnswerRunStep sequence per run and use that persisted audit line as the future inspection seam. | The engine test and audit-repo test proved that one answer run can durably capture resolved query, answerability, citations, retrieval summary, and ordered resolve/classify/plan/retrieve/rank/answerability/assemble steps. Persisting that ordered audit line avoids reconstructing execution from handler-local state and gives S04 a queryable inspection seam for recent runs. | Yes | agent |
| D153 | M011/S04/T01 | testing | How to evaluate the new knowledge-answer engine deterministically before rollout | Use a fixture-driven evaluation harness that runs the real KnowledgeAnswerEngine seam with stubbed retrieval responses and compares shipped answer payloads, including exact multiline final_text formatting. | This keeps evaluation coverage on the project-owned engine contract instead of runtime-specific handlers, makes product/pricing/compare/coaching/blocked cases reproducible without a live knowledge base, and prevents false negatives caused by whitespace-normalizing the assembler's numbered answer text. | Yes | agent |
| D154 | M011/S04/T02 | observability | How the knowledge-answer debug API should expose run inspection without rebuilding runtime-local traces | Ship a read-only admin/support debug router backed directly by persisted KnowledgeAnswerRun and KnowledgeAnswerRunStep rows, with separate list/detail/steps endpoints under /api/v1/knowledge-debug. | The persisted audit rows are already the cross-entrypoint authority seam established in S03. Reading them directly gives downstream debugging and operational inspection one truthful surface, keeps permissions narrow, and avoids reconstructing traces from StepFun handler-local state or replay-specific projections. | Yes | agent |
| D155 | M011/S04/T03 | architecture | How the new knowledge-answer engine should be rolled out on the existing StepFun internal search seam | Gate the engine at the compat seam with three explicit modes: legacy default, engine-enabled cutover, and dual-run shadow audit; keep the legacy payload user-visible in dual-run while persisting a shadow audit run when session_id is available. | The current runtime still serves stable learner/report/replay contracts through the legacy search payload shape, so rollout safety depends on making the engine an additive compat-layer choice instead of inlining a second partially visible path. A seam-owned rollout mode preserves existing behavior by default, enables truthful shadow comparisons through persisted KnowledgeAnswerRun rows, and limits the cutover blast radius to one place. | Yes | agent |
| D156 | M012/S01/T01 | authentication | What password hashing scheme should password-reset flows use for user-specific passwords? | Use a CryptContext with pbkdf2_sha256 as the primary scheme and bcrypt as a legacy verify fallback. | The local passlib+bcrypt backend in this repo cannot reliably mint new bcrypt hashes, so password-reset writes need a stable primary scheme. Keeping bcrypt in the context preserves compatibility for any existing bcrypt hashes while allowing new reset passwords to be stored and verified reliably. | Yes | agent |
| D157 | M012/S01 close-out | requirement | R031 | validated | M012/S01 added the login-page forgot-password entry point and complete self-service reset flow. Fresh verification passed `npm --prefix web test -- --run login`, covering the login-page forgot-password link plus forgot/reset page behavior and validation without requiring admin action. | Yes | agent |
| D158 | M012/S01 close-out | requirement | R029 | validated | M012/S01 delivered persisted password-reset tokens, forgot/reset-password APIs, frontend forgot/reset pages, and fresh verification via `backend/venv/bin/python -m pytest -c backend/pyproject.toml backend/tests/ -k password_reset -x -q` plus `npm --prefix web test -- --run login` proving self-service password recovery works without admin intervention. | Yes | agent |
| D159 | M012/S01 close-out | requirement | R030 | validated | M012/S01 replaced the dashboard hardcoded greeting/date/version with `useCurrentUser()` display-name/email-prefix fallback, time-based greeting logic, and `package.json` version sourcing. Fresh verification passed `npm --prefix web test -- --run dashboard`, asserting the real user name, dynamic version badge, and absence of the old hardcoded date. | Yes | agent |
| D160 | M012/S01 close-out | backend-dependency | How should backend TTS optional dependencies behave during unrelated test collection and fallback mode? | Keep `common.audio.tts_service` on lazy/guarded `edge_tts` import instead of a hard module-load import. | M012/S01 close-out verification showed repo-root backend pytest could fail before any password-reset tests ran because importing `presentation_handler` pulled in `common.audio.tts_service`, which hard-imported `edge_tts` and then crashed on a broken optional `aiohttp/multidict` stack. Guarding the import keeps unrelated suites collectable and preserves the intended browser-TTS fallback when the optional binary dependency chain is unavailable. | Yes | agent |
| D161 | M012/S02 planning | learner-shell-ux | How should S02 close learner feedback and route-resilience gaps without inventing new backend scope? | Use a frontend-only learner help/feedback affordance shared by dashboard and practice shells, and use App Router `error.tsx` boundaries with one shared learner fallback presenter for practice/report/replay. | The codebase already has working learner shells plus existing history/report routes, but no real support-contact persistence or ticketing backend. A shared frontend-only help entry closes the roadmap promise without binding to mock admin settings, while framework-native `error.tsx` boundaries retire the live-practice white-screen risk at the correct seam and avoid introducing the legacy class ErrorBoundary into App Router route failures. | Yes | agent |
| D162 | M012 S02 T01 | navigation/ui | How learner help/feedback should be mounted across learner shells | Use one frontend-only LearnerHelpEntry component mounted through shared shell seams (Sidebar/SidebarContent footer slot plus practice layout), and only expose bounded pathname/sessionId context. | This keeps the feedback affordance consistent across dashboard desktop, collapsed sidebar, mobile drawer, and practice shells without introducing new network dependencies or coupling to the unfinished admin support-email configuration. Sanitizing route/session context also satisfies the slice redaction constraint. | Yes | agent |
