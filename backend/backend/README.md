# Legacy Directory Notice

`backend/backend/` is a historical leftover directory and is **not** the active backend source root.

Use these paths for real development:

- Backend source: `backend/src`
- Backend tests: `backend/tests`
- Backend migrations: `backend/alembic`

This directory is kept only for compatibility/transition cleanup and should not be used for new code.
